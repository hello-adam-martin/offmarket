---
phase: 06-user-app-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/wanted/[id]/page.tsx
  - apps/web/src/app/property/[address]/page.tsx
autonomous: true
requirements: [USER-05, USER-06]

must_haves:
  truths:
    - "Wanted ad detail shows demand data with tabular-nums on all numbers"
    - "Wanted ad detail has no icon-in-circle patterns — all icons are inline w-5 h-5"
    - "Wanted ad detail has no gradient demand cards — all cards use flat .card class"
    - "Property view demand card uses border-l-4 border-accent"
    - "Match scores use single text-accent color — no green/yellow/orange tiers"
    - "Budget displays use tabular-nums with formatNZD"
  artifacts:
    - path: "apps/web/src/app/wanted/[id]/page.tsx"
      provides: "Wanted ad detail restyled"
      contains: "max-w-content"
    - path: "apps/web/src/app/property/[address]/page.tsx"
      provides: "Property view restyled"
      contains: "border-l-4 border-accent"
  key_links:
    - from: "apps/web/src/app/wanted/[id]/page.tsx"
      to: "globals.css"
      via: "card, badge-*, tabular-nums classes"
      pattern: "tabular-nums"
---

<objective>
Restyle the wanted ad detail page (889 lines, largest file) and property view page with DESIGN.md tokens, removing icon-in-circle patterns and gradient cards.

Purpose: These are the primary data display pages where buyers and owners see match information and demand data. Numbers must use tabular-nums for fintech-grade data rendering.
Output: 2 restyled files with flat cards, inline icons, tabular-nums on all numbers, and zero hardcoded colors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-app-pages/06-CONTEXT.md
@.planning/phases/06-user-app-pages/06-UI-SPEC.md
@apps/web/src/app/globals.css
@DESIGN.md

<!-- Phase 5 patterns for demand panel and match scores -->
@.planning/phases/05-owner-pages/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle wanted ad detail page</name>
  <files>apps/web/src/app/wanted/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/wanted/[id]/page.tsx
    - apps/web/src/app/globals.css
    - DESIGN.md
    - .planning/phases/06-user-app-pages/06-UI-SPEC.md
    - .planning/phases/05-owner-pages/05-02-SUMMARY.md
  </read_first>
  <action>
    **Layout per UI-SPEC Wanted Ad Detail:**
    - Replace any `max-w-3xl` / `max-w-4xl` / `max-w-7xl` with `max-w-content` per D-20
    - Two-column at lg: main content left (2/3 via `lg:col-span-2`), sidebar right (1/3 via `lg:col-span-1`). Single column below lg

    **Demand panel (sidebar):**
    - Use `.card` + `border-l-4 border-accent` per UI-SPEC (matches Phase 5 owner detail pattern)
    - Match total: `text-xl tabular-nums font-semibold font-display` per UI-SPEC
    - Direct/criteria split: inline text row, no colored sub-cards (matches Phase 5 D-03)

    **Match scores:**
    - ALL match scores use single `text-accent` color per UI-SPEC and Phase 5 D-05 decision
    - Remove any green/yellow/orange tier coloring
    - Score numbers: `tabular-nums font-semibold`

    **Address header:**
    - Remove icon-in-circle container (any `w-10 h-10 rounded-full bg-*` wrapping an icon)
    - Replace with inline icon `w-5 h-5 text-muted` before address text per UI-SPEC

    **Gradient card removal:**
    - Replace any `bg-gradient-to-br` / `bg-gradient-to-r` demand cards with flat `.card` class

    **Budget display:**
    - All price values: `tabular-nums` on container per D-28
    - Verify `formatNZD` usage retained (no changes to formatting logic)

    **Stat values throughout:**
    - `tabular-nums font-semibold text-xl font-display` per UI-SPEC

    **Cross-cutting token swaps:**
    - All `gray-*` -> semantic tokens per Token Reference:
      - `bg-gray-50` / `bg-gray-100` -> `bg-surface-raised`
      - `text-gray-400` / `text-gray-500` -> `text-muted`
      - `text-gray-600` / `text-gray-700` -> `text-secondary`
      - `text-gray-900` -> `text-primary`
      - `border-gray-200` / `border-gray-300` -> `border-border`
    - All `primary-*` -> `accent` / `accent-light` / `accent-hover` per D-23
    - All `green-*` -> `success` / `success-light` or `text-accent` per D-24
    - All `red-*` -> `error` / `error-light` per D-22
    - All `yellow-*` / `amber-*` -> `warning` per D-25
    - All `blue-*` / `purple-*` / `indigo-*` -> semantic tokens per D-26
    - All `rounded-full` on non-avatar elements -> `rounded-sm` per D-29
    - Loading skeletons: `bg-surface-raised animate-pulse` per D-27
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c 'gray-\|primary-[0-9]\|red-[0-9]\|green-[0-9]\|yellow-\|blue-[0-9]\|purple-\|indigo-\|bg-gradient' apps/web/src/app/wanted/\[id\]/page.tsx | grep -v ':0$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `wanted/[id]/page.tsx` contains `max-w-content`
    - `wanted/[id]/page.tsx` contains `border-l-4 border-accent` (demand panel)
    - `wanted/[id]/page.tsx` contains `tabular-nums` (at least 3 occurrences — demand total, match scores, budget)
    - `wanted/[id]/page.tsx` contains `text-accent` for match score values
    - `wanted/[id]/page.tsx` does NOT contain `bg-gradient` (gradient cards removed)
    - `wanted/[id]/page.tsx` does NOT contain `w-10 h-10 rounded-full` (icon-in-circle removed)
    - `wanted/[id]/page.tsx` does NOT contain `gray-` hardcoded classes
    - `wanted/[id]/page.tsx` does NOT contain `primary-` followed by a digit
    - `wanted/[id]/page.tsx` does NOT contain `green-[0-9]` or `red-[0-9]` hardcoded classes
  </acceptance_criteria>
  <done>Wanted ad detail renders with flat demand panel card, inline icons, single text-accent match scores, tabular-nums on all numbers, and zero hardcoded color classes across 889 lines</done>
</task>

<task type="auto">
  <name>Task 2: Restyle property view page</name>
  <files>apps/web/src/app/property/[address]/page.tsx</files>
  <read_first>
    - apps/web/src/app/property/[address]/page.tsx
    - apps/web/src/app/globals.css
    - DESIGN.md
    - .planning/phases/06-user-app-pages/06-UI-SPEC.md
  </read_first>
  <action>
    **Layout per UI-SPEC Property View:**
    - Replace any `max-w-3xl` / `max-w-4xl` with `max-w-content` per D-20
    - Follow Phase 5 property detail patterns

    **Demand card:**
    - Flat `.card` class (no gradient) per UI-SPEC
    - `border-l-4 border-accent` for demand data prominence per UI-SPEC

    **Cross-cutting token swaps:**
    - All `gray-*` -> semantic tokens per Token Reference
    - All `primary-*` -> `accent` / `accent-light` per D-23
    - All `green-*` -> `success` / `success-light` per D-24
    - All `red-*` -> `error` / `error-light` per D-22
    - All `blue-*` / `purple-*` -> semantic tokens per D-26
    - All `rounded-full` on non-avatar, non-image elements -> `rounded-sm` per D-29
    - Loading skeletons: `bg-surface-raised animate-pulse` per D-27
    - All numeric displays: `tabular-nums` per D-28
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c 'gray-\|primary-[0-9]\|red-[0-9]\|green-[0-9]\|blue-[0-9]\|purple-\|bg-gradient' apps/web/src/app/property/\[address\]/page.tsx | grep -v ':0$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `property/[address]/page.tsx` contains `max-w-content`
    - `property/[address]/page.tsx` contains `border-l-4 border-accent` (demand card)
    - `property/[address]/page.tsx` contains `tabular-nums`
    - `property/[address]/page.tsx` does NOT contain `gray-` hardcoded classes
    - `property/[address]/page.tsx` does NOT contain `primary-` followed by a digit
    - `property/[address]/page.tsx` does NOT contain `bg-gradient`
    - `property/[address]/page.tsx` does NOT contain `rounded-full` on badge elements
  </acceptance_criteria>
  <done>Property view renders with flat demand card, border-l-4 accent stripe, tabular-nums on numbers, and zero hardcoded color classes</done>
</task>

</tasks>

<verification>
- Zero hardcoded color classes in both files
- No icon-in-circle patterns remain
- No gradient backgrounds remain
- All numbers display with tabular-nums
- Demand panels use border-l-4 border-accent
- Match scores use single text-accent color
</verification>

<success_criteria>
- Wanted ad detail (889 lines) fully converted to semantic tokens
- Property view demand card uses flat card with accent border stripe
- All match scores use uniform text-accent color
- All numeric data uses tabular-nums
- Dark mode renders correctly via semantic tokens
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-app-pages/06-03-SUMMARY.md`
</output>
