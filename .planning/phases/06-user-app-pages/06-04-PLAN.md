---
phase: 06-user-app-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/saved-searches/page.tsx
  - apps/web/src/app/notifications/page.tsx
autonomous: true
requirements: [USER-07, USER-08]

must_haves:
  truths:
    - "Saved searches list items use card-compact with no icon-in-circle wrappers"
    - "Saved searches type badges use badge-info not bg-blue-100"
    - "Saved searches tip box uses bg-accent-light border-accent not bg-blue-50"
    - "Notifications list items have no icon-in-circle (w-10 h-10 rounded-full) wrappers"
    - "Notification icons are inline w-5 h-5 with semantic color mapping"
    - "Unread notifications distinguished by bg-surface and border-l-2 border-accent"
  artifacts:
    - path: "apps/web/src/app/saved-searches/page.tsx"
      provides: "Saved searches page restyled"
      contains: "badge-info"
    - path: "apps/web/src/app/notifications/page.tsx"
      provides: "Notifications page restyled"
      contains: "max-w-content"
  key_links:
    - from: "apps/web/src/app/notifications/page.tsx"
      to: "globals.css"
      via: "card-compact, badge-*, semantic tokens"
      pattern: "border-l-2 border-accent"
---

<objective>
Restyle the saved searches and notifications pages, removing icon-in-circle AI slop patterns and applying DESIGN.md semantic tokens throughout.

Purpose: These are list-heavy pages where icon-in-circle violations are concentrated. Both need the same pattern: inline icons replacing circle-wrapped icons, semantic badge classes, and card-compact list items.
Output: 2 restyled files with zero AI slop violations and zero hardcoded color classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-app-pages/06-CONTEXT.md
@.planning/phases/06-user-app-pages/06-UI-SPEC.md
@apps/web/src/app/globals.css
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle saved searches page</name>
  <files>apps/web/src/app/saved-searches/page.tsx</files>
  <read_first>
    - apps/web/src/app/saved-searches/page.tsx
    - apps/web/src/app/globals.css
    - DESIGN.md
    - .planning/phases/06-user-app-pages/06-UI-SPEC.md
  </read_first>
  <action>
    **Layout per UI-SPEC Saved Searches:**
    - Replace any `max-w-3xl` / `max-w-4xl` / `max-w-md` with `max-w-content` per D-20
    - Page heading: `text-xl font-display font-semibold text-primary`

    **List items:**
    - Each saved search item: `card-compact` class per UI-SPEC
    - Remove icon-in-circle wrappers: any `w-10 h-10 rounded-full bg-gray-100` (or similar) wrapping icons
    - Replace with inline icon: `w-5 h-5 text-muted shrink-0` per UI-SPEC

    **Type badges:**
    - Property type tags: replace `bg-blue-100 text-blue-800` (or similar hardcoded) with `badge-info` per UI-SPEC

    **Tip/info box:**
    - Replace `bg-blue-50` tip boxes with `bg-accent-light border border-accent text-accent rounded-md p-3` per UI-SPEC

    **Empty state:**
    - Use `EmptyState` component pattern (COMP-09) with inline icon, no circle container
    - Heading: "No saved searches"
    - Body: "Save a search to get notified when matching properties appear."

    **Delete confirmation:**
    - Confirm via `btn-destructive` labeled "Delete Search" per UI-SPEC Copywriting

    **Cross-cutting token swaps:**
    - All `gray-*` -> semantic tokens per Token Reference
    - All `primary-*` -> `accent` / `accent-light` per D-23
    - All `green-*` -> `success` / `success-light` per D-24
    - All `red-*` -> `error` / `error-light` per D-22
    - All `blue-*` -> `accent` / `accent-light` or `badge-info` per D-26
    - All `rounded-full` on non-avatar elements -> `rounded-sm` per D-29
    - Loading skeletons: `bg-surface-raised animate-pulse` per D-27
    - Link colors: `text-accent hover:text-accent-hover` replacing `text-primary-600`
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c 'gray-\|primary-[0-9]\|red-[0-9]\|green-[0-9]\|blue-[0-9]\|w-10 h-10 rounded-full' apps/web/src/app/saved-searches/page.tsx | grep -v ':0$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `saved-searches/page.tsx` contains `max-w-content`
    - `saved-searches/page.tsx` contains `card-compact` (list items)
    - `saved-searches/page.tsx` contains `badge-info` (type badges)
    - `saved-searches/page.tsx` contains `bg-accent-light` (tip box)
    - `saved-searches/page.tsx` does NOT contain `w-10 h-10 rounded-full` (icon-in-circle removed)
    - `saved-searches/page.tsx` does NOT contain `bg-blue-` hardcoded classes
    - `saved-searches/page.tsx` does NOT contain `gray-` hardcoded classes
    - `saved-searches/page.tsx` does NOT contain `primary-` followed by a digit
  </acceptance_criteria>
  <done>Saved searches page renders with card-compact list items, badge-info type tags, accent-light tip box, inline icons, and zero hardcoded color classes</done>
</task>

<task type="auto">
  <name>Task 2: Restyle notifications page</name>
  <files>apps/web/src/app/notifications/page.tsx</files>
  <read_first>
    - apps/web/src/app/notifications/page.tsx
    - apps/web/src/app/globals.css
    - DESIGN.md
    - .planning/phases/06-user-app-pages/06-UI-SPEC.md
  </read_first>
  <action>
    **Layout per UI-SPEC Notifications:**
    - Replace any `max-w-3xl` / `max-w-4xl` with `max-w-content` per D-20
    - Page heading: `text-xl font-display font-semibold text-primary`

    **Type filtering tabs:**
    - Container: `bg-surface-raised rounded-md p-1 inline-flex`
    - Active tab: `bg-surface rounded-sm text-accent font-medium shadow-sm`
    - Inactive tab: `text-secondary`

    **List items per UI-SPEC:**
    - Divider list: `border-b border-border py-4`
    - Remove icon-in-circle: any `w-10 h-10 rounded-full bg-gray-100` (or similar)
    - Replace with inline icon: `w-5 h-5 text-muted shrink-0` (no circle wrapper)

    **Unread vs read distinction per UI-SPEC:**
    - Unread: `bg-surface` + `border-l-2 border-accent`
    - Read: `bg-background` (default)

    **Notification icon color mapping per UI-SPEC:**
    - Info type: `text-accent`
    - Warning type: `text-warning`
    - Success type: `text-success`
    - System type: `text-muted`

    **Link colors:**
    - `text-accent hover:text-accent-hover` replacing `text-primary-600` per D-23

    **Empty state:**
    - Use `EmptyState` component pattern (COMP-09) with inline icon, no circle container
    - Heading: "No notifications"
    - Body: "You're up to date. Activity on your ads and properties will appear here."

    **Cross-cutting token swaps:**
    - All `gray-*` -> semantic tokens per Token Reference
    - All `primary-*` -> `accent` / `accent-light` per D-23
    - All `green-*` -> `success` / `text-accent` per D-24
    - All `red-*` -> `error` / `error-light` per D-22
    - All `blue-*` / `purple-*` -> semantic tokens per D-26
    - All `rounded-full` on non-avatar elements -> `rounded-sm` per D-29
    - Loading skeletons: `bg-surface-raised animate-pulse` per D-27
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c 'gray-\|primary-[0-9]\|red-[0-9]\|green-[0-9]\|blue-[0-9]\|purple-\|w-10 h-10 rounded-full' apps/web/src/app/notifications/page.tsx | grep -v ':0$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `notifications/page.tsx` contains `max-w-content`
    - `notifications/page.tsx` contains `border-l-2 border-accent` (unread indicator)
    - `notifications/page.tsx` contains `text-accent` (info icon color and links)
    - `notifications/page.tsx` contains `text-warning` (warning icon color)
    - `notifications/page.tsx` contains `text-success` (success icon color)
    - `notifications/page.tsx` does NOT contain `w-10 h-10 rounded-full` (icon-in-circle removed)
    - `notifications/page.tsx` does NOT contain `gray-` hardcoded classes
    - `notifications/page.tsx` does NOT contain `primary-` followed by a digit
    - `notifications/page.tsx` does NOT contain `blue-` or `purple-` hardcoded classes
  </acceptance_criteria>
  <done>Notifications page renders with inline icons, semantic color mapping per type, unread border-l-2 indicator, filter tabs with accent styling, and zero hardcoded color classes</done>
</task>

</tasks>

<verification>
- Zero hardcoded color classes in both files
- No icon-in-circle patterns (w-10 h-10 rounded-full bg-*) remain in either file
- Notification icons use semantic color mapping (accent/warning/success/muted)
- Saved search type badges use badge-info
- Both use max-w-content containers
</verification>

<success_criteria>
- Saved searches page uses card-compact list items with badge-info type tags
- Notifications page uses inline icons with semantic color mapping
- Unread notifications have border-l-2 border-accent visual distinction
- Both pages have zero icon-in-circle violations
- Both pages have zero hardcoded color classes
- Dark mode renders correctly via semantic tokens
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-app-pages/06-04-SUMMARY.md`
</output>
