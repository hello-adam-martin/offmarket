---
phase: 06-user-app-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/dashboard/page.tsx
  - apps/web/src/app/dashboard/DashboardClient.tsx
  - apps/web/src/app/profile/page.tsx
autonomous: true
requirements: [USER-01, USER-02]

must_haves:
  truths:
    - "Dashboard stat cards display numbers with tabular-nums and correct surface colors"
    - "Dashboard upgrade CTA card has no gradient background — uses flat card with border-l-4 border-accent"
    - "Dashboard Pro badge uses badge-info class, not rounded-full primary colors"
    - "Dashboard quick action links use bg-surface with hover:border-accent"
    - "Profile page form sections use .card class with .input and .label patterns"
    - "Profile page has no hardcoded gray-* or primary-* color classes"
  artifacts:
    - path: "apps/web/src/app/dashboard/DashboardClient.tsx"
      provides: "Dashboard client component restyled"
      contains: "badge-info"
    - path: "apps/web/src/app/dashboard/page.tsx"
      provides: "Dashboard server component restyled"
      contains: "max-w-content"
    - path: "apps/web/src/app/profile/page.tsx"
      provides: "Profile page restyled"
      contains: "max-w-content"
  key_links:
    - from: "apps/web/src/app/dashboard/DashboardClient.tsx"
      to: "globals.css"
      via: "badge-info, badge-neutral, card classes"
      pattern: "badge-info|badge-neutral|card"
---

<objective>
Restyle the dashboard (server + client components) and profile/settings page with DESIGN.md semantic tokens, removing all AI slop violations from the dashboard upgrade CTA card.

Purpose: Dashboard is the first page users see after login — it must exemplify the design system. Profile page is a standard form page that follows established patterns.
Output: 3 restyled files with zero hardcoded color classes and full dark mode compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-app-pages/06-CONTEXT.md
@.planning/phases/06-user-app-pages/06-UI-SPEC.md
@apps/web/src/app/globals.css
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle dashboard server component and DashboardClient</name>
  <files>apps/web/src/app/dashboard/page.tsx, apps/web/src/app/dashboard/DashboardClient.tsx</files>
  <read_first>
    - apps/web/src/app/dashboard/page.tsx
    - apps/web/src/app/dashboard/DashboardClient.tsx
    - apps/web/src/app/globals.css
    - DESIGN.md
    - .planning/phases/06-user-app-pages/06-UI-SPEC.md
  </read_first>
  <action>
    **dashboard/page.tsx (server component):**
    - Replace any `max-w-7xl` / `max-w-6xl` / `max-w-4xl` with `max-w-content` per D-20
    - Replace all `gray-*` classes with semantic tokens per the Token Reference in UI-SPEC:
      - `bg-gray-50` / `bg-gray-100` -> `bg-surface-raised`
      - `text-gray-400` / `text-gray-500` -> `text-muted`
      - `text-gray-600` / `text-gray-700` -> `text-secondary`
      - `text-gray-900` -> `text-primary`
      - `border-gray-200` -> `border-border`
    - Stat cards: wrap in `.card` class. Numbers get `tabular-nums font-semibold text-xl font-display`. Labels get `text-sm text-secondary` per D-28
    - Quick action links: `bg-surface border border-border rounded-md p-4 hover:border-accent transition-colors`. Text `text-primary` / `text-secondary`. Arrow icon `text-muted` per D-12
    - All `primary-*` colors -> `accent` / `accent-light` / `accent-hover` per D-23
    - All `green-*` -> `success` / `success-light` per D-24
    - All `red-*` -> `error` / `error-light` per D-22
    - All `rounded-full` on non-avatar elements -> `rounded-sm` per D-29
    - Loading skeletons: `bg-surface-raised animate-pulse` per D-27

    **dashboard/DashboardClient.tsx (client component):**
    - REMOVE `bg-gradient-to-r from-purple-50 via-primary-50 to-accent-50` from upgrade CTA card — replace with `card border-l-4 border-accent` per D-11
    - Replace Pro badge `bg-primary-100 text-primary-700 rounded-full` with `badge-info` per D-13
    - Replace "Free Plan" badge with `badge-neutral` per D-11
    - Usage progress bar track: `bg-surface-raised h-2 rounded-full`. Fill logic per D-14:
      - Normal (< 66%): `bg-accent`
      - High (>= 66%): `bg-warning`
      - Full (100%): `bg-error`
    - Usage warning badge: `badge-warning` or `badge-error` based on remaining count per D-11
    - All `gray-*` -> semantic tokens per mapping above
    - All `primary-*` -> `accent` / `accent-light` per D-23
    - All `blue-*` / `purple-*` -> semantic tokens or removed per D-26
    - All numeric displays (usage counts, stat values): add `tabular-nums` per D-28
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c 'gray-\|primary-[0-9]\|bg-gradient\|purple-\|blue-[0-9]' apps/web/src/app/dashboard/page.tsx apps/web/src/app/dashboard/DashboardClient.tsx | grep -v ':0$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `DashboardClient.tsx` contains `badge-info` (Pro badge per D-13)
    - `DashboardClient.tsx` contains `badge-neutral` (Free Plan badge per D-11)
    - `DashboardClient.tsx` contains `border-l-4 border-accent` (upgrade CTA per D-11)
    - `DashboardClient.tsx` does NOT contain `bg-gradient` (AI slop removed per D-11)
    - `DashboardClient.tsx` does NOT contain `purple-` (AI slop removed per D-26)
    - `DashboardClient.tsx` does NOT contain `rounded-full` except on avatar elements
    - `DashboardClient.tsx` contains `bg-accent` and `bg-warning` and `bg-error` (progress bar per D-14)
    - `page.tsx` contains `max-w-content`
    - `page.tsx` contains `tabular-nums` on stat number elements
    - `page.tsx` contains `hover:border-accent` on quick action links (D-12)
    - Neither file contains `gray-` hardcoded classes
    - Neither file contains `primary-` followed by a digit
  </acceptance_criteria>
  <done>Dashboard renders with DESIGN.md tokens: flat upgrade CTA card with accent left border, badge-info Pro badge, tabular-nums on all numbers, semantic surface colors, correct progress bar color tiers</done>
</task>

<task type="auto">
  <name>Task 2: Restyle profile/settings page</name>
  <files>apps/web/src/app/profile/page.tsx</files>
  <read_first>
    - apps/web/src/app/profile/page.tsx
    - apps/web/src/app/globals.css
    - DESIGN.md
    - .planning/phases/06-user-app-pages/06-UI-SPEC.md
  </read_first>
  <action>
    - Replace any `max-w-3xl` / `max-w-4xl` / `max-w-md` with `max-w-content` per D-20
    - Form sections: wrap each section in `.card` with `mb-6` gaps per UI-SPEC Profile section
    - Form fields: ensure `.input` class on all inputs/selects/textareas, `.label` class on all labels
    - Section dividers: `border-t border-border` replacing any `border-gray-200` per UI-SPEC
    - Save button: `btn-primary` at bottom of each section
    - Destructive account actions: `btn-destructive`
    - Success/error feedback: `text-success` / `text-error` inline
    - All `gray-*` -> semantic tokens per Token Reference:
      - `bg-gray-50` / `bg-gray-100` -> `bg-surface-raised`
      - `text-gray-400` / `text-gray-500` -> `text-muted`
      - `text-gray-600` / `text-gray-700` -> `text-secondary`
      - `text-gray-900` -> `text-primary`
      - `border-gray-200` / `border-gray-300` -> `border-border`
    - All `primary-*` -> `accent` / `accent-light` / `accent-hover` per D-23
    - All `red-*` -> `error` / `error-light` per D-22
    - All `green-*` -> `success` / `success-light` per D-24
    - All `rounded-full` on non-avatar elements -> `rounded-sm` per D-29
    - Page heading: `text-xl font-display font-semibold text-primary`
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c 'gray-\|primary-[0-9]\|red-[0-9]\|green-[0-9]\|blue-[0-9]' apps/web/src/app/profile/page.tsx | grep -v ':0$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `profile/page.tsx` contains `max-w-content`
    - `profile/page.tsx` contains `card` class on form section containers
    - `profile/page.tsx` contains `btn-primary` on save buttons
    - `profile/page.tsx` contains `border-border` (section dividers)
    - `profile/page.tsx` does NOT contain `gray-` hardcoded classes
    - `profile/page.tsx` does NOT contain `primary-` followed by a digit
    - `profile/page.tsx` does NOT contain `red-` followed by a digit
    - `profile/page.tsx` does NOT contain `green-` followed by a digit
  </acceptance_criteria>
  <done>Profile page renders with .card form sections, .input fields, .label labels, semantic tokens throughout, btn-primary save buttons, and no hardcoded color classes</done>
</task>

</tasks>

<verification>
- Zero `gray-*`, `primary-*`, `red-*`, `green-*`, `blue-*`, `purple-*` hardcoded classes in all 3 files
- `max-w-content` used in all page containers
- `tabular-nums` on dashboard stat numbers
- No `bg-gradient` in any file
- No `rounded-full` on non-avatar elements
</verification>

<success_criteria>
- Dashboard upgrade CTA is a flat card with border-l-4 border-accent, no gradient
- Dashboard Pro badge renders as badge-info
- Dashboard progress bar uses accent/warning/error color tiers
- Profile form sections use .card containers with .input fields
- All 3 files have zero hardcoded color classes
- Dark mode renders correctly (automatic via semantic tokens)
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-app-pages/06-01-SUMMARY.md`
</output>
