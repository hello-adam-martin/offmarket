---
phase: 06-user-app-pages
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/upgrade/page.tsx
  - apps/web/src/app/claim/[code]/page.tsx
autonomous: true
requirements: [USER-09, USER-10]

must_haves:
  truths:
    - "Upgrade page header is left-aligned, not centered"
    - "Upgrade page Pro card has border-2 border-accent and badge-info Recommended tag"
    - "Upgrade page billing toggle uses bg-surface-raised with bg-surface active tab"
    - "Upgrade page price numbers use tabular-nums font-semibold"
    - "Upgrade page alert banners use semantic success/warning/error tokens"
    - "Claim page uses standard site Header and Footer, not standalone mini header"
    - "Claim page has no gradient backgrounds — uses bg-background"
    - "Claim page cards use standard .card class, not rounded-2xl shadow-xl"
    - "Claim page has no icon-in-circle patterns — success/error use border-l-4 cards"
    - "Claim code displays in font-mono tabular-nums (JetBrains Mono)"
  artifacts:
    - path: "apps/web/src/app/upgrade/page.tsx"
      provides: "Upgrade/pricing page restyled"
      contains: "border-2 border-accent"
    - path: "apps/web/src/app/claim/[code]/page.tsx"
      provides: "Claim flow restyled"
      contains: "font-mono tabular-nums"
  key_links:
    - from: "apps/web/src/app/claim/[code]/page.tsx"
      to: "components/layout (Header/Footer)"
      via: "import and render site Header + Footer"
      pattern: "Header|Footer"
---

<objective>
Restyle the upgrade/pricing page and claim flow page — the two most AI-slop-heavy files in Phase 6. The upgrade page needs left-aligned header, billing toggle restyle, and Pro card accent border. The claim page needs complete layout overhaul: remove standalone header/footer, remove all gradients, remove icon-in-circle patterns, use standard site layout.

Purpose: These pages have the highest concentration of DESIGN.md violations in the phase. The upgrade page is the revenue conversion page and must look polished. The claim page is the first-touch for postcard recipients and must use the standard site experience.
Output: 2 restyled files with zero AI slop violations and zero hardcoded color classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-app-pages/06-CONTEXT.md
@.planning/phases/06-user-app-pages/06-UI-SPEC.md
@apps/web/src/app/globals.css
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle upgrade/pricing page</name>
  <files>apps/web/src/app/upgrade/page.tsx</files>
  <read_first>
    - apps/web/src/app/upgrade/page.tsx
    - apps/web/src/app/globals.css
    - DESIGN.md
    - .planning/phases/06-user-app-pages/06-UI-SPEC.md
  </read_first>
  <action>
    **Layout per D-01, UI-SPEC Upgrade/Pricing:**
    - Replace any `max-w-3xl` / `max-w-4xl` / `max-w-7xl` with `max-w-content` per D-20
    - Page header: LEFT-ALIGNED (remove any `text-center` on heading) per D-01
    - Heading: `text-xl font-display font-semibold text-primary`
    - Two-column card grid at md+: `grid grid-cols-1 md:grid-cols-2 gap-6`. Free card left, Pro card right

    **Billing toggle per D-02:**
    - Outer container: `bg-surface-raised rounded-md p-1 inline-flex`
    - Active tab: `bg-surface rounded-sm text-accent font-medium shadow-sm`
    - Inactive tab: `text-secondary`
    - Savings badge next to yearly: `badge-success`

    **Free tier card per D-04:**
    - Standard `.card` class with default `border-border`
    - CTA button: `btn-secondary` (not primary — primary reserved for Pro)

    **Pro tier card per D-04:**
    - `.card` + `border-2 border-accent`
    - Tag at top: `badge-info` reading "Recommended" (text: `text-xs font-semibold uppercase tracking-wide`)
    - CTA: `btn-primary`

    **Feature checklist per D-03:**
    - Available features: checkmark SVG icon `w-5 h-5 text-accent` + `text-primary` feature text
    - Unavailable features: X SVG icon `w-5 h-5 text-muted` + `text-muted` feature text

    **Price display per D-06:**
    - Price numbers: `tabular-nums font-bold` (note: D-06 specifies font-bold, not font-semibold)
    - Period text: `text-secondary`

    **Alert banners (top of page) per D-05:**
    - Success (upgrade complete): `bg-success-light border border-success text-success rounded-md p-4` with copy "You're now on the Pro plan. All features are active."
    - Canceled: `bg-secondary-light border border-warning text-warning rounded-md p-4` with copy "Upgrade was not completed. Your plan has not changed."
    - Error: `bg-error-light border border-error text-error rounded-md p-4` with copy "Something went wrong with your upgrade. Contact support if this continues."

    **Cross-cutting token swaps:**
    - All `gray-*` -> semantic tokens per Token Reference
    - All `primary-*` -> `accent` / `accent-light` / `accent-hover` per D-23
    - All `green-*` -> `success` / `success-light` per D-24
    - All `red-*` -> `error` / `error-light` per D-22
    - All `yellow-*` / `amber-*` -> `warning` per D-25
    - All `blue-*` / `purple-*` / `indigo-*` -> semantic tokens per D-26
    - All `rounded-full` on non-avatar elements -> `rounded-sm` per D-29
    - Loading skeletons: `bg-surface-raised animate-pulse` per D-27
    - Remove any centered hero text patterns (AI slop) per D-01
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c 'gray-\|primary-[0-9]\|red-[0-9]\|green-[0-9]\|yellow-\|blue-[0-9]\|purple-\|indigo-\|amber-' apps/web/src/app/upgrade/page.tsx | grep -v ':0$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `upgrade/page.tsx` contains `max-w-content`
    - `upgrade/page.tsx` contains `border-2 border-accent` (Pro card per D-04)
    - `upgrade/page.tsx` contains `badge-info` ("Recommended" tag per D-04)
    - `upgrade/page.tsx` contains `badge-success` (savings badge per D-02)
    - `upgrade/page.tsx` contains `btn-primary` (Pro CTA per D-04)
    - `upgrade/page.tsx` contains `bg-surface-raised rounded-md p-1` (billing toggle container per D-02)
    - `upgrade/page.tsx` contains `tabular-nums font-bold` (price per D-06)
    - `upgrade/page.tsx` contains `bg-success-light` (success banner per D-05)
    - `upgrade/page.tsx` contains `bg-error-light` (error banner per D-05)
    - `upgrade/page.tsx` does NOT contain `text-center` on the page heading (left-aligned per D-01)
    - `upgrade/page.tsx` does NOT contain `gray-` hardcoded classes
    - `upgrade/page.tsx` does NOT contain `primary-` followed by a digit
    - `upgrade/page.tsx` does NOT contain `purple-` or `indigo-` classes
  </acceptance_criteria>
  <done>Upgrade page renders with left-aligned header, billing toggle with surface-raised container, Pro card with border-2 border-accent and badge-info tag, semantic alert banners, tabular-nums prices, and zero AI slop</done>
</task>

<task type="auto">
  <name>Task 2: Restyle claim flow page</name>
  <files>apps/web/src/app/claim/[code]/page.tsx</files>
  <read_first>
    - apps/web/src/app/claim/[code]/page.tsx
    - apps/web/src/app/globals.css
    - apps/web/src/components/header/Header.tsx
    - DESIGN.md
    - .planning/phases/06-user-app-pages/06-UI-SPEC.md
  </read_first>
  <action>
    **Layout overhaul per D-15, D-16:**
    - REMOVE standalone mini header and footer — replace with standard site `Header` and `Footer` component imports per D-16
    - Import `Header` from `@/components/header` (or wherever the shared Header is exported)
    - Import `Footer` from `@/components/footer` (or wherever the shared Footer is exported)
    - Wrap page content in `max-w-content mx-auto px-4` container per D-16
    - REMOVE all full-screen gradient backgrounds per D-15:
      - `bg-gradient-to-br from-primary-50 to-white`
      - `from-green-50 to-primary-50`
      - Any other `bg-gradient-*` classes
    - Use `bg-background` as page background per D-15

    **Card style per D-18:**
    - Replace ALL `rounded-2xl shadow-xl` cards with standard `.card` class (8px border-radius, no heavy shadow)

    **Icon-in-circle removal per D-17:**
    - REMOVE all icon-in-circle patterns: `rounded-full` containers wrapping success/error icons (e.g., `w-16 h-16 rounded-full bg-green-100` or similar)
    - Success state: `.card` + `border-l-4 border-success`. Inline success icon `text-success w-5 h-5`. Left-aligned text
    - Error state: `.card` + `border-l-4 border-error`. Inline error icon `text-error w-5 h-5`. Left-aligned text

    **Claim code display per D-19:**
    - Claim code text: `font-mono tabular-nums` (renders in JetBrains Mono)
    - Wrap in: `bg-surface-raised rounded-md px-4 py-3 text-center`

    **4 states per UI-SPEC Claim Flow:**
    - Loading: skeleton with `bg-surface-raised animate-pulse`
    - Valid / enter name: `.card` standard. Form fields `.input` + `.label`. CTA `btn-primary`
    - Success: `.card` + `border-l-4 border-success`. Heading "Property claimed successfully". Inline icon
    - Error / invalid: `.card` + `border-l-4 border-error`. Heading "This claim link is not valid". Body "The link may have expired or already been used. Check your postcard for details."

    **Cross-cutting token swaps:**
    - All `gray-*` -> semantic tokens per Token Reference
    - All `primary-*` -> `accent` / `accent-light` / `accent-hover` per D-23
    - All `green-*` -> `success` / `success-light` per D-24
    - All `red-*` -> `error` / `error-light` per D-22
    - All `blue-*` / `purple-*` -> semantic tokens per D-26
    - All `yellow-*` / `amber-*` -> `warning` per D-25
    - All `rounded-full` on non-avatar elements -> `rounded-sm` or removed per D-29
    - All numeric displays: `tabular-nums` per D-28
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c 'gray-\|primary-[0-9]\|red-[0-9]\|green-[0-9]\|blue-[0-9]\|purple-\|bg-gradient\|rounded-2xl\|shadow-xl' apps/web/src/app/claim/\[code\]/page.tsx | grep -v ':0$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `claim/[code]/page.tsx` contains import for `Header` component (site header per D-16)
    - `claim/[code]/page.tsx` contains import for `Footer` component (site footer per D-16)
    - `claim/[code]/page.tsx` contains `max-w-content` (page container per D-16)
    - `claim/[code]/page.tsx` contains `font-mono tabular-nums` (claim code per D-19)
    - `claim/[code]/page.tsx` contains `border-l-4 border-success` (success state per D-17)
    - `claim/[code]/page.tsx` contains `border-l-4 border-error` (error state per D-17)
    - `claim/[code]/page.tsx` contains `card` class on state cards (per D-18)
    - `claim/[code]/page.tsx` does NOT contain `bg-gradient` (gradients removed per D-15)
    - `claim/[code]/page.tsx` does NOT contain `rounded-2xl` (heavy cards removed per D-18)
    - `claim/[code]/page.tsx` does NOT contain `shadow-xl` (heavy shadows removed per D-18)
    - `claim/[code]/page.tsx` does NOT contain `gray-` hardcoded classes
    - `claim/[code]/page.tsx` does NOT contain `primary-` followed by a digit
    - `claim/[code]/page.tsx` does NOT contain `green-` followed by a digit (use success tokens)
  </acceptance_criteria>
  <done>Claim page renders with standard site Header/Footer, bg-background page, .card containers, border-l-4 success/error state cards, font-mono claim code, inline icons, and zero AI slop violations</done>
</task>

</tasks>

<verification>
- Zero hardcoded color classes in both files
- No gradient backgrounds in either file
- No icon-in-circle patterns in claim page
- No rounded-2xl or shadow-xl in claim page
- Claim page imports and renders site Header and Footer
- Upgrade page header text is NOT centered
- All prices/numbers use tabular-nums
</verification>

<success_criteria>
- Upgrade page is left-aligned with billing toggle, Pro card accent border, badge-info Recommended tag
- Upgrade page alert banners use semantic success/warning/error tokens
- Claim page uses standard site layout (Header + Footer + max-w-content)
- Claim page has zero gradient backgrounds
- Claim page state cards use border-l-4 with success/error
- Claim code renders in JetBrains Mono (font-mono tabular-nums)
- Both files have zero hardcoded color classes
- Dark mode renders correctly via semantic tokens
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-app-pages/06-05-SUMMARY.md`
</output>
