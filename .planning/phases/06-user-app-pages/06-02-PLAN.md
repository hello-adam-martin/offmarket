---
phase: 06-user-app-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/constants.ts
  - apps/web/src/app/inquiries/page.tsx
  - apps/web/src/app/inquiries/[id]/page.tsx
autonomous: true
requirements: [USER-03, USER-04]

must_haves:
  truths:
    - "Inquiry listing shows status badges using badge-warning/success/error/neutral classes"
    - "Inquiry detail message bubbles use bg-accent text-white for own messages and bg-surface-raised for others"
    - "Accept/Decline action buttons use badge-success/badge-error styling"
    - "STATUS_LABELS in constants.ts uses badge-* classes not hardcoded colors"
    - "Unread count indicator uses badge-info not bg-primary-600 rounded-full"
  artifacts:
    - path: "apps/web/src/lib/constants.ts"
      provides: "STATUS_LABELS with badgeClass field"
      contains: "badge-warning"
    - path: "apps/web/src/app/inquiries/page.tsx"
      provides: "Inquiry listing restyled"
      contains: "max-w-content"
    - path: "apps/web/src/app/inquiries/[id]/page.tsx"
      provides: "Inquiry detail restyled"
      contains: "bg-accent text-white"
  key_links:
    - from: "apps/web/src/app/inquiries/page.tsx"
      to: "apps/web/src/lib/constants.ts"
      via: "STATUS_LABELS import for badge classes"
      pattern: "STATUS_LABELS"
---

<objective>
Restyle the inquiries listing page, inquiry detail/thread page, and update STATUS_LABELS in constants.ts to use semantic badge classes for all inquiry statuses.

Purpose: Inquiries are the core communication channel between buyers and owners. The message thread and status badges must use DESIGN.md tokens for consistent visual hierarchy.
Output: 3 restyled files with badge-* status labels, accent message bubbles, and full token compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-app-pages/06-CONTEXT.md
@.planning/phases/06-user-app-pages/06-UI-SPEC.md
@apps/web/src/app/globals.css
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update STATUS_LABELS and restyle inquiries listing page</name>
  <files>apps/web/src/lib/constants.ts, apps/web/src/app/inquiries/page.tsx</files>
  <read_first>
    - apps/web/src/lib/constants.ts
    - apps/web/src/app/inquiries/page.tsx
    - apps/web/src/app/globals.css
    - DESIGN.md
    - .planning/phases/06-user-app-pages/06-UI-SPEC.md
  </read_first>
  <action>
    **constants.ts — STATUS_LABELS refactor per D-10:**
    Update the STATUS_LABELS dictionary. Rename `className` field to `badgeClass` to match Phase 4 pattern. New values:
    ```
    PENDING: { label: "Pending", badgeClass: "badge-warning" }
    ACCEPTED: { label: "Accepted", badgeClass: "badge-success" }
    DECLINED: { label: "Declined", badgeClass: "badge-error" }
    COMPLETED: { label: "Completed", badgeClass: "badge-neutral" }
    ```
    Update the TypeScript type from `className: string` to `badgeClass: string`.
    Search ALL files that import STATUS_LABELS and update references from `.className` to `.badgeClass`.

    **inquiries/page.tsx — full restyle per D-10, UI-SPEC Inquiries Listing:**
    - Replace any `max-w-3xl` / `max-w-4xl` with `max-w-content` per D-20
    - Filter tabs: container `bg-surface-raised rounded-md p-1 inline-flex`. Active tab `bg-surface border-b-2 border-accent text-accent font-medium`. Inactive tab `text-secondary`
    - Unread count indicator: replace `bg-primary-600 text-white rounded-full` with `badge-info` per UI-SPEC
    - Role badge (owner/buyer): `badge-neutral` per UI-SPEC
    - Status badges: use `STATUS_LABELS[status].badgeClass` directly (will render badge-warning/success/error/neutral)
    - List items: use `card-compact` or `border-b border-border` divider list
    - All `gray-*` -> semantic tokens per Token Reference
    - All `primary-*` -> `accent` / `accent-light` per D-23
    - All `green-*` -> `success` / `success-light` per D-24
    - All `yellow-*` -> `warning` per D-25
    - All `red-*` -> `error` / `error-light` per D-22
    - All `rounded-full` on status pills -> `rounded-sm` per D-29 (handled by badge-* classes)
    - Loading skeletons: `bg-surface-raised animate-pulse` per D-27
    - Page heading: `text-xl font-display font-semibold text-primary`
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c 'gray-\|primary-[0-9]\|yellow-\|red-[0-9]\|green-[0-9]\|blue-[0-9]\|bg-primary-600' apps/web/src/app/inquiries/page.tsx | grep -v ':0$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `constants.ts` contains `badgeClass: "badge-warning"` for PENDING
    - `constants.ts` contains `badgeClass: "badge-success"` for ACCEPTED
    - `constants.ts` contains `badgeClass: "badge-error"` for DECLINED
    - `constants.ts` contains `badgeClass: "badge-neutral"` for COMPLETED
    - `inquiries/page.tsx` contains `max-w-content`
    - `inquiries/page.tsx` contains `badge-info` (unread count)
    - `inquiries/page.tsx` contains `badge-neutral` (role badge)
    - `inquiries/page.tsx` does NOT contain `gray-` hardcoded classes
    - `inquiries/page.tsx` does NOT contain `primary-` followed by a digit
    - `inquiries/page.tsx` does NOT contain `yellow-` or `red-` followed by a digit
    - `inquiries/page.tsx` does NOT contain `rounded-full` on status elements
  </acceptance_criteria>
  <done>Inquiry listing renders with badge-* status labels, badge-info unread counts, filter tabs with accent styling, and zero hardcoded color classes</done>
</task>

<task type="auto">
  <name>Task 2: Restyle inquiry detail/thread page</name>
  <files>apps/web/src/app/inquiries/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/inquiries/[id]/page.tsx
    - apps/web/src/lib/constants.ts
    - apps/web/src/app/globals.css
    - DESIGN.md
    - .planning/phases/06-user-app-pages/06-UI-SPEC.md
  </read_first>
  <action>
    **Full restyle per D-07, D-08, D-09, D-10, UI-SPEC Inquiry Detail:**

    - Replace any `max-w-3xl` / `max-w-4xl` with `max-w-content` per D-20

    **Message bubbles per D-07:**
    - Own messages: `bg-accent text-white rounded-lg rounded-br-sm p-3`
    - Other's messages: `bg-surface-raised text-primary rounded-lg rounded-bl-sm p-3`
    - Timestamps: `text-xs text-muted mt-1`

    **Avatar fallback per D-09:**
    - Replace `bg-primary-100 text-primary-600` with `bg-accent-light text-accent rounded-full`
    - Size: `w-8 h-8`

    **Action buttons per D-08:**
    - Accept button: `bg-success-light text-success border border-success rounded-sm px-3 py-1.5 text-sm font-medium` (badge-success styling as button)
    - Decline button: `bg-error-light text-error border border-error rounded-sm px-3 py-1.5 text-sm font-medium` (badge-error styling as button)
    - Mark Complete: `btn-secondary`

    **Reply form per UI-SPEC:**
    - Textarea: `.input` class
    - Submit button: `btn-primary`
    - Placeholder text: "Write your reply..."

    **Status badge:** Use `STATUS_LABELS[status].badgeClass` per D-10

    **Message container accessibility:**
    - Add `role="log"` on message list container
    - Add `aria-live="polite"` for new messages area

    **Cross-cutting token swaps:**
    - All `gray-*` -> semantic tokens per Token Reference
    - All `primary-*` -> `accent` / `accent-light` per D-23
    - All `green-*` -> `success` / `success-light` per D-24
    - All `red-*` -> `error` / `error-light` per D-22
    - All `blue-*` -> `accent` / `accent-light` per D-26
    - Loading skeletons: `bg-surface-raised animate-pulse` per D-27
    - All numeric displays: `tabular-nums` per D-28
    - All `rounded-full` on non-avatar elements -> `rounded-sm` per D-29
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c 'gray-\|primary-[0-9]\|yellow-\|red-[0-9]\|green-[0-9]\|blue-[0-9]' apps/web/src/app/inquiries/\[id\]/page.tsx | grep -v ':0$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `inquiries/[id]/page.tsx` contains `bg-accent text-white` (own message bubbles per D-07)
    - `inquiries/[id]/page.tsx` contains `bg-surface-raised text-primary` (other's message bubbles per D-07)
    - `inquiries/[id]/page.tsx` contains `bg-accent-light text-accent` (avatar fallback per D-09)
    - `inquiries/[id]/page.tsx` contains `bg-success-light text-success` (accept button per D-08)
    - `inquiries/[id]/page.tsx` contains `bg-error-light text-error` (decline button per D-08)
    - `inquiries/[id]/page.tsx` contains `btn-secondary` (mark complete per D-08)
    - `inquiries/[id]/page.tsx` contains `btn-primary` (reply submit)
    - `inquiries/[id]/page.tsx` contains `role="log"` (accessibility)
    - `inquiries/[id]/page.tsx` contains `max-w-content`
    - `inquiries/[id]/page.tsx` does NOT contain `gray-` hardcoded classes
    - `inquiries/[id]/page.tsx` does NOT contain `primary-` followed by a digit
  </acceptance_criteria>
  <done>Inquiry detail renders with accent own-message bubbles, surface-raised other-message bubbles, badge-styled action buttons, accessible message container, and zero hardcoded color classes</done>
</task>

</tasks>

<verification>
- Zero `gray-*`, `primary-*`, `red-*`, `green-*`, `yellow-*`, `blue-*` hardcoded classes in all 3 files
- STATUS_LABELS uses `badgeClass` field with `badge-*` values
- Message bubbles render with correct accent/surface-raised colors
- Action buttons use badge-success/error styling
- `max-w-content` on all page containers
</verification>

<success_criteria>
- Inquiry listing shows badge-* status labels from updated STATUS_LABELS
- Inquiry detail message thread uses accent bubbles for own messages
- Accept/Decline buttons use success/error badge styling
- Avatar fallbacks use accent-light/accent colors
- All 3 files have zero hardcoded color classes
- Dark mode renders correctly via semantic tokens
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-app-pages/06-02-SUMMARY.md`
</output>
