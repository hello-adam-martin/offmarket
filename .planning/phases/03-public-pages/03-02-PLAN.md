---
phase: 03-public-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/auth/signin/page.tsx
  - apps/web/src/app/help/page.tsx
autonomous: true
requirements:
  - PUBL-04
  - PUBL-07

must_haves:
  truths:
    - "Auth page card uses .card class with semantic tokens"
    - "Auth page Google button retains hardcoded gray-300/gray-700 per D-14"
    - "Auth page divider uses border-border and bg-surface"
    - "Auth page error state uses bg-error/10 border-error/30 text-error"
    - "Help page heading is left-aligned, not centered"
    - "Help FAQ category tabs use rounded-sm bg-accent for active, not rounded-full bg-primary-600"
    - "Help FAQ chevron uses text-accent when open, text-text-muted when closed"
    - "Help FAQ category badges use badge-info, badge-success, badge-neutral"
    - "Help contact box uses bg-surface-raised rounded-lg"
    - "No gray-* hardcoded tokens remain (except Google button per D-14)"
  artifacts:
    - path: "apps/web/src/app/auth/signin/page.tsx"
      provides: "Restyled auth page with semantic tokens"
      contains: "text-text-base"
    - path: "apps/web/src/app/help/page.tsx"
      provides: "Restyled help/FAQ page with DESIGN.md tabs and accordion"
      contains: "badge-info"
  key_links:
    - from: "apps/web/src/app/auth/signin/page.tsx"
      to: "globals.css"
      via: ".card, .input, .btn-primary classes"
      pattern: "card|input|btn-primary"
    - from: "apps/web/src/app/help/page.tsx"
      to: "globals.css"
      via: ".card, badge-info, badge-success, badge-neutral classes"
      pattern: "badge-info|badge-success|badge-neutral"
---

<objective>
Restyle the auth/signin page (153 lines) and help/FAQ page (288 lines) with DESIGN.md semantic tokens, removing all hardcoded gray-*/primary-* tokens and AI slop patterns (centered headings, rounded-full pills, icon-in-colored-circles).

Purpose: Auth and help are medium-complexity pages with structural fixes (tab pills, accordion tokens, error states) beyond simple token swaps.
Output: Two fully restyled page files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@apps/web/src/app/globals.css
@apps/web/tailwind.config.ts
@.planning/phases/03-public-pages/03-UI-SPEC.md
@.planning/phases/03-public-pages/03-CONTEXT.md

<interfaces>
Available CSS classes from globals.css:
```
.btn-primary — bg-accent text-white rounded-md px-4 py-2
.btn-secondary — border border-border bg-surface text-text-base rounded-md px-4 py-2
.card — bg-surface border border-border rounded-lg p-6
.input — border border-border bg-surface rounded-md px-3 py-2
.label — text-sm font-medium text-text-base
.badge-info — teal badge styling
.badge-success — green badge styling
.badge-neutral — neutral badge styling
```

Tailwind semantic tokens:
```
bg-bg: #fafaf7 light / #0f0f1a dark
bg-surface: #ffffff light / #1a1a2e dark
bg-surface-raised: #f5f5f0 light / #252540 dark
bg-accent: #0d9488
text-text-base: #1a1a2e light / #fafaf7 dark
text-text-secondary: #6b7280
text-text-muted: #9ca3af
text-accent: #0d9488
text-error: #dc2626
bg-error: #dc2626 (use bg-error/10 for light background)
border-border: #e5e5e0 light / #374151 dark
max-w-content: 1120px
font-display: General Sans
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle auth/signin page with semantic tokens and DESIGN.md error state</name>
  <files>apps/web/src/app/auth/signin/page.tsx</files>
  <read_first>
    - apps/web/src/app/auth/signin/page.tsx (full 153-line file — must see current token usage)
    - apps/web/src/app/globals.css (.card, .input, .label class definitions)
    - .planning/phases/03-public-pages/03-UI-SPEC.md (Auth/signin page contract section)
    - .planning/phases/03-public-pages/03-RESEARCH.md (Auth violation inventory)
  </read_first>
  <action>
Restyle apps/web/src/app/auth/signin/page.tsx. This is a client component ("use client"). The centered card layout is preserved per D-13.

**Token replacements (apply throughout file):**
- `text-gray-900` -> `text-text-base`
- `text-gray-600` -> `text-text-secondary`
- `text-gray-500` -> `text-text-muted`
- `bg-gray-200` -> `bg-surface-raised` (skeleton fallback)
- `border-gray-200` -> `border-border`
- `bg-white` (divider span background) -> `bg-surface`
- `text-primary-600` (disclaimer links) -> `text-accent`
- `hover:text-primary-800` -> `hover:text-accent-hover`

**EXCEPTION per D-14 — DO NOT CHANGE the Google OAuth button:**
The Google button with `border border-gray-300 text-gray-700 hover:bg-gray-50` MUST remain exactly as-is. This is an intentional brand trust signal.

**Ensure .card class on the auth card container:**
If the card wrapper div does not already have `card` class, add it. If it has inline styling like `bg-white rounded-xl shadow-lg border border-gray-200 p-8`, replace with `card` class (which provides bg-surface, border-border, rounded-lg, p-6). Add `p-8` separately if more padding is needed: `card p-8`.

**Page heading typography:**
- Main heading: add `font-display` to make it General Sans. Target: `text-2xl font-display font-bold text-text-base`
- Subtitle: `text-md text-text-secondary`

**Ensure .input class on email field:**
If the email input does not already have `input` class, add it. Remove any inline border/focus styling that duplicates what `.input` provides.

**Ensure .label class on form labels.**

**Divider between Google and email auth:**
- Line: `border-t border-border` (replacing `border-gray-200`)
- Span: `bg-surface text-text-muted` (replacing `bg-white text-gray-500`)

**Error state:**
Replace `bg-red-50 border border-red-200 rounded-lg text-red-700` (or similar) with:
`bg-error/10 border border-error/30 rounded-md text-error text-sm p-3`

**Disclaimer links at bottom:**
- Text color: `text-text-muted` (replacing `text-gray-500`)
- Link color: `text-accent hover:text-accent-hover underline` (replacing `text-primary-600 hover:underline`)

**Skeleton/loading fallback:**
Replace any `bg-gray-200` with `bg-surface-raised` and add `animate-pulse` if not present.

**Max-width:** If the page outer container uses `max-w-*` other than `max-w-content`, leave it — the auth page uses a narrow centered layout which is correct per D-13. The card itself is narrow by design.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/auth/signin/page.tsx contains `text-text-base`
    - apps/web/src/app/auth/signin/page.tsx contains `text-text-secondary`
    - apps/web/src/app/auth/signin/page.tsx contains `text-text-muted`
    - apps/web/src/app/auth/signin/page.tsx contains `bg-error/10`
    - apps/web/src/app/auth/signin/page.tsx contains `border-error/30`
    - apps/web/src/app/auth/signin/page.tsx contains `text-accent`
    - apps/web/src/app/auth/signin/page.tsx contains `border-border`
    - apps/web/src/app/auth/signin/page.tsx contains `bg-surface`
    - apps/web/src/app/auth/signin/page.tsx contains `font-display`
    - apps/web/src/app/auth/signin/page.tsx contains `border-gray-300` (Google button preserved per D-14)
    - apps/web/src/app/auth/signin/page.tsx does NOT contain `text-gray-900`
    - apps/web/src/app/auth/signin/page.tsx does NOT contain `text-primary-600`
    - apps/web/src/app/auth/signin/page.tsx does NOT contain `bg-red-50`
    - apps/web/src/app/auth/signin/page.tsx does NOT contain `border-red-200`
    - Build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Auth page uses semantic tokens throughout. Google button preserved. Error state uses DESIGN.md error tokens. Disclaimer links use text-accent. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle help/FAQ page — tabs, accordion tokens, category badges, contact box</name>
  <files>apps/web/src/app/help/page.tsx</files>
  <read_first>
    - apps/web/src/app/help/page.tsx (full 288-line file — must see tab structure, accordion, badges)
    - apps/web/src/app/globals.css (.card, badge-info, badge-success, badge-neutral definitions)
    - .planning/phases/03-public-pages/03-UI-SPEC.md (Help/FAQ page contract)
    - .planning/phases/03-public-pages/03-RESEARCH.md (Help violation inventory)
  </read_first>
  <action>
Restyle apps/web/src/app/help/page.tsx. This is a client component ("use client") with useState for accordion and category tabs.

**Container:**
Replace `max-w-4xl` (or whatever max-width is used) with `max-w-content`. Full pattern: `max-w-content mx-auto px-4 sm:px-6 lg:px-8`.

**Page heading (D-06, D-12):**
- Remove `text-center` from the h1 heading
- Apply: `text-2xl font-display font-bold text-text-base`
- Subtitle: `text-lg text-text-secondary` with no `text-center`

**Category tabs (D-12):**
- Change `justify-center` to `justify-start` on the tab container flex div
- Active tab: replace `rounded-full bg-primary-600 text-white` (or similar) with:
  `px-4 py-2 rounded-sm text-xs font-bold uppercase tracking-wide bg-accent text-white`
- Inactive tab: replace `rounded-full bg-gray-100 text-gray-700 hover:bg-gray-200` with:
  `px-4 py-2 rounded-sm text-xs font-bold uppercase tracking-wide bg-surface-raised text-text-secondary border border-border hover:bg-surface hover:text-text-base`
- Ensure minimum touch target: add `min-h-[44px]` to tab buttons if not already present, or rely on py-2 + text being sufficient (py-2 = 8px top/bottom + text ~16px = 32px — add `min-h-[44px]` for WCAG)

**FAQ accordion items:**
- Each FAQ item card: ensure `.card` class is used. Verify `overflow-hidden` is present.
- Question text: `text-md font-bold text-text-base` (replacing any `text-gray-900`)
- Answer text: `text-md text-text-secondary` (replacing `text-gray-600`)
- Answer separator (if present): `border-t border-border` (replacing `border-gray-100` or `border-gray-200`)

**Chevron/toggle icon:**
- When FAQ is open: `text-accent` (replacing `text-gray-500` or `text-primary-600`)
- When FAQ is closed: `text-text-muted` (replacing `text-gray-500`)
- Pattern in JSX: `className={isOpen ? "text-accent" : "text-text-muted"}`

**Category badges on FAQ items (shown in "all" view):**
- Buyers questions: replace `bg-blue-100 text-blue-700` (or similar) with `badge-info`
- Owners questions: replace `bg-green-100 text-green-700` with `badge-success`
- General questions: replace `bg-gray-100 text-gray-700` with `badge-neutral`

**Quick action cards (if present):**
- Icon color: replace `text-primary-600` with `text-accent`
- Card wrapper: use `.card` class if not already applied
- Heading: `text-text-base font-semibold`
- Description: `text-text-secondary`

**Contact box:**
- Replace `bg-gray-100 rounded-xl` with `bg-surface-raised rounded-lg`
- Heading: `text-text-base font-semibold` (replacing `text-gray-900`)
- Body text: `text-text-secondary` (replacing `text-gray-600`)
- Email link: `text-accent hover:text-accent-hover`

**Global token replacements (catch any remaining):**
- All `text-gray-900` -> `text-text-base`
- All `text-gray-700` or `text-gray-600` -> `text-text-secondary`
- All `text-gray-500` -> `text-text-muted`
- All `bg-gray-100` or `bg-gray-200` -> `bg-surface-raised`
- All `border-gray-*` -> `border-border`
- All `text-primary-600` -> `text-accent`
- All `bg-primary-600` -> `bg-accent`
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web build 2>&1 | tail -5 && echo "--- grep checks ---" && echo "justify-center count: $(grep -c 'justify-center' apps/web/src/app/help/page.tsx || echo 0)" && echo "rounded-full count: $(grep -c 'rounded-full' apps/web/src/app/help/page.tsx || echo 0)" && echo "gray- count: $(grep -c 'gray-' apps/web/src/app/help/page.tsx || echo 0)"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/help/page.tsx contains `max-w-content`
    - apps/web/src/app/help/page.tsx contains `badge-info`
    - apps/web/src/app/help/page.tsx contains `badge-success`
    - apps/web/src/app/help/page.tsx contains `badge-neutral`
    - apps/web/src/app/help/page.tsx contains `bg-accent text-white` (active tab)
    - apps/web/src/app/help/page.tsx contains `rounded-sm` (tabs, replacing rounded-full)
    - apps/web/src/app/help/page.tsx contains `text-accent` (open chevron, icons)
    - apps/web/src/app/help/page.tsx contains `text-text-muted` (closed chevron)
    - apps/web/src/app/help/page.tsx contains `bg-surface-raised rounded-lg` (contact box)
    - apps/web/src/app/help/page.tsx contains `justify-start` (tab row)
    - apps/web/src/app/help/page.tsx does NOT contain `text-center` on any heading element
    - apps/web/src/app/help/page.tsx does NOT contain `justify-center` on tab container
    - apps/web/src/app/help/page.tsx does NOT contain `rounded-full` (pills)
    - apps/web/src/app/help/page.tsx does NOT contain `bg-primary-600`
    - apps/web/src/app/help/page.tsx does NOT contain `text-gray-900`
    - apps/web/src/app/help/page.tsx does NOT contain `bg-gray-100`
    - apps/web/src/app/help/page.tsx does NOT contain `bg-blue-100`
    - apps/web/src/app/help/page.tsx does NOT contain `bg-green-100`
    - apps/web/src/app/help/page.tsx does NOT contain `max-w-4xl`
    - Build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Help/FAQ page uses DESIGN.md tabs (rounded-sm, bg-accent active), semantic tokens throughout, left-aligned headings, badge classes for categories, and bg-surface-raised contact box. Build passes.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `pnpm --filter @offmarket/web build` passes
2. `grep -c "text-gray-" apps/web/src/app/auth/signin/page.tsx` returns 1 (only the Google button gray-700)
3. `grep -c "border-gray-" apps/web/src/app/auth/signin/page.tsx` returns 1 (only Google button gray-300)
4. `grep -c "gray-" apps/web/src/app/help/page.tsx` returns 0
5. `grep -c "primary-[0-9]" apps/web/src/app/help/page.tsx` returns 0
6. `grep -c "rounded-full" apps/web/src/app/help/page.tsx` returns 0
</verification>

<success_criteria>
Auth page renders with semantic tokens (Google button exception preserved). Help/FAQ renders with DESIGN.md tab styling, badge classes, left-aligned headings, and semantic accordion tokens. Both pages build without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-pages/03-02-SUMMARY.md`
</output>
