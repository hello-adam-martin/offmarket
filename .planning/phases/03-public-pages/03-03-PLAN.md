---
phase: 03-public-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/privacy/page.tsx
  - apps/web/src/app/terms/page.tsx
  - apps/web/src/app/explore/page.tsx
  - apps/web/src/app/explore/[region]/page.tsx
  - apps/web/src/components/demand-checker.tsx
autonomous: true
requirements:
  - PUBL-02
  - PUBL-03
  - PUBL-05
  - PUBL-06

must_haves:
  truths:
    - "Privacy page has no prose prose-gray wrapper classes"
    - "Privacy page headings use font-display text-text-base"
    - "Terms page has no prose prose-gray wrapper classes"
    - "Terms page headings use font-display text-text-base"
    - "Explore page skeleton uses bg-surface-raised, not bg-gray-200"
    - "Explore page container uses max-w-content, not max-w-7xl"
    - "Region page skeleton uses bg-surface-raised, not bg-gray-200"
    - "Region page container uses max-w-content, not max-w-7xl"
    - "DemandChecker result states use semantic tokens (bg-success/10 or bg-surface-raised)"
    - "No gray-* or primary-* hardcoded tokens remain in any of the 5 files"
  artifacts:
    - path: "apps/web/src/app/privacy/page.tsx"
      provides: "Restyled privacy page with DESIGN.md typography"
      contains: "font-display"
    - path: "apps/web/src/app/terms/page.tsx"
      provides: "Restyled terms page with DESIGN.md typography"
      contains: "font-display"
    - path: "apps/web/src/app/explore/page.tsx"
      provides: "Restyled explore wrapper with semantic skeleton"
      contains: "bg-surface-raised"
    - path: "apps/web/src/app/explore/[region]/page.tsx"
      provides: "Restyled region wrapper with semantic skeleton"
      contains: "bg-surface-raised"
    - path: "apps/web/src/components/demand-checker.tsx"
      provides: "DemandChecker with semantic result state tokens"
      contains: "bg-success/10"
  key_links:
    - from: "apps/web/src/app/explore/page.tsx"
      to: "apps/web/src/components/browse/BrowsePageClient.tsx"
      via: "Suspense wrapper around BrowsePageClient"
      pattern: "BrowsePageClient"
---

<objective>
Restyle five lighter-lift files: privacy page (232 lines), terms page (141 lines), explore page wrapper (31 lines), region page wrapper (91 lines), and DemandChecker component (167 lines). All are straightforward token swaps and prose class removal with no structural changes.

Purpose: Complete the remaining PUBL-02, PUBL-03, PUBL-05, PUBL-06 requirements. These files share the same fix patterns (gray-* to semantic, max-w to max-w-content, prose removal).
Output: Five fully restyled files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@apps/web/src/app/globals.css
@apps/web/tailwind.config.ts
@.planning/phases/03-public-pages/03-UI-SPEC.md
@.planning/phases/03-public-pages/03-CONTEXT.md

<interfaces>
Tailwind semantic tokens:
```
bg-bg: #fafaf7 light / #0f0f1a dark
bg-surface: #ffffff light / #1a1a2e dark
bg-surface-raised: #f5f5f0 light / #252540 dark
bg-accent: #0d9488
bg-success: #059669 (use bg-success/10 for light background)
bg-error: #dc2626 (use bg-error/10 for light background)
text-text-base: #1a1a2e light / #fafaf7 dark
text-text-secondary: #6b7280
text-text-muted: #9ca3af
text-accent: #0d9488
text-accent-hover: #0f766e
text-success: #059669
text-error: #dc2626
border-border: #e5e5e0 light / #374151 dark
border-success: #059669 (use border-success/30 for light border)
border-error: #dc2626 (use border-error/30 for light border)
max-w-content: 1120px
font-display: General Sans
```

From globals.css:
```
.card — bg-surface border border-border rounded-lg p-6
.btn-primary — bg-accent text-white
.btn-secondary — border border-border bg-surface
.input — border border-border bg-surface rounded-md
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle privacy and terms pages — remove prose classes, apply DESIGN.md typography</name>
  <files>apps/web/src/app/privacy/page.tsx, apps/web/src/app/terms/page.tsx</files>
  <read_first>
    - apps/web/src/app/privacy/page.tsx (full 232-line file)
    - apps/web/src/app/terms/page.tsx (full 141-line file)
    - .planning/phases/03-public-pages/03-UI-SPEC.md (Privacy and Terms page contracts)
    - .planning/phases/03-public-pages/03-RESEARCH.md (Legal page violation inventory)
  </read_first>
  <action>
Restyle both legal pages with identical treatment. Both are server components (no "use client") with metadata exports.

**Privacy page (apps/web/src/app/privacy/page.tsx):**

Container: Replace `max-w-4xl` (or any other max-w) with `max-w-content`. Full pattern: `max-w-content mx-auto px-4 sm:px-6 lg:px-8 py-12`.

Prose wrapper: Find `<div className="prose prose-gray max-w-none">` and replace with plain `<div>`. Remove `prose`, `prose-gray`, and `max-w-none` classes entirely. Keep the div element to maintain DOM structure.

Heading hierarchy — replace all heading classNames:
- Every h1: `text-2xl font-display font-bold text-text-base mb-8`
- Every h2: `text-xl font-display font-bold text-text-base mb-4 mt-8` (replacing `text-xl font-semibold text-gray-900 mb-4`)
- Every h3: `text-lg font-display font-bold text-text-base mb-3` (replacing `text-lg font-semibold text-gray-800 mb-3` or similar)

Body text:
- Every `text-gray-700` paragraph -> `text-md text-text-secondary mb-4`
- Every `text-gray-600` paragraph -> `text-md text-text-secondary mb-4`
- Every `text-gray-800` paragraph -> `text-md text-text-secondary mb-4`

Links:
- Every `text-primary-600 hover:text-primary-800` -> `text-accent hover:text-accent-hover underline`

Lists:
- `<ul>` and `<ol>` items with `text-gray-700` -> `text-text-secondary`
- List markers should inherit text color (default behavior)

Footer separator:
- Replace `border-gray-200` with `border-border`
- Pattern: `border-t border-border mt-8 pt-8`

Footer "last updated" text: `text-sm text-text-muted`

**Terms page (apps/web/src/app/terms/page.tsx):**

Identical treatment as privacy page. Apply the exact same replacements:
- Container: `max-w-content mx-auto px-4 sm:px-6 lg:px-8 py-12`
- Remove `prose prose-gray max-w-none` from wrapper div
- Same heading hierarchy classes
- Same body text replacements
- Same link replacements
- Same footer separator
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/privacy/page.tsx contains `max-w-content`
    - apps/web/src/app/privacy/page.tsx contains `font-display`
    - apps/web/src/app/privacy/page.tsx contains `text-text-base`
    - apps/web/src/app/privacy/page.tsx contains `text-text-secondary`
    - apps/web/src/app/privacy/page.tsx contains `text-accent hover:text-accent-hover`
    - apps/web/src/app/privacy/page.tsx contains `border-border`
    - apps/web/src/app/privacy/page.tsx does NOT contain `prose`
    - apps/web/src/app/privacy/page.tsx does NOT contain `text-gray-`
    - apps/web/src/app/privacy/page.tsx does NOT contain `text-primary-600`
    - apps/web/src/app/privacy/page.tsx does NOT contain `max-w-4xl`
    - apps/web/src/app/terms/page.tsx contains `max-w-content`
    - apps/web/src/app/terms/page.tsx contains `font-display`
    - apps/web/src/app/terms/page.tsx contains `text-text-base`
    - apps/web/src/app/terms/page.tsx does NOT contain `prose`
    - apps/web/src/app/terms/page.tsx does NOT contain `text-gray-`
    - apps/web/src/app/terms/page.tsx does NOT contain `text-primary-600`
    - Build succeeds
  </acceptance_criteria>
  <done>Both legal pages use DESIGN.md typography (font-display headings, text-text-secondary body), semantic tokens throughout, no prose plugin classes, max-w-content containers. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle explore/region page wrappers and DemandChecker component</name>
  <files>apps/web/src/app/explore/page.tsx, apps/web/src/app/explore/[region]/page.tsx, apps/web/src/components/demand-checker.tsx</files>
  <read_first>
    - apps/web/src/app/explore/page.tsx (31 lines — skeleton and Suspense wrapper)
    - apps/web/src/app/explore/[region]/page.tsx (91 lines — skeleton, metadata, Suspense wrapper)
    - apps/web/src/components/demand-checker.tsx (167 lines — full component)
    - .planning/phases/03-public-pages/03-UI-SPEC.md (Explore, Region, DemandChecker contracts)
    - .planning/phases/03-public-pages/03-RESEARCH.md (violation inventories for all 3 files)
  </read_first>
  <action>
**Explore page (apps/web/src/app/explore/page.tsx, 31 lines):**

Container: Replace `max-w-7xl` with `max-w-content` in the BrowseLoading skeleton wrapper.

Skeleton colors: Replace all `bg-gray-200` and `bg-gray-100` with `bg-surface-raised`. Ensure `animate-pulse` and `rounded-md` are present on skeleton divs. Target pattern for skeleton items:
```
<div className="h-8 w-64 bg-surface-raised rounded-md animate-pulse" />
```

The inner `<BrowsePageClient />` component is already restyled from Phase 2 — do NOT modify it.

**Region page (apps/web/src/app/explore/[region]/page.tsx, 91 lines):**

Same treatment as explore page:
- Replace all `max-w-7xl` with `max-w-content` (appears ~2 times per research)
- Replace all `bg-gray-200` and `bg-gray-100` with `bg-surface-raised`
- Replace all `border-gray-200` or `border-gray-100` with `border-border` if any exist
- Replace any `text-gray-*` with semantic equivalents (text-text-base, text-text-secondary, text-text-muted)

The inner `<RegionPageClient />` component is already restyled from Phase 2 — do NOT modify it.

**DemandChecker component (apps/web/src/components/demand-checker.tsx, 167 lines):**

Error state (line ~115):
Replace `bg-red-50 border border-red-200 rounded-lg text-red-700` with:
`bg-error/10 border border-error/30 rounded-md text-error`

Result state — has interest (lines ~122-146):
- Replace `bg-green-50 border-green-200` with `bg-success/10 border border-success/30`
- Replace `text-green-600` with `text-success`
- Replace `text-green-700` with `text-success`
- Add `tabular-nums` to the buyer count number: `text-4xl font-bold text-success tabular-nums mb-2`

Result state — no interest (lines ~147-161):
- Replace `bg-gray-50 border-gray-200` with `bg-surface-raised border-border`
- Replace `text-gray-600` with `text-text-secondary`
- Replace `text-gray-500` with `text-text-muted`

The form elements (.card, .input, .label, .btn-primary, .btn-secondary) are already using component classes — verify and leave them as-is.

Container `max-w-xl mx-auto` on the outer div: leave as-is (DemandChecker has its own width constraint which is correct within its parent section).

Result rounded corners: replace any `rounded-lg` with `rounded-md` on the result state divs to match the error state pattern.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/explore/page.tsx contains `max-w-content`
    - apps/web/src/app/explore/page.tsx contains `bg-surface-raised`
    - apps/web/src/app/explore/page.tsx does NOT contain `max-w-7xl`
    - apps/web/src/app/explore/page.tsx does NOT contain `bg-gray-200`
    - apps/web/src/app/explore/page.tsx does NOT contain `bg-gray-100`
    - apps/web/src/app/explore/[region]/page.tsx contains `max-w-content`
    - apps/web/src/app/explore/[region]/page.tsx contains `bg-surface-raised`
    - apps/web/src/app/explore/[region]/page.tsx does NOT contain `max-w-7xl`
    - apps/web/src/app/explore/[region]/page.tsx does NOT contain `bg-gray-200`
    - apps/web/src/components/demand-checker.tsx contains `bg-success/10`
    - apps/web/src/components/demand-checker.tsx contains `border-success/30`
    - apps/web/src/components/demand-checker.tsx contains `text-success`
    - apps/web/src/components/demand-checker.tsx contains `bg-error/10`
    - apps/web/src/components/demand-checker.tsx contains `border-error/30`
    - apps/web/src/components/demand-checker.tsx contains `bg-surface-raised`
    - apps/web/src/components/demand-checker.tsx contains `text-text-secondary`
    - apps/web/src/components/demand-checker.tsx contains `tabular-nums`
    - apps/web/src/components/demand-checker.tsx does NOT contain `bg-red-50`
    - apps/web/src/components/demand-checker.tsx does NOT contain `bg-green-50`
    - apps/web/src/components/demand-checker.tsx does NOT contain `text-green-600`
    - apps/web/src/components/demand-checker.tsx does NOT contain `text-gray-600`
    - apps/web/src/components/demand-checker.tsx does NOT contain `bg-gray-50`
    - Build succeeds
  </acceptance_criteria>
  <done>Explore and region page wrappers use max-w-content and bg-surface-raised skeletons. DemandChecker uses semantic success/error/surface tokens with tabular-nums on buyer count. Build passes.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `pnpm --filter @offmarket/web build` passes
2. `grep -c "gray-" apps/web/src/app/privacy/page.tsx` returns 0
3. `grep -c "gray-" apps/web/src/app/terms/page.tsx` returns 0
4. `grep -c "gray-" apps/web/src/app/explore/page.tsx` returns 0
5. `grep -c "gray-" apps/web/src/components/demand-checker.tsx` returns 0
6. `grep -c "max-w-7xl" apps/web/src/app/explore/page.tsx` returns 0
7. `grep -c "prose" apps/web/src/app/privacy/page.tsx` returns 0
8. `grep -c "prose" apps/web/src/app/terms/page.tsx` returns 0
</verification>

<success_criteria>
All five files use DESIGN.md semantic tokens. Legal pages have no prose plugin classes and use font-display headings. Explore/region skeletons are bg-surface-raised. DemandChecker result states use success/error semantic tokens. All files build without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-pages/03-03-SUMMARY.md`
</output>
