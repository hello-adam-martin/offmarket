---
phase: 08-cross-cutting-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/browse/BrowsePageClient.tsx
  - apps/web/src/components/browse/FilterPanel.tsx
  - apps/web/src/components/browse/RegionFilterPanel.tsx
  - apps/web/src/components/browse/RegionPageClient.tsx
  - apps/web/src/components/browse/MarketSummaryBar.tsx
  - apps/web/src/components/browse/Pagination.tsx
  - apps/web/src/components/browse/PropertyCardGrid.tsx
  - apps/web/src/components/header.tsx
  - apps/web/src/components/footer.tsx
  - apps/web/src/app/page.tsx
autonomous: true
requirements:
  - XCUT-01
  - XCUT-02
  - XCUT-03
  - XCUT-05
must_haves:
  truths:
    - "Every page container uses max-w-content (1120px) — no max-w-7xl, max-w-6xl, or max-w-[1120px] remain"
    - "No hardcoded bg-white, bg-gray-*, text-gray-*, or border-gray-* tokens remain anywhere in apps/web/src/ except the documented D-14 exception in auth/signin/page.tsx"
    - "All interactive elements use teal accent via CSS variable — no primary-N or accent-N shade classes remain"
    - "No gradient backgrounds remain in browse components or homepage pricing grid"
    - "No non-semantic yellow tokens remain — rank badge uses design system amber"
  artifacts:
    - path: "apps/web/src/components/browse/RegionPageClient.tsx"
      provides: "Browse region page with all tokens replaced"
    - path: "apps/web/src/components/browse/BrowsePageClient.tsx"
      provides: "Browse page with max-w-content and semantic tokens"
    - path: "apps/web/src/components/browse/PropertyCardGrid.tsx"
      provides: "Property cards without gradient, using card utility"
    - path: "apps/web/src/components/header.tsx"
      provides: "Header with max-w-content"
    - path: "apps/web/src/components/footer.tsx"
      provides: "Footer with max-w-content"
  key_links:
    - from: "all browse components"
      to: "globals.css CSS variables"
      via: "Tailwind semantic token classes"
      pattern: "bg-surface|text-text-base|border-border|text-accent"
---

<objective>
Fix all structural DESIGN.md violations in browse components, header, footer, and homepage pricing grid.

Purpose: Browse components are the violation hotspot — 7 files with overlapping XCUT-01 (max-width), XCUT-02 (dark mode tokens), XCUT-03 (undefined color shades), and XCUT-05 (gradients/non-semantic colors) violations. Header and footer have hardcoded max-w-[1120px] instead of the max-w-content utility. Homepage pricing grid uses max-w-5xl.

Output: All 10 files updated with correct design system tokens, build passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/08-cross-cutting-audit/08-RESEARCH.md
@apps/web/src/app/globals.css

Use the Standard Replacement Map from 08-RESEARCH.md for all token swaps.

Key reminders:
- D-14 exception: Do NOT touch Google OAuth button gray tokens in auth/signin/page.tsx
- Use `card` utility class where elements are cards (bg-white + border-gray-200 + rounded pattern)
- `primary-N` and `accent-N` shade classes are NOT defined in tailwind config — they render as transparent
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix browse components — max-width, dark mode tokens, accent shades, and AI slop</name>
  <files>
    apps/web/src/components/browse/BrowsePageClient.tsx
    apps/web/src/components/browse/FilterPanel.tsx
    apps/web/src/components/browse/RegionFilterPanel.tsx
    apps/web/src/components/browse/RegionPageClient.tsx
    apps/web/src/components/browse/MarketSummaryBar.tsx
    apps/web/src/components/browse/Pagination.tsx
    apps/web/src/components/browse/PropertyCardGrid.tsx
  </files>
  <action>
Read each file first, then apply the following fixes per 08-RESEARCH.md file:line data.

**BrowsePageClient.tsx:**
- Line 260: `max-w-7xl` -> `max-w-content`
- Line 264: `border-primary-600` -> `border-accent` (spinner)
- Line 265: `text-gray-600` -> `text-text-secondary`
- Lines 291, 306: `text-gray-900` -> `text-text-base`
- Line 325: `text-gray-500` -> `text-text-muted`

**FilterPanel.tsx:**
- Line 66: `max-w-7xl` -> `max-w-content`

**RegionFilterPanel.tsx:**
- Line 294: `max-w-7xl` -> `max-w-content`

**RegionPageClient.tsx (heaviest — 35+ lines):**
- Lines 289, 311: `max-w-7xl` -> `max-w-content`
- Line 291: `border-primary-600` -> `border-accent` (spinner)
- All `bg-white rounded-xl border border-gray-200 p-4` patterns (lines 373, 377, 381, 387, 399, 424, 447, 500) -> `card p-4`
- All `text-gray-900` -> `text-text-base`
- All `text-gray-600` -> `text-text-secondary`
- All `text-gray-500` / `text-gray-400` -> `text-text-muted`
- All `text-gray-700` -> `text-text-base`
- All `border-gray-200` -> `border-border`
- All `bg-gray-100` / `bg-gray-50` -> `bg-surface-raised`
- Line 432: `bg-yellow-100 text-yellow-700` -> `bg-secondary-light text-secondary` (rank badge, XCUT-05)
- Lines 316, 325, 340: `hover:text-primary-600` -> `hover:text-accent`
- Line 374: `text-primary-600` -> `text-text-base`
- Line 378: `text-accent-600` -> `text-accent`
- Line 408: `bg-primary-500` -> `bg-accent` (progress bar)
- Line 525: `bg-gradient-to-r from-primary-50 to-accent-50` -> `bg-surface-raised` (remove gradient, XCUT-05)

**MarketSummaryBar.tsx:**
- Line 27: `card bg-gray-50 border-gray-200` -> `card` (card utility handles bg/border)
- Lines 30, 51, 58: `text-gray-900` -> `text-text-base`
- Lines 38, 45, 52, 59: `text-gray-500` -> `text-text-muted`
- Line 31: `text-gray-600` -> `text-text-secondary`
- Line 37: `text-accent-600` -> `text-accent`
- Line 44: `text-primary-600` -> `text-text-base`

**Pagination.tsx:**
- Lines 38, 55, 66: `border border-gray-300 bg-white hover:bg-gray-50` -> `border border-border bg-surface hover:bg-surface-raised`
- Line 45: `text-gray-500` -> `text-text-muted`
- Line 54: `bg-primary-600 text-white` -> `bg-accent text-white`

**PropertyCardGrid.tsx:**
- Line 30: Remove `bg-gradient-to-br from-white to-primary-50` — use `card` utility instead (XCUT-05). Also fix `border-primary-200` -> `border-border`, `hover:border-primary-400` -> `hover:border-accent`
- Lines 34, 49: `text-gray-900` -> `text-text-base`
- Lines 35, 47: `text-gray-600` -> `text-text-secondary`
- Line 40: `text-primary-600` -> `text-accent`
- Line 41: `text-gray-500` -> `text-text-muted`
- Line 46: `border-primary-100` -> `border-border`
- Remove `from-white` gradient reference

After all edits, check for any template-literal class construction (ternary patterns) that might contain primary-N or gray-N tokens that grep would miss. RegionPageClient.tsx is the most likely location.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -rn "max-w-7xl\|max-w-6xl" apps/web/src/components/browse/ --include="*.tsx" && echo "FAIL: max-w-7xl/6xl still present" || echo "PASS: no max-w-7xl/6xl in browse" && grep -rn "primary-[0-9]\|accent-[0-9]" apps/web/src/components/browse/ --include="*.tsx" && echo "FAIL: undefined shade classes remain" || echo "PASS: no undefined shades" && grep -rn "bg-gradient" apps/web/src/components/browse/ --include="*.tsx" && echo "FAIL: gradients remain" || echo "PASS: no gradients" && grep -rn "bg-yellow-100" apps/web/src/components/browse/ --include="*.tsx" && echo "FAIL: yellow tokens remain" || echo "PASS: no yellow tokens"</automated>
  </verify>
  <done>
    All 7 browse component files use max-w-content, semantic color tokens (text-text-base, bg-surface, border-border, text-accent), no undefined primary-N/accent-N shades, no gradients, no non-semantic yellow tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix header, footer, and homepage max-width — then build verify</name>
  <files>
    apps/web/src/components/header.tsx
    apps/web/src/components/footer.tsx
    apps/web/src/app/page.tsx
  </files>
  <action>
Read each file first, then apply fixes:

**header.tsx:**
- Line 43: `max-w-[1120px]` -> `max-w-content`

**footer.tsx:**
- Line 6: `max-w-[1120px]` -> `max-w-content`

**page.tsx (homepage):**
- Line 300: `max-w-5xl` on pricing grid div -> `max-w-content` (or remove — the outer container already constrains width; prefer removing if the parent already has max-w-content, otherwise change to max-w-content)

After edits, run full build to confirm no TypeScript or runtime errors.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -rn "max-w-\[1120px\]\|max-w-7xl\|max-w-6xl" apps/web/src/components/header.tsx apps/web/src/components/footer.tsx apps/web/src/app/page.tsx && echo "FAIL: hardcoded max-width remains" || echo "PASS: no hardcoded max-width" && cd apps/web && pnpm build</automated>
  </verify>
  <done>
    Header, footer, and homepage all use max-w-content. Full `pnpm build` passes with zero errors. No max-w-7xl, max-w-6xl, or max-w-[1120px] remain anywhere in the codebase.
  </done>
</task>

</tasks>

<verification>
Run these grep assertions to confirm all XCUT violations resolved by this plan:

```bash
# XCUT-01: No non-standard max-width containers
grep -rn "max-w-7xl\|max-w-6xl\|max-w-5xl\|max-w-\[1120px\]" apps/web/src/ --include="*.tsx"
# Expected: empty (no results)

# XCUT-02: No hardcoded gray tokens site-wide (only D-14 exception in auth/signin allowed)
grep -rn "bg-gray-\|text-gray-\|border-gray-\|bg-white" apps/web/src/ --include="*.tsx" | grep -v "auth/signin"
# Expected: empty

# XCUT-03: No undefined color shades
grep -rn "primary-[0-9]\|accent-[0-9]" apps/web/src/ --include="*.tsx"
# Expected: empty

# XCUT-05: No gradients in browse components
grep -rn "bg-gradient" apps/web/src/components/browse/ --include="*.tsx"
# Expected: empty

# Build passes
cd apps/web && pnpm build
# Expected: exit 0
```
</verification>

<success_criteria>
1. Zero grep matches for max-w-7xl, max-w-6xl, or max-w-[1120px] across all of apps/web/src/
2. Zero grep matches for bg-gray-*, text-gray-*, border-gray-*, bg-white in apps/web/src/components/browse/
3. Zero grep matches for primary-N or accent-N shade classes across all of apps/web/src/
4. Zero grep matches for bg-gradient in apps/web/src/components/browse/
5. Zero grep matches for bg-yellow-100 in apps/web/src/components/browse/
6. `pnpm build` in apps/web exits with code 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-cutting-audit/08-01-SUMMARY.md`
</output>
