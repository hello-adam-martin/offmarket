---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/web/src/app/globals.css
  - apps/web/src/app/layout.tsx
  - apps/web/src/components/providers.tsx
autonomous: false
requirements:
  - FOUN-01
  - FOUN-09
  - FOUN-11
  - FOUN-12
  - FOUN-13
  - FOUN-14
  - FOUN-15

must_haves:
  truths:
    - "CSS custom properties for all DESIGN.md colors exist in :root and .dark blocks"
    - "globals.css component classes (.btn-primary, .card, .input, etc.) reference new tokens — no primary-600, gray-300, or gray-* classes remain"
    - "Inter font is completely removed from layout.tsx"
    - "General Sans, DM Sans, JetBrains Mono CSS variables applied to html element"
    - "suppressHydrationWarning present on html element"
    - "ThemeProvider wraps app content in providers.tsx"
    - "Grain texture pseudo-element defined on .card class"
    - "tabular-nums applied to table elements in @layer base"
  artifacts:
    - path: "apps/web/src/app/globals.css"
      provides: "CSS variables + component classes"
      contains: "--color-bg"
    - path: "apps/web/src/app/layout.tsx"
      provides: "Font variable application + hydration suppression"
      contains: "suppressHydrationWarning"
    - path: "apps/web/src/components/providers.tsx"
      provides: "ThemeProvider wrapping SessionProvider"
      contains: "ThemeProvider"
  key_links:
    - from: "apps/web/src/app/globals.css"
      to: "apps/web/tailwind.config.ts"
      via: "CSS variables consumed by Tailwind color tokens"
      pattern: "--color-accent.*#0d9488"
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/app/fonts.ts"
      via: "font instance imports"
      pattern: "import.*generalSans.*dmSans.*jetbrainsMono.*from.*fonts"
    - from: "apps/web/src/components/providers.tsx"
      to: "apps/web/src/components/ThemeProvider.tsx"
      via: "ThemeProvider import and wrapping"
      pattern: "import.*ThemeProvider.*from.*ThemeProvider"
---

<objective>
Wire the design system into the app: rewrite globals.css with CSS variables and component classes, swap fonts in layout.tsx, and connect ThemeProvider in providers.tsx.

Purpose: This plan connects all the contracts created in Plan 01 to the running application. After this plan, the app renders with DESIGN.md fonts, colors switch via CSS variables, and dark mode infrastructure is live.
Output: globals.css (rewritten), layout.tsx (modified), providers.tsx (modified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@DESIGN.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From apps/web/src/app/fonts.ts (created in Plan 01):
```typescript
export const generalSans: NextFont  // variable: "--font-general-sans"
export const dmSans: NextFont       // variable: "--font-dm-sans"
export const jetbrainsMono: NextFont // variable: "--font-jetbrains-mono"
```

From apps/web/src/components/ThemeProvider.tsx (created in Plan 01):
```typescript
export function ThemeProvider({ children }: { children: React.ReactNode }): JSX.Element
```

From apps/web/tailwind.config.ts (rewritten in Plan 01):
- darkMode: "selector" (activates dark: prefix when .dark on html)
- colors: bg, surface, surface-raised, border, primary, accent, accent-hover, accent-light, secondary, secondary-light, text-base, text-secondary, text-muted, text-inverse, success, warning, error, info
- fontFamily: display (General Sans), sans (DM Sans), mono (JetBrains Mono)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite globals.css with CSS variables, component classes, and grain texture</name>
  <files>apps/web/src/app/globals.css</files>
  <read_first>
    - apps/web/src/app/globals.css (current file — contains old primary-600, gray-300, rounded-xl references)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Full Color Token Set table, globals.css Component Classes section, Grain texture section)
    - .planning/phases/01-foundation/01-RESEARCH.md (globals.css Complete Structure code example, Pattern 4: CSS SVG Noise Grain Texture)
    - DESIGN.md (Color section, CSS Variables block if present)
  </read_first>
  <action>
Replace the entire `apps/web/src/app/globals.css` with this exact content (per D-03 1:1 mapping, per D-09 component classes rewritten, per D-10 CSS-only grain):

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --color-primary: #1a1a2e;
    --color-accent: #0d9488;
    --color-accent-hover: #0f766e;
    --color-accent-light: #ccfbf1;
    --color-secondary: #d97706;
    --color-secondary-light: #fef3c7;
    --color-bg: #fafaf7;
    --color-surface: #ffffff;
    --color-surface-raised: #f5f5f0;
    --color-border: #e5e5e0;
    --color-text: #1a1a2e;
    --color-text-secondary: #6b7280;
    --color-text-muted: #9ca3af;
    --color-text-inverse: #fafaf7;
    --color-success: #059669;
    --color-warning: #d97706;
    --color-error: #dc2626;
    --color-info: #0d9488;
  }

  .dark {
    --color-bg: #0f0f1a;
    --color-surface: #1a1a2e;
    --color-surface-raised: #252540;
    --color-border: #374151;
    --color-primary: #fafaf7;
    --color-text: #fafaf7;
    --color-text-secondary: #9ca3af;
    --color-text-muted: #6b7280;
    --color-text-inverse: #1a1a2e;
    --color-accent: #0b8276;
    --color-accent-hover: #0d6b62;
    --color-accent-light: #134e4a;
    --color-secondary-light: #451a03;
  }

  html {
    @apply scroll-smooth;
  }

  body {
    @apply antialiased;
  }

  table {
    font-variant-numeric: tabular-nums;
  }
}

@layer components {
  .btn {
    @apply inline-flex items-center justify-center rounded-md px-4 py-2 text-sm font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-accent-light focus:ring-offset-2 disabled:opacity-50 disabled:pointer-events-none;
  }

  .btn-primary {
    @apply btn bg-accent text-white hover:bg-accent-hover;
  }

  .btn-secondary {
    @apply btn bg-surface text-text-base border border-border hover:bg-surface-raised;
  }

  .btn-ghost {
    @apply btn text-text-secondary hover:bg-surface-raised hover:text-text-base;
  }

  .btn-destructive {
    @apply btn bg-error text-white hover:bg-red-700 focus:ring-red-300;
  }

  .input {
    @apply block w-full rounded-md border border-border bg-surface px-4 py-2 text-text-base placeholder:text-text-muted focus:border-accent focus:ring-1 focus:ring-accent-light focus:outline-none;
  }

  .label {
    @apply block text-xs font-semibold text-text-secondary mb-1 uppercase tracking-wide;
  }

  .card {
    @apply relative overflow-hidden bg-surface rounded-lg border border-border p-6;
  }

  .card::before {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='grain'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23grain)'/%3E%3C/svg%3E");
    opacity: 0.035;
    pointer-events: none;
  }
}

@layer utilities {
  .hide-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  .hide-scrollbar::-webkit-scrollbar {
    display: none;
  }
}
```

Key changes from current globals.css:
- ADDED: `:root` block with all 18 DESIGN.md color CSS variables (FOUN-01)
- ADDED: `.dark` block with all dark mode overrides (FOUN-11)
- ADDED: `table { font-variant-numeric: tabular-nums; }` in @layer base (FOUN-14)
- REWRITTEN: `.btn` uses `rounded-md` (8px) instead of `rounded-lg`, focus ring uses `ring-accent-light`
- REWRITTEN: `.btn-primary` uses `bg-accent`/`hover:bg-accent-hover` instead of `bg-primary-600`/`hover:bg-primary-700`
- REWRITTEN: `.btn-secondary` uses `bg-surface`/`text-text-base`/`border-border` instead of `bg-white`/`text-gray-700`/`border-gray-300`
- REWRITTEN: `.btn-ghost` uses `text-text-secondary`/`hover:bg-surface-raised` instead of `text-gray-600`/`hover:bg-gray-100`
- ADDED: `.btn-destructive` (new — `bg-error text-white`)
- REWRITTEN: `.input` uses `border-border`/`bg-surface`/`text-text-base`/`placeholder:text-text-muted`/`focus:border-accent`/`focus:ring-accent-light` instead of old gray/primary tokens
- REWRITTEN: `.label` uses `text-xs font-semibold text-text-secondary uppercase tracking-wide` per DESIGN.md xs token
- REWRITTEN: `.card` uses `bg-surface rounded-lg border-border` instead of `bg-white rounded-xl border-gray-100`, adds `relative overflow-hidden` for grain
- ADDED: `.card::before` grain texture pseudo-element (FOUN-15) — CSS-only SVG feTurbulence at 0.035 opacity
- REMOVED: All `primary-600`, `primary-500`, `primary-700`, `gray-*` references
- PRESERVED: `.hide-scrollbar` utility unchanged
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -q "\-\-color-bg: #fafaf7" apps/web/src/app/globals.css && grep -q "\-\-color-bg: #0f0f1a" apps/web/src/app/globals.css && grep -q "font-variant-numeric: tabular-nums" apps/web/src/app/globals.css && grep -q "bg-accent" apps/web/src/app/globals.css && grep -q "feTurbulence" apps/web/src/app/globals.css && grep -q "opacity: 0.035" apps/web/src/app/globals.css && ! grep -q "primary-600\|primary-500\|gray-300\|gray-700\|gray-100\|rounded-xl" apps/web/src/app/globals.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--color-bg: #fafaf7` in :root
    - globals.css contains `--color-accent: #0d9488` in :root
    - globals.css contains `--color-bg: #0f0f1a` in .dark
    - globals.css contains `--color-accent: #0b8276` in .dark
    - globals.css contains `--color-surface: #1a1a2e` in .dark
    - globals.css contains `--color-surface-raised: #252540` in .dark
    - globals.css contains `font-variant-numeric: tabular-nums` on table
    - globals.css contains `bg-accent` in .btn-primary
    - globals.css contains `bg-surface` in .btn-secondary
    - globals.css contains `text-text-secondary` in .btn-ghost
    - globals.css contains `bg-error` in .btn-destructive
    - globals.css contains `border-border` in .input
    - globals.css contains `text-text-secondary` in .label
    - globals.css contains `rounded-lg` in .card (not rounded-xl)
    - globals.css contains `relative overflow-hidden` in .card
    - globals.css contains `feTurbulence` (grain texture SVG)
    - globals.css contains `opacity: 0.035` (grain opacity)
    - globals.css does NOT contain `primary-600`, `primary-500`, `gray-300`, `gray-700`, `gray-100`, or `rounded-xl`
  </acceptance_criteria>
  <done>globals.css fully rewritten with :root and .dark CSS variables, all component classes using new tokens, grain texture on .card, tabular-nums on tables, zero references to old color scales</done>
</task>

<task type="auto">
  <name>Task 2: Modify layout.tsx and providers.tsx to wire fonts and ThemeProvider</name>
  <files>apps/web/src/app/layout.tsx, apps/web/src/components/providers.tsx</files>
  <read_first>
    - apps/web/src/app/layout.tsx (current file — Inter import on line 2, inter.className on line 68)
    - apps/web/src/components/providers.tsx (current file — SessionProvider only)
    - apps/web/src/app/fonts.ts (created in Plan 01 — exports generalSans, dmSans, jetbrainsMono)
    - apps/web/src/components/ThemeProvider.tsx (created in Plan 01 — exports ThemeProvider)
    - .planning/phases/01-foundation/01-UI-SPEC.md (layout.tsx Structure section)
    - .planning/phases/01-foundation/01-RESEARCH.md (layout.tsx font application pattern)
  </read_first>
  <action>
**Part A: Modify `apps/web/src/app/layout.tsx`**

Replace the font import and application. The file keeps ALL existing metadata, structure, Header/Footer imports, and Providers usage. Only the font loading and html/body attributes change.

Changes to make (per D-02 Inter swap in layout.tsx only, per FOUN-09, FOUN-12):

1. REMOVE line 2: `import { Inter } from "next/font/google";`
2. REMOVE line 8: `const inter = Inter({ subsets: ["latin"] });`
3. ADD import: `import { generalSans, dmSans, jetbrainsMono } from "./fonts";`
4. CHANGE `<html lang="en">` to:
   ```tsx
   <html
     lang="en"
     className={`${generalSans.variable} ${dmSans.variable} ${jetbrainsMono.variable}`}
     suppressHydrationWarning
   >
   ```
5. CHANGE `<body className={inter.className}>` to:
   ```tsx
   <body className="font-sans bg-bg text-text-base antialiased">
   ```
6. CHANGE `<div className="min-h-screen bg-gray-50 flex flex-col">` to:
   ```tsx
   <div className="min-h-screen flex flex-col">
   ```
   (bg-gray-50 removed — body already has bg-bg)

Everything else stays: metadata object, Providers wrapper, Header, main, Footer, the entire structure.

**Part B: Modify `apps/web/src/components/providers.tsx`**

Add ThemeProvider wrapping around SessionProvider children:

Replace entire file with:
```typescript
"use client";

import { SessionProvider } from "next-auth/react";
import { ThemeProvider } from "@/components/ThemeProvider";

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <SessionProvider>
      <ThemeProvider>
        {children}
      </ThemeProvider>
    </SessionProvider>
  );
}
```

Changes: Added ThemeProvider import and wrapping. SessionProvider remains the outer provider.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -q 'import.*generalSans.*dmSans.*jetbrainsMono' apps/web/src/app/layout.tsx && grep -q 'suppressHydrationWarning' apps/web/src/app/layout.tsx && grep -q 'font-sans bg-bg text-text-base' apps/web/src/app/layout.tsx && ! grep -q 'Inter' apps/web/src/app/layout.tsx && ! grep -q 'inter\.' apps/web/src/app/layout.tsx && ! grep -q 'bg-gray-50' apps/web/src/app/layout.tsx && grep -q 'ThemeProvider' apps/web/src/components/providers.tsx && grep -q 'import.*ThemeProvider.*from.*ThemeProvider' apps/web/src/components/providers.tsx && pnpm --filter @offmarket/web build 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains `import { generalSans, dmSans, jetbrainsMono } from "./fonts"`
    - layout.tsx contains `${generalSans.variable} ${dmSans.variable} ${jetbrainsMono.variable}` on html className
    - layout.tsx contains `suppressHydrationWarning` on html element
    - layout.tsx contains `className="font-sans bg-bg text-text-base antialiased"` on body
    - layout.tsx does NOT contain `Inter` (import removed)
    - layout.tsx does NOT contain `inter.className` or `inter.` (usage removed)
    - layout.tsx does NOT contain `bg-gray-50` (old background removed)
    - layout.tsx STILL contains the full metadata object (title, description, keywords, openGraph, twitter, robots)
    - layout.tsx STILL contains Header, Footer, Providers, main structure
    - providers.tsx contains `import { ThemeProvider } from "@/components/ThemeProvider"`
    - providers.tsx contains `<ThemeProvider>` wrapping children inside SessionProvider
    - `pnpm --filter @offmarket/web build` passes without errors
  </acceptance_criteria>
  <done>Inter completely removed, three font variables applied to html, suppressHydrationWarning present, body uses font-sans bg-bg text-text-base, ThemeProvider wrapping app in providers.tsx, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of foundation token layer</name>
  <what-built>Complete design token infrastructure: CSS variables (light + dark), font loading (General Sans + DM Sans + JetBrains Mono), Tailwind semantic tokens, ThemeProvider with next-themes, grain texture on cards, tabular-nums on tables.</what-built>
  <how-to-verify>
    1. Start dev server: `cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web dev`
    2. Open http://localhost:3000 in browser
    3. **Font check:** Inspect any heading — computed font-family should show "General Sans" (not Inter). Inspect body text — should show "DM Sans".
    4. **Color check:** Open DevTools, inspect body — `background-color` should compute to `rgb(250, 250, 247)` (#fafaf7 warm off-white). Inspect any `.btn-primary` — background should be `rgb(13, 148, 136)` (#0d9488 teal).
    5. **Dark mode check:** In DevTools, add class `dark` to the `<html>` element. Page background should change to `rgb(15, 15, 26)` (#0f0f1a). Card surfaces should show `rgb(26, 26, 46)` (#1a1a2e). Remove `dark` class to return to light mode.
    6. **No FOUC check:** Hard refresh the page — it should NOT flash white then dark (or vice versa). The `suppressHydrationWarning` prevents React warnings.
    7. **Grain texture check:** Inspect any element with `.card` class — the `::before` pseudo-element should show the SVG grain texture at low opacity.
    8. **Tabular-nums check:** If any table is visible, inspect it — computed `font-variant-numeric` should be `tabular-nums`.
    9. **Note:** Pages will look broken (wrong colors on existing components that reference old tokens like `primary-600`, `gray-*`). This is expected per D-04. Phases 2-7 will restyle individual pages.
  </how-to-verify>
  <resume-signal>Type "approved" if fonts, colors, dark mode, and grain texture render correctly. Describe any issues if not.</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
1. `pnpm --filter @offmarket/web build` passes
2. No references to `Inter`, `primary-600`, `gray-300`, `gray-700`, `bg-gray-50` in modified files
3. CSS variables defined for all 18 DESIGN.md color tokens in both :root and .dark
4. Font variables applied to html element, body uses font-sans
5. ThemeProvider wrapping app via providers.tsx
6. Grain texture pseudo-element on .card class
7. tabular-nums on table elements
8. Visual confirmation: correct fonts, colors, dark mode, grain in browser
</verification>

<success_criteria>
- All 18 CSS custom properties defined in :root with DESIGN.md light values
- All dark mode overrides defined in .dark with DESIGN.md dark values
- Component classes (.btn-primary, .btn-secondary, .btn-ghost, .btn-destructive, .input, .label, .card) use new tokens exclusively
- Inter font completely removed from layout.tsx
- Three font CSS variables applied to html element
- suppressHydrationWarning on html element
- ThemeProvider wrapping app content
- Grain texture visible on .card elements
- tabular-nums applied to table elements
- Build passes and visual verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
