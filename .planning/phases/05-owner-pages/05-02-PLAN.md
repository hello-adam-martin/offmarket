---
phase: 05-owner-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/owner/my-properties/page.tsx
  - apps/web/src/app/owner/properties/[id]/page.tsx
autonomous: true
requirements:
  - OWNR-02
  - OWNR-03

must_haves:
  truths:
    - "My properties listing shows property cards with hover:border-accent and badge-neutral for property type"
    - "Demand count on property cards uses text-accent font-bold tabular-nums"
    - "Property detail demand summary panel uses .card with border-l-4 border-accent left stripe, no gradient"
    - "Direct vs criteria buyer split is inline text row, not colored sub-cards"
    - "All match score percentages use single text-accent color, no green/yellow/orange tier coloring"
    - "Match criteria tags use badge-info class"
    - "Direct interest section uses .card with border-l-4 border-accent, no gradient, no icon-in-circle"
    - "Contact buttons use btn-primary, not bg-green-600"
    - "Delete button uses text-error, not text-red-600"
  artifacts:
    - path: "apps/web/src/app/owner/my-properties/page.tsx"
      provides: "My properties listing with DESIGN.md styling"
      contains: "badge-neutral"
    - path: "apps/web/src/app/owner/properties/[id]/page.tsx"
      provides: "Property detail with DESIGN.md demand panel and match display"
      contains: "border-l-4 border-accent"
  key_links:
    - from: "apps/web/src/app/owner/my-properties/page.tsx"
      to: "globals.css"
      via: "CSS classes"
      pattern: "badge-neutral|btn-primary|text-accent|text-error"
    - from: "apps/web/src/app/owner/properties/[id]/page.tsx"
      to: "globals.css"
      via: "CSS classes"
      pattern: "badge-info|btn-primary|card|bg-surface-raised"
---

<objective>
Restyle the my-properties listing and property detail page to DESIGN.md compliance.

Purpose: The property listing and detail pages are the core owner experience — showing demand data, match scores, and buyer interactions. The detail page requires significant restructuring: demand summary panel redesign (flat card with accent stripe), match score simplification (single color), direct interest section cleanup (no gradients, no icon-in-circle), and comprehensive token swaps.

Output: Two restyled pages — /owner/my-properties and /owner/properties/[id] — with full DESIGN.md token compliance and restructured demand display.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-owner-pages/05-CONTEXT.md
@.planning/phases/05-owner-pages/05-UI-SPEC.md
@apps/web/src/app/globals.css
@apps/web/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle my-properties listing page</name>
  <files>apps/web/src/app/owner/my-properties/page.tsx</files>
  <read_first>
    - apps/web/src/app/owner/my-properties/page.tsx (current state — 232 lines)
    - apps/web/src/app/globals.css (available CSS classes: .card, .btn-primary, .btn-secondary, .badge-neutral, .btn-destructive)
    - DESIGN.md (design system source of truth)
    - .planning/phases/05-owner-pages/05-UI-SPEC.md (page-level interaction contract for /owner/my-properties)
  </read_first>
  <action>
    Restyle the my-properties listing page with these specific changes:

    **All containers (per D-24):**
    - Replace all `max-w-4xl` with `max-w-content` (appears 3 times: loading, no-session, main)

    **Loading state skeleton (per D-28):**
    - Replace `bg-gray-200` with `bg-surface-raised` on skeleton divs (2 occurrences)

    **No-session state (per D-25):**
    - h1: change `text-gray-900` to `text-primary`
    - p: change `text-gray-600` to `text-secondary`

    **Page header (per D-25):**
    - h1: change `text-gray-900` to `text-primary`
    - p: change `text-gray-600` to `text-secondary`

    **Error state (per D-26):**
    - Replace `p-4 bg-red-50 border border-red-200 rounded-lg text-red-700 mb-6` with `p-4 bg-error-light border border-error rounded-lg text-error mb-6`

    **Empty state (per D-25):**
    - h2: change `text-gray-900` to `text-primary`
    - p: change `text-gray-600` to `text-secondary`

    **Property cards (per D-19):**
    - Change `hover:border-primary-300` to `hover:border-accent` on the Link card wrapper

    **Property address/location (per D-25):**
    - h2: change `text-gray-900` to `text-primary`
    - Location p: change `text-gray-600` to `text-secondary`

    **Property type badge (per D-20):**
    - Replace inline `inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-700` with `badge-neutral`

    **Metadata row (per D-25):**
    - Change `text-sm text-gray-500` on the flex wrapper to `text-sm text-secondary`
    - Add `tabular-nums` to spans containing numeric data (bedrooms, bathrooms, estimatedValue)

    **Added date (per D-25):**
    - Change `text-xs text-gray-400` to `text-xs text-muted`

    **Demand count (per D-21, D-29):**
    - Replace the conditional color `${property.demandCount > 0 ? "text-primary-600" : "text-gray-400"}` with `text-accent font-bold tabular-nums` (always accent — the number itself communicates whether there is demand)
    - Keep `text-2xl font-bold` but ensure `tabular-nums` is added
    - Change demand label `text-xs text-gray-500` to `text-xs text-secondary`

    **View Details link (per D-23):**
    - Change `text-sm text-primary-600` to `text-sm text-accent hover:text-accent-hover`

    **Delete button (per D-22):**
    - Change `text-sm text-red-600 hover:text-red-800` to `text-sm text-error hover:text-error-hover`
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c "gray-\|primary-300\|primary-600\|red-\|rounded-full" apps/web/src/app/owner/my-properties/page.tsx | grep -q "^0$" && echo "PASS: no old tokens" || echo "FAIL: old tokens remain"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/owner/my-properties/page.tsx contains `max-w-content` (not max-w-4xl)
    - apps/web/src/app/owner/my-properties/page.tsx does NOT contain `text-gray-900`, `text-gray-600`, `text-gray-500`, `text-gray-400`
    - apps/web/src/app/owner/my-properties/page.tsx does NOT contain `bg-gray-200` or `bg-gray-100`
    - apps/web/src/app/owner/my-properties/page.tsx does NOT contain `text-primary-600` or `hover:border-primary-300`
    - apps/web/src/app/owner/my-properties/page.tsx does NOT contain `text-red-600` or `hover:text-red-800`
    - apps/web/src/app/owner/my-properties/page.tsx does NOT contain `bg-red-50` or `border-red-200` or `text-red-700`
    - apps/web/src/app/owner/my-properties/page.tsx does NOT contain `rounded-full`
    - apps/web/src/app/owner/my-properties/page.tsx contains `badge-neutral` (property type badge)
    - apps/web/src/app/owner/my-properties/page.tsx contains `text-accent` and `tabular-nums` (demand count)
    - apps/web/src/app/owner/my-properties/page.tsx contains `text-error` (delete button)
    - apps/web/src/app/owner/my-properties/page.tsx contains `hover:border-accent` (card hover)
    - apps/web/src/app/owner/my-properties/page.tsx contains `bg-error-light` (error state)
    - apps/web/src/app/owner/my-properties/page.tsx contains `bg-surface-raised` (skeleton)
  </acceptance_criteria>
  <done>My properties listing uses semantic tokens, badge-neutral for property type, text-accent tabular-nums for demand counts, text-error for delete, hover:border-accent on cards, max-w-content container</done>
</task>

<task type="auto">
  <name>Task 2: Restyle property detail page with demand panel and match display</name>
  <files>apps/web/src/app/owner/properties/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/owner/properties/[id]/page.tsx (current state — 611 lines)
    - apps/web/src/app/globals.css (available CSS classes: .card, .btn-primary, .badge-info, .badge-neutral, .btn-destructive)
    - DESIGN.md (design system source of truth)
    - .planning/phases/05-owner-pages/05-UI-SPEC.md (page-level interaction contract for /owner/properties/[id])
    - .planning/phases/05-owner-pages/05-CONTEXT.md (decisions D-01 through D-15 for demand panel and match display)
  </read_first>
  <action>
    Restyle the property detail page with significant structural changes to the demand panel and match display. This is the most complex page in Phase 5.

    **All containers (per D-24):**
    - Replace all `max-w-4xl` with `max-w-content` (appears 4 times: loading, no-session, error, main)

    **Loading state skeleton (per D-28):**
    - Replace all `bg-gray-200` with `bg-surface-raised` on skeleton divs (3 occurrences)

    **No-session and error states (per D-25):**
    - All `text-gray-900` → `text-primary`
    - All `text-gray-600` → `text-secondary`

    **Back link (per UI-SPEC):**
    - Change `text-gray-600 hover:text-gray-900` to `text-sm text-secondary hover:text-primary`

    **Property header card — property info section (per D-25):**
    - h1: change `text-gray-900` to `text-primary`
    - Location p with icon: change `text-gray-600` to `text-secondary`
    - Location icon: change `text-gray-400` to `text-muted`
    - Property details row: change `text-sm text-gray-600` to `text-sm text-secondary`
    - All detail icons: change `text-gray-400` to `text-muted`
    - Estimated value span: add `tabular-nums` class (per D-29)
    - Land size and floor area spans: add `tabular-nums` class (per D-29)

    **Feature badges on property info (per D-06):**
    - Replace `inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-600` with `badge-info`

    **Added date (per D-25):**
    - Change `text-xs text-gray-400` to `text-xs text-muted`

    **Demand Summary Panel — MAJOR RESTRUCTURE (per D-01, D-02, D-03, D-04):**
    - Replace the entire demand summary div. Current: `flex-shrink-0 lg:w-64 p-4 bg-gradient-to-br from-primary-50 to-white rounded-lg border border-primary-200`
    - New: `flex-shrink-0 lg:w-64 card border-l-4 border-accent` (flat card with teal left stripe)
    - Remove the people SVG icon from the header
    - Header h2: change `text-sm font-semibold text-gray-900` to `text-sm font-semibold text-primary`
    - Total buyer count: change `text-3xl font-bold text-primary-600` to `text-xl font-bold text-accent tabular-nums` (per D-02 — 24px, not 48px)
    - Buyer label: change `text-xs text-gray-600` to `text-xs text-secondary`
    - **Remove the 2-column grid with colored sub-cards** (per D-03). Delete the entire `grid grid-cols-2 gap-2 mb-3` div containing `bg-green-50` and `bg-blue-50` boxes
    - Replace with inline stats row below the total: `<p className="text-sm text-secondary tabular-nums">{demand.directInterestCount || 0} direct &middot; {demand.criteriaMatchCount || 0} criteria</p>`
    - Budget range section: change `border-t border-primary-100` to `border-t border-border`
    - Budget label: change `text-xs text-gray-500` to `text-xs text-secondary`
    - Budget value: change `text-sm font-semibold text-gray-900` to `text-sm font-semibold text-primary tabular-nums` (per D-04, D-29)
    - Remove `text-center` from the total display — left-align all content

    **Property Images section (per D-25, D-27):**
    - h2: change `text-gray-900` to `text-primary`
    - p: change `text-gray-600` to `text-secondary`
    - Toggle button: change `text-primary-600 hover:text-primary-800` to `text-accent hover:text-accent-hover`
    - Image thumbnails: keep `rounded-lg` (8px)
    - "More" button: change `bg-gray-100 rounded-lg ... text-gray-500 hover:bg-gray-200` to `bg-surface-raised rounded-lg ... text-secondary hover:opacity-80`

    **Direct Interest Section — MAJOR RESTRUCTURE (per D-12, D-13, D-14, D-15):**
    - Outer card: change `card border-2 border-green-200 bg-gradient-to-br from-green-50 to-white` to `card border-l-4 border-accent` (per D-12 — flat card with teal left stripe)
    - **Remove the icon-in-circle header** (per D-12). Delete the `w-8 h-8 rounded-full bg-green-100` wrapper div and the heart SVG inside it
    - Section heading h2: change `text-gray-900` to `text-primary` (per D-13)
    - Section subtext p: change `text-sm text-green-700` to `text-sm text-secondary` (per D-13)
    - Restructure header: remove the `flex items-center gap-2` wrapper that held icon + text, just have the text divs directly

    **Individual direct interest match items (per D-15):**
    - Change `p-4 bg-white rounded-lg border border-green-200 hover:border-green-400` to `p-4 bg-surface rounded-md border border-border hover:border-accent` (per D-15)
    - Buyer name: change `text-gray-900` to `text-primary`
    - Match time: change `text-xs text-gray-500` to `text-xs text-muted`
    - Budget text: change `text-sm text-gray-600` to `text-sm text-secondary tabular-nums`
    - Contact button: change `px-4 py-2 text-sm font-medium text-white bg-green-600 rounded-lg hover:bg-green-700 transition-colors` to `btn-primary btn-sm` (per D-14)

    **Criteria Matches section (per D-25, D-27):**
    - h2: change `text-gray-900` to `text-primary`
    - p: change `text-gray-600` to `text-secondary`
    - Refresh button: change `text-primary-600 hover:text-primary-800` to `text-accent hover:text-accent-hover`

    **No matches empty state (per D-25):**
    - Remove icon-in-circle: delete `w-12 h-12 mx-auto mb-4 rounded-full bg-gray-100` wrapper
    - Keep the SVG icon but restyle: `w-6 h-6 text-muted mx-auto mb-4`
    - Change `text-gray-600` to `text-secondary`
    - Change `text-sm text-gray-500` to `text-sm text-muted`
    - Secondary empty state: change `text-gray-500` to `text-secondary`

    **Criteria match items (per D-05, D-06, D-25):**
    - Item wrapper: change `p-4 bg-gray-50 rounded-lg border border-gray-200 hover:border-primary-200` to `p-4 bg-surface rounded-md border border-border hover:border-accent`
    - Wanted ad link: change `text-gray-900 hover:text-primary-600` to `text-primary hover:text-accent`
    - Budget text: change `text-sm text-gray-600` to `text-sm text-secondary tabular-nums`

    **Match criteria tags (per D-06):**
    - Replace `inline-flex items-center px-2 py-0.5 rounded text-xs bg-blue-100 text-blue-700` with `badge-info`

    **Match score display — SIMPLIFICATION (per D-05):**
    - Remove the entire conditional color ternary: `${match.matchScore >= 75 ? "text-green-600" : match.matchScore >= 50 ? "text-yellow-600" : "text-orange-600"}`
    - Replace with single class: `text-2xl font-bold text-accent tabular-nums` (per D-05 — all scores same color)
    - Score label: change `text-xs text-gray-500` to `text-xs text-secondary`

    **Match time (per D-25):**
    - Change `text-xs text-gray-500 mt-2` to `text-xs text-muted mt-2`
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c "gray-\|primary-50\|primary-100\|primary-200\|primary-500\|primary-600\|primary-800\|green-\|blue-50\|blue-100\|blue-700\|yellow-\|orange-\|red-\|bg-gradient\|bg-white\b" apps/web/src/app/owner/properties/\[id\]/page.tsx | grep -q "^0$" && echo "PASS: no old tokens" || echo "FAIL: old tokens remain"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/owner/properties/[id]/page.tsx contains `max-w-content` (not max-w-4xl)
    - apps/web/src/app/owner/properties/[id]/page.tsx does NOT contain `bg-gradient-to-br` (no gradients)
    - apps/web/src/app/owner/properties/[id]/page.tsx does NOT contain `from-primary-50` or `from-green-50`
    - apps/web/src/app/owner/properties/[id]/page.tsx does NOT contain `bg-green-50` or `bg-blue-50` (colored sub-cards removed)
    - apps/web/src/app/owner/properties/[id]/page.tsx does NOT contain `text-green-600` or `text-yellow-600` or `text-orange-600` (score tier coloring removed)
    - apps/web/src/app/owner/properties/[id]/page.tsx does NOT contain `bg-green-600` or `hover:bg-green-700` (contact button restyled)
    - apps/web/src/app/owner/properties/[id]/page.tsx does NOT contain `rounded-full bg-green-100` (icon-in-circle removed)
    - apps/web/src/app/owner/properties/[id]/page.tsx does NOT contain `bg-blue-100 text-blue-700` (old match tags removed)
    - apps/web/src/app/owner/properties/[id]/page.tsx contains `border-l-4 border-accent` (demand panel left stripe — should appear twice: demand summary + direct interest)
    - apps/web/src/app/owner/properties/[id]/page.tsx contains `badge-info` (match criteria tags + feature badges)
    - apps/web/src/app/owner/properties/[id]/page.tsx contains `btn-primary` (contact button)
    - apps/web/src/app/owner/properties/[id]/page.tsx contains `text-accent tabular-nums` (match scores and demand count)
    - apps/web/src/app/owner/properties/[id]/page.tsx contains `direct` and `criteria` as inline text (inline stats row per D-03)
    - apps/web/src/app/owner/properties/[id]/page.tsx contains `bg-surface-raised` (skeleton, more-images button)
    - apps/web/src/app/owner/properties/[id]/page.tsx does NOT contain any `gray-` tokens
    - apps/web/src/app/owner/properties/[id]/page.tsx contains `text-primary` and `text-secondary` and `text-muted` (semantic text tokens)
    - apps/web/src/app/owner/properties/[id]/page.tsx contains `border-border` (border token)
  </acceptance_criteria>
  <done>Property detail page has flat demand summary panel with accent left stripe, inline buyer split stats, single-color match scores (text-accent), badge-info match criteria tags, btn-primary contact buttons, no gradients, no icon-in-circle, no colored sub-cards, all semantic tokens applied</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "gray-\|primary-[0-9]\|green-\|blue-[0-9]\|yellow-\|orange-\|red-\|bg-gradient\|rounded-full\|max-w-4xl\|bg-white" apps/web/src/app/owner/my-properties/page.tsx apps/web/src/app/owner/properties/\[id\]/page.tsx` returns zero matches
2. `grep -c "text-primary\|text-secondary\|text-accent\|bg-surface\|border-accent\|badge-info\|badge-neutral\|tabular-nums\|max-w-content\|btn-primary\|text-error\|border-border" apps/web/src/app/owner/my-properties/page.tsx apps/web/src/app/owner/properties/\[id\]/page.tsx` returns non-zero for both files
3. `grep -c "border-l-4 border-accent" apps/web/src/app/owner/properties/\[id\]/page.tsx` returns 2 (demand panel + direct interest)
4. `npx tsc --noEmit` passes or `npx next build` compiles without errors
</verification>

<success_criteria>
- My properties listing shows semantic tokens, badge-neutral property type, text-accent demand counts, text-error delete, hover:border-accent cards
- Property detail shows flat demand panel with accent left stripe, inline buyer split, single-color match scores, badge-info criteria tags, btn-primary contact buttons
- Zero hardcoded gray-*, primary-N, green-*, blue-*, yellow-*, orange-*, red-* color tokens remain in either file
- Both pages use max-w-content (1120px) containers
- No AI slop patterns remain: no gradients, no icon-in-circle, no colored sub-cards
</success_criteria>

<output>
After completion, create `.planning/phases/05-owner-pages/05-02-SUMMARY.md`
</output>
