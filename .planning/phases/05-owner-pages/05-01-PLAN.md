---
phase: 05-owner-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/owner/page.tsx
  - apps/web/src/app/owner/register/page.tsx
autonomous: true
requirements:
  - OWNR-01

must_haves:
  truths:
    - "Owner landing page hero is left-aligned with no gradient background and no centered text"
    - "Benefits section uses plain icon + text rows, no icon-in-circle pattern"
    - "CTA section uses flat bg-surface-raised card, no gradient"
    - "Register form sections use .card class with text-primary headings"
    - "Feature selection badges use badge-info (active) and badge-neutral (inactive) with rounded-sm, not rounded-full"
    - "All gray-* hardcoded colors replaced with semantic tokens"
    - "Page containers use max-w-content (1120px)"
  artifacts:
    - path: "apps/web/src/app/owner/page.tsx"
      provides: "Owner landing page with DESIGN.md styling"
      contains: "max-w-content"
    - path: "apps/web/src/app/owner/register/page.tsx"
      provides: "Register property form with DESIGN.md styling"
      contains: "badge-info"
  key_links:
    - from: "apps/web/src/app/owner/page.tsx"
      to: "globals.css"
      via: "CSS classes"
      pattern: "btn-primary|card|bg-surface-raised"
    - from: "apps/web/src/app/owner/register/page.tsx"
      to: "globals.css"
      via: "CSS classes"
      pattern: "badge-info|badge-neutral|btn-primary"
---

<objective>
Restyle the owner landing page and register property form to DESIGN.md compliance.

Purpose: The owner entry point (landing page) and primary action (register form) must use semantic tokens, remove AI slop patterns (centered hero, icon-in-circle benefits, gradient CTAs), and match the design system established in Phases 1-4.

Output: Two restyled pages — /owner landing and /owner/register — with full DESIGN.md token compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-owner-pages/05-CONTEXT.md
@.planning/phases/05-owner-pages/05-UI-SPEC.md
@apps/web/src/app/globals.css
@apps/web/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle owner landing page (/owner)</name>
  <files>apps/web/src/app/owner/page.tsx</files>
  <read_first>
    - apps/web/src/app/owner/page.tsx (current state — 157 lines)
    - apps/web/src/app/globals.css (available CSS classes: .card, .btn-primary, .btn-secondary, .badge-info)
    - DESIGN.md (design system source of truth)
    - .planning/phases/05-owner-pages/05-UI-SPEC.md (page-level interaction contract for /owner)
  </read_first>
  <action>
    Restyle the owner landing page with these specific changes:

    **Container (per D-11, D-24):**
    - Replace `max-w-7xl` with `max-w-content` on the outer div

    **Hero section (per D-07):**
    - Remove `text-center` from the hero wrapper div — make it left-aligned
    - h1: change `text-gray-900` to `text-primary`
    - p: change `text-lg text-gray-600 max-w-2xl mx-auto` to `text-base text-secondary` (remove mx-auto and max-w-2xl since no longer centered)
    - Add a `btn-primary btn-lg` CTA link to `/owner/register` with text "Register Your Property" below the paragraph

    **Quick Check section:**
    - h2: change `text-gray-900 text-center` to `text-primary` (remove text-center to left-align)
    - DemandChecker component: leave as-is (already restyled in Phase 3)

    **Benefits section (per D-08 — AI slop removal):**
    - Replace the 3-column grid of centered cards with icon-in-circle pattern
    - New structure: a single column `space-y-6` with 3 benefit rows
    - Each row: `flex items-start gap-4` containing:
      - Plain Lucide-style SVG icon at 24px (`w-6 h-6 text-accent flex-shrink-0 mt-0.5`) — NO wrapping div with rounded-full background
      - Text div with: heading `text-base font-bold text-primary` and body `text-sm text-secondary`
    - Remove all `text-center`, `mx-auto`, `rounded-full bg-accent-100`, `w-12 h-12` icon wrapper divs
    - Keep the same 3 benefits content (100% Private, See Real Demand, Connect on Your Terms) and same SVG icons

    **CTA section (per D-09):**
    - Replace `bg-gradient-to-br from-accent-50 to-primary-50 text-center py-10` with `bg-surface-raised border-border text-center py-10`
    - h2: change `text-gray-900` to `text-primary`
    - p: change `text-gray-600` to `text-secondary`, remove `mx-auto` centering
    - Buttons remain: `btn-primary` and `btn-secondary` (already correct classes)

    **FAQ section (per D-10, D-25):**
    - h2: change `text-gray-900` to `text-primary`
    - FAQ card headings (h3): change `text-gray-900` to `text-primary`
    - FAQ card body (p): change `text-gray-600` to `text-secondary`
    - Cards already use `.card` class — leave as-is

    **Cross-cutting token swaps (per D-25):**
    - All remaining `text-gray-900` → `text-primary`
    - All remaining `text-gray-600` → `text-secondary`
    - All remaining `text-gray-500` → `text-secondary`
    - All remaining `text-gray-400` → `text-muted`
    - All remaining `bg-gray-*` → `bg-surface-raised`
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c "gray-" apps/web/src/app/owner/page.tsx | grep -q "^0$" && echo "PASS: no gray tokens" || echo "FAIL: gray tokens remain"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/owner/page.tsx contains `max-w-content` (not max-w-7xl)
    - apps/web/src/app/owner/page.tsx does NOT contain `text-center` on the hero section div (the first child div after the outer container)
    - apps/web/src/app/owner/page.tsx does NOT contain `rounded-full bg-accent-100` (icon-in-circle removed)
    - apps/web/src/app/owner/page.tsx does NOT contain `bg-gradient-to-br` (gradient CTA removed)
    - apps/web/src/app/owner/page.tsx contains `bg-surface-raised` (flat CTA card)
    - apps/web/src/app/owner/page.tsx contains `text-primary` and `text-secondary` (semantic tokens)
    - apps/web/src/app/owner/page.tsx does NOT contain any `gray-` tokens
    - apps/web/src/app/owner/page.tsx contains `text-accent` on benefit icons (`w-6 h-6 text-accent`)
    - apps/web/src/app/owner/page.tsx contains `btn-primary btn-lg` for hero CTA
  </acceptance_criteria>
  <done>Owner landing page is left-aligned, AI slop free (no centered hero, no icon-in-circle, no gradient), all semantic tokens applied, max-w-content container</done>
</task>

<task type="auto">
  <name>Task 2: Restyle register property form (/owner/register)</name>
  <files>apps/web/src/app/owner/register/page.tsx</files>
  <read_first>
    - apps/web/src/app/owner/register/page.tsx (current state — 454 lines)
    - apps/web/src/app/globals.css (available CSS classes: .card, .btn-primary, .input, .label, .badge-info, .badge-neutral)
    - DESIGN.md (design system source of truth)
    - .planning/phases/05-owner-pages/05-UI-SPEC.md (page-level interaction contract for /owner/register)
  </read_first>
  <action>
    Restyle the register property form with these specific changes:

    **All containers (per D-24):**
    - Replace all `max-w-3xl` with `max-w-content` (appears 3 times: loading state, no-session state, main form)

    **Loading state skeleton (per D-28):**
    - Replace `bg-gray-200` with `bg-surface-raised` on skeleton divs (2 occurrences)

    **No-session state (per D-25):**
    - h1: change `text-gray-900` to `text-primary`
    - p: change `text-gray-600` to `text-secondary`

    **Main form header (per D-18, D-25):**
    - h1: change `text-gray-900` to `text-primary`
    - p: change `text-gray-600` to `text-secondary`

    **Section card headings (per D-18) — appears 5 times (Address, Details, Size, Features, Valuation):**
    - Change all `text-lg font-semibold text-gray-900` to `text-base font-bold text-primary` on h2 elements inside .card sections

    **Helper text (per D-25):**
    - `text-xs text-gray-500 mt-1` → `text-xs text-muted mt-1` (address helper, valuation helpers)
    - `text-sm text-gray-600 mb-4` → `text-sm text-secondary mb-4` (valuation description)

    **Feature selection badges (per D-17):**
    - Replace the entire ternary className on feature buttons:
      - Active state: change `bg-primary-600 text-white` to `badge-info` (which provides accent-light bg, accent text, rounded-sm)
      - Inactive state: change `bg-gray-100 text-gray-700 hover:bg-gray-200` to `badge-neutral` with `hover:bg-surface-raised`
    - Remove `rounded-full` from both states — badge-info and badge-neutral already include `rounded-sm` (4px)
    - Keep `px-3 py-1.5 text-sm font-medium transition-colors` as base classes
    - Final className: `px-3 py-1.5 text-sm font-medium transition-colors cursor-pointer ${features.includes(feature.value) ? "badge-info" : "badge-neutral hover:opacity-80"}`

    **Error state (per D-26):**
    - Replace `p-4 bg-red-50 border border-red-200 rounded-lg text-red-700` with `p-4 bg-error-light border border-error rounded-lg text-error`

    **Cross-cutting token swaps (per D-25, D-27):**
    - All remaining `text-gray-900` → `text-primary`
    - All remaining `text-gray-600` → `text-secondary`
    - All remaining `text-gray-500` → `text-muted`
    - All remaining `bg-gray-*` → `bg-surface-raised`

    **Numeric displays (per D-29):**
    - Add `tabular-nums` class to the estimated value and RV display text (the `text-xs text-muted mt-1` paragraphs showing formatted currency)
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -c "gray-\|primary-600\|red-\|rounded-full" apps/web/src/app/owner/register/page.tsx | grep -q "^0$" && echo "PASS: no old tokens" || echo "FAIL: old tokens remain"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/owner/register/page.tsx contains `max-w-content` (not max-w-3xl)
    - apps/web/src/app/owner/register/page.tsx does NOT contain `text-gray-900` or `text-gray-600` or `text-gray-500`
    - apps/web/src/app/owner/register/page.tsx does NOT contain `bg-gray-200` or `bg-gray-100`
    - apps/web/src/app/owner/register/page.tsx does NOT contain `bg-primary-600` or `text-primary-600`
    - apps/web/src/app/owner/register/page.tsx does NOT contain `bg-red-50` or `border-red-200` or `text-red-700`
    - apps/web/src/app/owner/register/page.tsx does NOT contain `rounded-full` on feature buttons
    - apps/web/src/app/owner/register/page.tsx contains `badge-info` (active feature badges)
    - apps/web/src/app/owner/register/page.tsx contains `badge-neutral` (inactive feature badges)
    - apps/web/src/app/owner/register/page.tsx contains `bg-error-light` and `text-error` (error state)
    - apps/web/src/app/owner/register/page.tsx contains `text-base font-bold text-primary` (section headings)
    - apps/web/src/app/owner/register/page.tsx contains `bg-surface-raised` (skeleton loading)
    - apps/web/src/app/owner/register/page.tsx contains `tabular-nums` (numeric displays)
  </acceptance_criteria>
  <done>Register form uses semantic tokens throughout, feature badges use badge-info/badge-neutral with rounded-sm, error states use error tokens, section headings use text-primary, max-w-content container</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "gray-\|primary-600\|red-\|green-\|blue-\|rounded-full\|bg-gradient\|max-w-7xl\|max-w-3xl" apps/web/src/app/owner/page.tsx apps/web/src/app/owner/register/page.tsx` returns zero matches
2. `grep -c "text-primary\|text-secondary\|text-accent\|bg-surface-raised\|max-w-content\|badge-info\|badge-neutral\|bg-error-light\|tabular-nums" apps/web/src/app/owner/page.tsx apps/web/src/app/owner/register/page.tsx` returns non-zero for both files
3. `npx next build` compiles without errors (if available) or `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Owner landing page renders left-aligned hero, flat benefit rows (no icon-in-circle), flat CTA card (no gradient), DESIGN.md FAQ styling
- Register property form renders with semantic tokens, badge-info/badge-neutral feature toggles, error tokens, tabular-nums on currency displays
- Zero hardcoded gray-*, primary-*, red-*, green-*, blue-* color tokens remain in either file
- Both pages use max-w-content (1120px) containers
</success_criteria>

<output>
After completion, create `.planning/phases/05-owner-pages/05-01-SUMMARY.md`
</output>
