---
phase: 07-admin-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/admin/layout.tsx
  - apps/web/src/app/admin/page.tsx
  - apps/web/src/app/admin/users/page.tsx
  - apps/web/src/app/admin/billing/page.tsx
autonomous: true
requirements:
  - ADMN-01
  - ADMN-02
  - ADMN-03
  - ADMN-04
must_haves:
  truths:
    - "Admin layout shell uses bg-primary header bar with text-text-inverse title and bg-surface sidebar with accent-colored active nav items"
    - "Admin dashboard stat cards are uniform .card styling with no rainbow color classes, one card has border-l-4 border-accent"
    - "Users table has semantic token styling with role badges using badge-warning (ADMIN) and badge-neutral (USER)"
    - "Billing overview page stat cards and quick actions use .card class with no icon-in-colored-circle pattern"
  artifacts:
    - path: "apps/web/src/app/admin/layout.tsx"
      provides: "Restyled admin shell — header, sidebar, nav items"
      contains: "bg-primary"
    - path: "apps/web/src/app/admin/page.tsx"
      provides: "Restyled admin dashboard — stat cards, recent activity"
      contains: "tabular-nums"
    - path: "apps/web/src/app/admin/users/page.tsx"
      provides: "Restyled user management table"
      contains: "badge-warning"
    - path: "apps/web/src/app/admin/billing/page.tsx"
      provides: "Restyled billing overview — stat cards, feedback states, quick actions"
      contains: "border-l-4 border-success"
  key_links:
    - from: "apps/web/src/app/admin/layout.tsx"
      to: "all admin pages"
      via: "Next.js layout wrapper"
      pattern: "bg-primary.*text-text-inverse"
    - from: "apps/web/src/app/admin/page.tsx"
      to: "globals.css"
      via: ".card class usage"
      pattern: "card.*tabular-nums"
---

<objective>
Restyle the admin layout shell, dashboard, user management table, and billing overview page with DESIGN.md semantic tokens.

Purpose: The admin shell (header + sidebar) wraps every admin page — restyling it first ensures the chrome is correct. The dashboard, users table, and billing overview are the highest-traffic admin pages and contain the most AI slop (rainbow stat cards, icon-in-colored-circles, hardcoded gray-* tokens).

Output: 4 files restyled with DESIGN.md compliance — no gray-* hardcoded colors, no rainbow stat cards, no bg-white, all semantic tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/07-admin-pages/07-CONTEXT.md
@.planning/phases/07-admin-pages/07-UI-SPEC.md
@apps/web/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle admin layout shell and dashboard page</name>
  <files>apps/web/src/app/admin/layout.tsx, apps/web/src/app/admin/page.tsx</files>
  <read_first>
    - apps/web/src/app/admin/layout.tsx
    - apps/web/src/app/admin/page.tsx
    - apps/web/src/app/globals.css
    - .planning/phases/07-admin-pages/07-UI-SPEC.md
    - DESIGN.md
  </read_first>
  <action>
**layout.tsx (ADMN-01 shell, per D-01 through D-05):**

Restyle the admin header bar:
- Replace `bg-gray-900` (or whatever dark bg) with `bg-primary`
- "Admin Panel" title: `text-lg font-display font-semibold text-text-inverse`
- Any subtitle: `text-sm text-text-muted`
- "Back to Site" link: `text-sm text-text-muted hover:text-text-inverse transition-colors`
- Header is full-width (no max-w constraint per D-05)

Restyle the sidebar:
- Replace `bg-gray-100` / `bg-white` with `bg-surface border-r border-border`
- Set width `w-64 min-h-[calc(100vh-52px)]`
- Nav container: `p-4 space-y-1`
- Active nav item: `bg-accent-light text-accent rounded-md px-3 py-2` with icon `text-accent w-5 h-5` and label `text-sm font-medium`
- Inactive nav item: `text-text-secondary hover:bg-surface-raised hover:text-text-base rounded-md px-3 py-2 transition-colors` with icon `text-text-muted w-5 h-5` and label `text-sm font-medium`
- Replace any `bg-primary-50 text-primary-700` active state with `bg-accent-light text-accent`
- Replace any `text-gray-700 hover:bg-gray-100` inactive state with `text-text-secondary hover:bg-surface-raised`

Add Postcards as 5th nav item (per D-04):
- Add nav item linking to `/admin/postcards` with an appropriate inline SVG icon (mail/postcard style)
- Position after "Email Templates" in the nav list

Main content area: `flex-1 p-6 bg-bg`

**page.tsx (ADMN-01 dashboard, per D-06 through D-08, D-23 through D-29):**

Remove the rainbow `colorClasses` object (the one mapping stat keys to blue-*, green-*, purple-*, orange-*, pink-*, cyan-*, yellow-* Tailwind classes). Replace ALL stat cards with uniform `.card` class:
- Standard stat card: `card` class with inner `<p class="text-sm text-text-secondary mb-1">{label}</p>` and `<p class="text-xl font-display font-semibold text-text-base tabular-nums">{value}</p>`
- ONE emphasized card (Total Users or first card): add `border-l-4 border-accent` alongside the `card` class
- All other cards: just `card` class, no extra border

Page heading: replace `text-2xl font-bold text-gray-900` with `text-xl font-display font-semibold text-text-base`

Replace all `bg-white rounded-lg shadow` with `card` class.
Replace all `text-gray-*` with semantic tokens per the Token Swap Reference in UI-SPEC:
- `text-gray-900` -> `text-text-base`
- `text-gray-700` -> `text-text-secondary`
- `text-gray-600` -> `text-text-secondary`
- `text-gray-500` -> `text-text-secondary` or `text-text-muted`
- `text-gray-400` -> `text-text-muted`
- `bg-gray-*` -> appropriate semantic token (`bg-bg`, `bg-surface`, `bg-surface-raised`)
- `border-gray-200` -> `border-border`
- `divide-gray-200` -> `divide-border`
- `hover:bg-gray-100` -> `hover:bg-surface-raised`

Replace role badges:
- `bg-purple-100 text-purple-700` (ADMIN) -> `badge-warning`
- Any USER role badge -> `badge-neutral`

Loading skeletons: replace `bg-gray-200` with `bg-surface-raised`.

All numeric values must have `tabular-nums` class.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains `bg-primary` (admin header)
    - layout.tsx contains `text-text-inverse` (header title)
    - layout.tsx contains `bg-accent-light text-accent` (active nav)
    - layout.tsx contains `bg-surface` (sidebar background)
    - layout.tsx contains `/admin/postcards` (5th nav item per D-04)
    - layout.tsx does NOT contain `bg-gray-` (no hardcoded grays)
    - layout.tsx does NOT contain `bg-primary-50` or `text-primary-700` (old active state)
    - page.tsx contains `tabular-nums` (numeric values)
    - page.tsx contains the string `card` as a className (stat cards)
    - page.tsx contains `border-l-4 border-accent` (one emphasized card)
    - page.tsx contains `badge-warning` (ADMIN role badge)
    - page.tsx does NOT contain `colorClasses` (rainbow removed)
    - page.tsx does NOT contain `bg-white` (no hardcoded white)
    - page.tsx does NOT contain `bg-gray-` (no hardcoded grays)
    - page.tsx does NOT contain `text-gray-` (no hardcoded gray text)
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <done>Admin layout shell renders bg-primary header with text-text-inverse, bg-surface sidebar with accent active nav items, Postcards nav item added. Dashboard stat cards are uniform .card with one border-l-4 border-accent, no rainbow colors, all semantic tokens, role badges use badge-warning/badge-neutral.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle users table and billing overview page</name>
  <files>apps/web/src/app/admin/users/page.tsx, apps/web/src/app/admin/billing/page.tsx</files>
  <read_first>
    - apps/web/src/app/admin/users/page.tsx
    - apps/web/src/app/admin/billing/page.tsx
    - apps/web/src/app/globals.css
    - .planning/phases/07-admin-pages/07-UI-SPEC.md
    - DESIGN.md
  </read_first>
  <action>
**users/page.tsx (ADMN-02, per D-09 through D-13, D-23 through D-29):**

Page heading: replace with `text-xl font-display font-semibold text-text-base`.

Search/filter bar (if present): use `<input class="input max-w-sm" placeholder="Search users..." />` pattern.

Table wrapper: replace `bg-white rounded-lg shadow` with `card overflow-x-auto`, inner table gets `w-full min-w-[640px]`.

Table header row: `<tr class="border-b border-border">` with `<th class="px-6 py-3 text-left text-xs font-semibold text-text-muted uppercase tracking-wide">` for each column header.

Table body: `<tbody class="divide-y divide-border">` with rows `<tr class="hover:bg-surface-raised transition-colors">`.

Table cells: `<td class="px-6 py-4 text-sm text-text-base">` for standard data. Date cells add `text-text-secondary tabular-nums`. Email cells use `text-text-secondary`.

Role badges in table:
- ADMIN role: replace `bg-purple-100 text-purple-700` with `badge-warning`
- USER role: replace any colored badge with `badge-neutral`

Action buttons in table rows: right-aligned column using `btn-secondary btn-sm` for standard actions and `btn-destructive btn-sm` for delete/ban. Minimum touch target `min-h-[44px]`.

Replace ALL remaining token swaps:
- `bg-white` -> `bg-surface`
- `bg-gray-*` -> semantic (`bg-bg`, `bg-surface`, `bg-surface-raised`)
- `text-gray-*` -> semantic per Token Swap Reference
- `border-gray-*` -> `border-border`
- `shadow` on cards -> remove (`.card` handles borders)
- `rounded-lg shadow` -> `card` class
- `max-w-7xl` -> `max-w-content`

Loading skeletons: `bg-surface-raised` not `bg-gray-200`.

**billing/page.tsx (ADMN-03, ADMN-04, per D-06 through D-08, D-23 through D-29):**

Page heading: `text-xl font-display font-semibold text-text-base`.

Stat cards grid: Replace ALL colored stat cards with uniform `.card` class:
- Each stat card: `card` with label `text-sm text-text-secondary mb-1` and value `text-xl font-display font-semibold text-text-base tabular-nums`
- ONE emphasized card (e.g., Total Revenue or MRR): `card border-l-4 border-accent`
- Remove ALL semantic-colored stat values (e.g., `text-green-600` on active count, `text-yellow-600` on past_due count) — ALL values use `text-text-base tabular-nums` per D-06

Quick Actions section — REMOVE icon-in-colored-circle pattern:
- Current: cards with `p-2 bg-primary-100 rounded-lg` / `p-2 bg-green-100 rounded-lg` / `p-2 bg-purple-100 rounded-lg` icon wrappers
- Replace with: flat `.card` link blocks. Icon renders inline with `text-text-secondary w-5 h-5 mr-2`, no colored circle wrapper. Link text in `text-sm font-medium text-text-base`.

Process result feedback states:
- Success: replace `bg-green-50 border-green-200 text-green-700` with `card border-l-4 border-success` and `<p class="text-sm text-text-base">{message}</p>`
- Error: replace `bg-red-50 border-red-200 text-red-700` with `card border-l-4 border-error` and `<p class="text-sm text-text-base">{message}</p>`

Replace ALL remaining token swaps (same as users page list above).

Dollar amounts: `font-mono text-sm text-text-base tabular-nums` and right-aligned where in table context.

All `max-w-7xl` -> `max-w-content`.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - users/page.tsx contains `badge-warning` (ADMIN role badge)
    - users/page.tsx contains `badge-neutral` (USER role badge)
    - users/page.tsx contains `hover:bg-surface-raised` (table row hover)
    - users/page.tsx contains `text-text-muted uppercase tracking-wide` (table headers)
    - users/page.tsx contains `divide-border` (table row dividers)
    - users/page.tsx does NOT contain `bg-white` (no hardcoded white)
    - users/page.tsx does NOT contain `bg-gray-` (no hardcoded grays)
    - users/page.tsx does NOT contain `text-gray-` (no hardcoded gray text)
    - users/page.tsx does NOT contain `bg-purple-` (no hardcoded purple badge)
    - billing/page.tsx contains `card` as className (stat cards)
    - billing/page.tsx contains `border-l-4 border-accent` (emphasized card)
    - billing/page.tsx contains `border-l-4 border-success` (success feedback)
    - billing/page.tsx contains `border-l-4 border-error` (error feedback)
    - billing/page.tsx contains `tabular-nums` (numeric values)
    - billing/page.tsx does NOT contain `bg-white` (no hardcoded white)
    - billing/page.tsx does NOT contain `bg-gray-` (no hardcoded grays)
    - billing/page.tsx does NOT contain `bg-primary-100` (no icon-circle pattern)
    - billing/page.tsx does NOT contain `bg-green-100` (no icon-circle pattern)
    - billing/page.tsx does NOT contain `bg-purple-100` (no icon-circle pattern)
    - billing/page.tsx does NOT contain `text-green-600` (no semantic-colored stat values)
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <done>Users table has DESIGN.md table styling with semantic tokens, role badges, and row hover states. Billing overview has uniform stat cards, cleaned quick actions (no icon circles), and border-l-4 feedback states. No hardcoded gray-*, bg-white, or AI slop patterns remain in either file.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web build` passes with zero errors
- No `bg-gray-*`, `text-gray-*`, `border-gray-*`, `bg-white`, `shadow` (standalone) remain in the 4 modified files
- No `colorClasses` rainbow object in page.tsx
- No `bg-purple-100`, `bg-primary-50`, `bg-green-100` icon-circle patterns remain
- Admin sidebar has 5 nav items including Postcards
</verification>

<success_criteria>
All 4 files (layout.tsx, page.tsx, users/page.tsx, billing/page.tsx) compile without errors, use only DESIGN.md semantic tokens, and contain no AI slop blacklist violations.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-pages/07-01-SUMMARY.md`
</output>
