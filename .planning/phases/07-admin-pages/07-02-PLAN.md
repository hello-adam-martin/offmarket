---
phase: 07-admin-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/admin/billing/subscriptions/page.tsx
  - apps/web/src/app/admin/billing/escrows/page.tsx
  - apps/web/src/app/admin/billing/settings/page.tsx
  - apps/web/src/app/admin/email-templates/page.tsx
  - apps/web/src/app/admin/email-templates/[name]/page.tsx
  - apps/web/src/app/admin/postcards/page.tsx
autonomous: true
requirements:
  - ADMN-05
  - ADMN-06
  - ADMN-07
  - ADMN-08
  - ADMN-09
must_haves:
  truths:
    - "Subscriptions table has status badges: ACTIVE=badge-success, CANCELED=badge-neutral, PAST_DUE=badge-error, TRIALING=badge-info"
    - "Escrows table shows monetary values in font-mono tabular-nums right-aligned, with status badges per D-14 through D-18"
    - "Billing settings form sections are each wrapped in .card with .input fields and btn-primary save buttons"
    - "Email templates listing and editor use .card and .input classes with no hardcoded gray-* or bg-white"
    - "Postcards admin page uses DESIGN.md table or card patterns with semantic tokens throughout"
  artifacts:
    - path: "apps/web/src/app/admin/billing/subscriptions/page.tsx"
      provides: "Restyled subscriptions table with status badges"
      contains: "badge-success"
    - path: "apps/web/src/app/admin/billing/escrows/page.tsx"
      provides: "Restyled escrows table with financial data formatting"
      contains: "font-mono"
    - path: "apps/web/src/app/admin/billing/settings/page.tsx"
      provides: "Restyled billing settings form"
      contains: "card"
    - path: "apps/web/src/app/admin/email-templates/page.tsx"
      provides: "Restyled email templates listing"
      contains: "card"
    - path: "apps/web/src/app/admin/email-templates/[name]/page.tsx"
      provides: "Restyled email template editor"
      contains: "input"
    - path: "apps/web/src/app/admin/postcards/page.tsx"
      provides: "Restyled postcards admin"
      contains: "card"
  key_links:
    - from: "apps/web/src/app/admin/billing/escrows/page.tsx"
      to: "globals.css"
      via: "badge-* and font-mono classes"
      pattern: "badge-(warning|info|success|neutral|error)"
    - from: "apps/web/src/app/admin/billing/settings/page.tsx"
      to: "globals.css"
      via: ".card and .input classes"
      pattern: "card.*input"
---

<objective>
Restyle the billing detail pages (subscriptions, escrows, settings), email template pages (listing + editor), and postcards admin with DESIGN.md semantic tokens.

Purpose: These 6 files complete the admin pages phase. The billing detail pages contain the most critical financial data display requiring fintech-grade formatting (font-mono tabular-nums, correct status badge mapping). The billing settings page at 823 lines is the largest single file in the phase.

Output: 6 files restyled with DESIGN.md compliance — correct status badge semantic mapping, font-mono monetary values, form sections in .card containers, no hardcoded colors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/07-admin-pages/07-CONTEXT.md
@.planning/phases/07-admin-pages/07-UI-SPEC.md
@apps/web/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle subscriptions table, escrows table, and billing settings form</name>
  <files>apps/web/src/app/admin/billing/subscriptions/page.tsx, apps/web/src/app/admin/billing/escrows/page.tsx, apps/web/src/app/admin/billing/settings/page.tsx</files>
  <read_first>
    - apps/web/src/app/admin/billing/subscriptions/page.tsx
    - apps/web/src/app/admin/billing/escrows/page.tsx
    - apps/web/src/app/admin/billing/settings/page.tsx
    - apps/web/src/app/globals.css
    - .planning/phases/07-admin-pages/07-UI-SPEC.md
    - DESIGN.md
  </read_first>
  <action>
**subscriptions/page.tsx (ADMN-05, per D-09 through D-13, D-19 through D-22):**

Page heading: `text-xl font-display font-semibold text-text-base`.

Table wrapper: replace `bg-white rounded-lg shadow` (or similar) with `card overflow-x-auto`. Inner table: `w-full min-w-[640px]`.

Table header: `<tr class="border-b border-border">` with `<th class="px-6 py-3 text-left text-xs font-semibold text-text-muted uppercase tracking-wide">`.

Table body: `<tbody class="divide-y divide-border">` with rows `<tr class="hover:bg-surface-raised transition-colors">`.

Standard cells: `<td class="px-6 py-4 text-sm text-text-base">`.
Date cells: `<td class="px-6 py-4 text-sm text-text-secondary tabular-nums">`.
Dollar amount cells: `<td class="px-6 py-4 text-right font-mono text-sm text-text-base tabular-nums">`. Format all dollar amounts with cents: `$X.XX`.

Create or update STATUS_LABELS mapping object for subscription statuses (per D-19 through D-22):
```typescript
const STATUS_LABELS: Record<string, { label: string; badgeClass: string }> = {
  ACTIVE: { label: "Active", badgeClass: "badge-success" },
  CANCELED: { label: "Canceled", badgeClass: "badge-neutral" },
  PAST_DUE: { label: "Past Due", badgeClass: "badge-error" },
  TRIALING: { label: "Trialing", badgeClass: "badge-info" },
};
```
Render status badges: `<span className={STATUS_LABELS[status]?.badgeClass || "badge-neutral"}>{STATUS_LABELS[status]?.label || status}</span>`

Replace ALL token swaps:
- `bg-white` -> `bg-surface`
- `bg-gray-*` -> semantic tokens
- `text-gray-*` -> semantic tokens per Token Swap Reference
- `border-gray-*` -> `border-border`
- `divide-gray-*` -> `divide-border`
- `shadow` on cards -> remove
- `max-w-7xl` -> `max-w-content`
- Any `bg-green-*`/`bg-red-*`/`bg-yellow-*` status badges -> STATUS_LABELS mapping

Action buttons: `btn-secondary btn-sm` for standard, `btn-destructive btn-sm` for cancel/delete. Right-aligned column.

Loading skeletons: `bg-surface-raised` animate-pulse.

**escrows/page.tsx (ADMN-06, per D-09 through D-18):**

Same table pattern as subscriptions above.

Create or update STATUS_LABELS for escrow statuses (per D-14 through D-18):
```typescript
const ESCROW_STATUS_LABELS: Record<string, { label: string; badgeClass: string }> = {
  PENDING: { label: "Pending", badgeClass: "badge-warning" },
  HELD: { label: "Held", badgeClass: "badge-info" },
  RELEASED: { label: "Released", badgeClass: "badge-success" },
  REFUNDED: { label: "Refunded", badgeClass: "badge-neutral" },
  EXPIRED: { label: "Expired", badgeClass: "badge-error" },
};
```

ALL monetary columns (deposit amount, etc.): `<td class="px-6 py-4 text-right font-mono text-sm text-text-base tabular-nums">$X.XX</td>`. Always show cents.

Date columns: `text-text-secondary tabular-nums`.

Replace all hardcoded status colors with ESCROW_STATUS_LABELS badge mapping.

Same full token swap as subscriptions page.

**settings/page.tsx (ADMN-04, 823 lines — per D-23 through D-29):**

This is the largest file. Apply token swaps methodically section by section.

Page heading: `text-xl font-display font-semibold text-text-base`.

Each form section: wrap in `card` class (replace `bg-white rounded-lg shadow p-6`).
- Section heading inside card: `text-lg font-semibold text-text-base mb-4`
- Form labels: use existing label patterns or `text-sm font-medium text-text-secondary mb-1`
- Form inputs: ensure all have `input` class
- Select elements: ensure all have `input` class
- Save buttons: `btn-primary`
- Destructive actions: `btn-destructive`

Success/error feedback states:
- Replace `bg-green-50 border-green-200 text-green-700` with `card border-l-4 border-success` + `<p class="text-sm text-text-base">`
- Replace `bg-red-50 border-red-200 text-red-700` with `card border-l-4 border-error` + `<p class="text-sm text-text-base">`

Complete token swap for ALL 823 lines:
- `bg-white` -> `bg-surface`
- `bg-gray-*` -> semantic
- `text-gray-*` -> semantic per Token Swap Reference
- `border-gray-*` -> `border-border`
- `hover:bg-gray-*` -> `hover:bg-surface-raised`
- `rounded-lg shadow` -> `card` class
- `max-w-7xl` -> `max-w-content`
- `text-2xl font-bold` heading -> `text-xl font-display font-semibold`

Loading skeletons: `bg-surface-raised` animate-pulse.

Numeric displays: add `tabular-nums` where numbers appear.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - subscriptions/page.tsx contains `badge-success` (ACTIVE status)
    - subscriptions/page.tsx contains `badge-error` (PAST_DUE status)
    - subscriptions/page.tsx contains `badge-neutral` (CANCELED status)
    - subscriptions/page.tsx contains `badge-info` (TRIALING status)
    - subscriptions/page.tsx contains `font-mono` (monetary values)
    - subscriptions/page.tsx contains `tabular-nums` (numeric values)
    - subscriptions/page.tsx contains `text-text-muted uppercase tracking-wide` (table headers)
    - subscriptions/page.tsx contains `hover:bg-surface-raised` (row hover)
    - subscriptions/page.tsx does NOT contain `bg-white` or `bg-gray-` or `text-gray-`
    - escrows/page.tsx contains `badge-warning` (PENDING)
    - escrows/page.tsx contains `badge-info` (HELD)
    - escrows/page.tsx contains `badge-success` (RELEASED)
    - escrows/page.tsx contains `badge-neutral` (REFUNDED)
    - escrows/page.tsx contains `badge-error` (EXPIRED)
    - escrows/page.tsx contains `font-mono` and `tabular-nums` (monetary values)
    - escrows/page.tsx contains `text-right` (right-aligned monetary column)
    - escrows/page.tsx does NOT contain `bg-white` or `bg-gray-` or `text-gray-`
    - settings/page.tsx contains `card` as className (form section wrappers)
    - settings/page.tsx contains `input` as className (form fields)
    - settings/page.tsx contains `btn-primary` (save buttons)
    - settings/page.tsx does NOT contain `bg-white` (no hardcoded white)
    - settings/page.tsx does NOT contain `bg-gray-` (no hardcoded grays)
    - settings/page.tsx does NOT contain `text-gray-` (no hardcoded gray text)
    - settings/page.tsx does NOT contain `shadow` without `card` class context
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <done>Subscriptions and escrows tables have fintech-grade formatting: font-mono tabular-nums right-aligned monetary values, correct status badge mapping per D-14 through D-22, DESIGN.md table styling. Billing settings form has all sections in .card containers with .input fields. No hardcoded colors remain in any of the 3 files.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle email templates pages and postcards admin</name>
  <files>apps/web/src/app/admin/email-templates/page.tsx, apps/web/src/app/admin/email-templates/[name]/page.tsx, apps/web/src/app/admin/postcards/page.tsx</files>
  <read_first>
    - apps/web/src/app/admin/email-templates/page.tsx
    - apps/web/src/app/admin/email-templates/[name]/page.tsx
    - apps/web/src/app/admin/postcards/page.tsx
    - apps/web/src/app/globals.css
    - .planning/phases/07-admin-pages/07-UI-SPEC.md
    - DESIGN.md
  </read_first>
  <action>
**email-templates/page.tsx (ADMN-07):**

Page heading: `text-xl font-display font-semibold text-text-base`.

If the page has a table listing templates:
- Table wrapper: `card overflow-x-auto`
- Table header: `<tr class="border-b border-border">` with `<th class="px-6 py-3 text-left text-xs font-semibold text-text-muted uppercase tracking-wide">`
- Table body: `<tbody class="divide-y divide-border">` with `<tr class="hover:bg-surface-raised transition-colors">`
- Standard cells: `<td class="px-6 py-4 text-sm text-text-base">`
- Date cells: `text-text-secondary tabular-nums`
- Action column: `btn-secondary btn-sm` for edit, `btn-destructive btn-sm` for delete

If the page has a card listing instead of table:
- Each template card: `card` class
- Template name: `text-sm font-semibold text-text-base`
- Subject/description: `text-sm text-text-secondary`
- Action links: `text-sm text-accent hover:text-accent-hover`

Search/filter bar (if present): `<input class="input max-w-sm" placeholder="Search templates..." />`

Empty state: if no templates, show "No templates" / "Add an email template to get started." pattern.

Complete token swap:
- `bg-white` -> `bg-surface`
- `bg-gray-*` -> semantic
- `text-gray-*` -> semantic per Token Swap Reference
- `border-gray-*` -> `border-border`
- `rounded-lg shadow` -> `card`
- `max-w-7xl` -> `max-w-content`

**email-templates/[name]/page.tsx (ADMN-08):**

Page heading: `text-xl font-display font-semibold text-text-base`.

Editor form: wrap in `card` class.
- Form labels: `text-sm font-medium text-text-secondary mb-1`
- Template name input: `input` class
- Subject input: `input` class
- Body textarea: `input min-h-[300px] font-mono text-sm resize-y`
- Variable placeholder guidance below textarea: `<p class="text-sm text-text-muted mt-2">Available variables: {{variableName}} -- replaced at send time</p>`
- Save button: `btn-primary` with text "Save Template"
- Delete button (if present): `btn-destructive` with text "Delete Template"

Preview area (if present): `card bg-surface-raised` with content in `font-sans text-sm text-text-base`.

Complete token swap (same list as above).

Success/error feedback:
- Success: `card border-l-4 border-success` with `<p class="text-sm text-text-base">Changes saved.</p>`
- Error: `card border-l-4 border-error` with `<p class="text-sm text-text-base">{error message}</p>`

**postcards/page.tsx (ADMN-09, 651 lines):**

Page heading: `text-xl font-display font-semibold text-text-base`.

If the page has a data table:
- Table wrapper: `card overflow-x-auto`
- Apply same table pattern as other admin tables (header, body, dividers, hover)
- Date cells: `text-text-secondary tabular-nums`
- Status badges: map postcard statuses to appropriate `badge-*` classes:
  - Any "pending"/"requested" state -> `badge-warning`
  - Any "sent"/"mailed"/"completed" state -> `badge-success`
  - Any "canceled"/"rejected" state -> `badge-error`
  - Any "processing"/"in progress" state -> `badge-info`
  - Fallback -> `badge-neutral`
- Action buttons: `btn-secondary btn-sm` for standard actions, `btn-destructive btn-sm` for cancel/reject

If the page uses card layout for postcard requests:
- Each request card: `card` class
- Request details in semantic tokens
- Address/recipient info in `text-sm text-text-secondary`

Complete token swap:
- `bg-white` -> `bg-surface`
- `bg-gray-*` -> semantic
- `text-gray-*` -> semantic per Token Swap Reference
- `border-gray-*` -> `border-border`
- `divide-gray-*` -> `divide-border`
- `hover:bg-gray-*` -> `hover:bg-surface-raised`
- `rounded-lg shadow` -> `card`
- `max-w-7xl` -> `max-w-content`
- Any hardcoded color badges -> `badge-*` classes

Loading skeletons: `bg-surface-raised` animate-pulse.

Numeric displays (counts, IDs): `tabular-nums`.

Empty state: "No postcard requests" / "Postcard requests from buyers will appear here."
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - email-templates/page.tsx contains `card` as className
    - email-templates/page.tsx contains `text-text-muted` (table headers or metadata)
    - email-templates/page.tsx does NOT contain `bg-white`
    - email-templates/page.tsx does NOT contain `bg-gray-`
    - email-templates/page.tsx does NOT contain `text-gray-`
    - email-templates/[name]/page.tsx contains `input` as className (form fields)
    - email-templates/[name]/page.tsx contains `font-mono` (textarea for template body)
    - email-templates/[name]/page.tsx contains `card` as className (form wrapper)
    - email-templates/[name]/page.tsx contains `btn-primary` (save button)
    - email-templates/[name]/page.tsx does NOT contain `bg-white`
    - email-templates/[name]/page.tsx does NOT contain `bg-gray-`
    - email-templates/[name]/page.tsx does NOT contain `text-gray-`
    - postcards/page.tsx contains `card` as className
    - postcards/page.tsx contains `tabular-nums` (numeric displays)
    - postcards/page.tsx does NOT contain `bg-white`
    - postcards/page.tsx does NOT contain `bg-gray-`
    - postcards/page.tsx does NOT contain `text-gray-`
    - postcards/page.tsx does NOT contain `shadow` as standalone class without `.card`
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <done>Email templates listing uses DESIGN.md table/card pattern. Template editor has card-wrapped form with .input fields and font-mono textarea. Postcards admin uses DESIGN.md table styling with status badges. No hardcoded gray-*, bg-white, or shadow remain in any of the 3 files.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web build` passes with zero errors
- No `bg-gray-*`, `text-gray-*`, `border-gray-*`, `bg-white`, standalone `shadow` remain in the 6 modified files
- Subscriptions uses STATUS_LABELS with badge-success/badge-neutral/badge-error/badge-info
- Escrows uses ESCROW_STATUS_LABELS with badge-warning/badge-info/badge-success/badge-neutral/badge-error
- All monetary values use font-mono tabular-nums and show cents ($X.XX)
- All date values use text-text-secondary tabular-nums
</verification>

<success_criteria>
All 6 files compile without errors, use only DESIGN.md semantic tokens, have correct status badge mappings, and financial data displays font-mono tabular-nums with right-alignment.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-pages/07-02-SUMMARY.md`
</output>
