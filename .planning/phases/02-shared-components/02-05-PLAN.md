---
phase: 02-shared-components
plan: 05
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - apps/web/src/components/AddressAutocomplete.tsx
  - apps/web/src/components/PropertyImageUpload.tsx
autonomous: true
requirements:
  - COMP-13
  - COMP-14
must_haves:
  truths:
    - "AddressAutocomplete input uses .input class with teal focus ring"
    - "AddressAutocomplete dropdown uses semantic surface background, border, and shadow"
    - "PropertyImageUpload drop zone uses dashed border, surface-raised background, accent highlights on hover/drag"
    - "PropertyImageUpload image previews use rounded-md thumbnails"
  artifacts:
    - path: "apps/web/src/components/AddressAutocomplete.tsx"
      provides: "Restyled address autocomplete"
      contains: "bg-surface"
    - path: "apps/web/src/components/PropertyImageUpload.tsx"
      provides: "Restyled property image upload"
      contains: "border-dashed"
  key_links:
    - from: "apps/web/src/components/AddressAutocomplete.tsx"
      to: "apps/web/src/app/globals.css"
      via: ".input class on autocomplete input"
      pattern: "input"
---

<objective>
Restyle AddressAutocomplete and PropertyImageUpload utility components with semantic tokens.

Purpose: These utility components are used in property registration and wanted ad creation flows. They need to match the design system before page-specific work in later phases.

Output: AddressAutocomplete with semantic dropdown styling. PropertyImageUpload with DESIGN.md drop zone treatment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-components/02-CONTEXT.md
@.planning/phases/02-shared-components/02-UI-SPEC.md
@.planning/phases/02-shared-components/02-RESEARCH.md

@apps/web/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle AddressAutocomplete with semantic tokens</name>
  <files>apps/web/src/components/AddressAutocomplete.tsx</files>
  <read_first>
    - apps/web/src/components/AddressAutocomplete.tsx (343 lines — Google Maps autocomplete with custom dropdown)
    - .planning/phases/02-shared-components/02-UI-SPEC.md (AddressAutocomplete contract section)
    - .planning/phases/02-shared-components/02-RESEARCH.md (Pattern 1 — token replacement)
    - apps/web/src/app/globals.css (verify .input class exists)
  </read_first>
  <action>
Restyle AddressAutocomplete.tsx with semantic tokens per UI-SPEC contract.

**Input element**: Apply `.input` class (which provides `rounded-md border-border bg-surface focus:border-accent focus:ring-1 focus:ring-accent-light`).

**Dropdown container**: Replace current styling with:
`className="bg-surface rounded-md border border-border shadow-lg mt-1"`

**Dropdown items**: Replace with:
`className="px-4 py-2 text-sm text-text-base hover:bg-surface-raised cursor-pointer"`

**Active/highlighted dropdown item**:
`className="px-4 py-2 text-sm bg-accent-light text-accent cursor-pointer"`

**Loading state text**:
`className="text-xs text-text-muted px-4 py-2"` with text `"Searching..."`

**No results text**:
`className="text-xs text-text-muted px-4 py-2"` with text `"No addresses found"`

**Token replacements throughout:**
- `bg-white` -> `bg-surface`
- `bg-gray-*` -> appropriate semantic token
- `border-gray-*` -> `border-border`
- `text-gray-900`/`800` -> `text-text-base`
- `text-gray-600`/`700` -> `text-text-secondary`
- `text-gray-400`/`500` -> `text-text-muted`
- `bg-primary-*` / `text-primary-*` -> `bg-accent` / `text-accent`
- `hover:bg-gray-*` -> `hover:bg-surface-raised`
- `focus:ring-primary-*` -> `focus:ring-accent-light`
- `rounded-xl` -> `rounded-md`

**Do NOT change:** Google Maps API integration, debounce logic, suggestion fetching, selection callbacks, or prop interfaces.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AddressAutocomplete.tsx contains `input` class reference (for the text input)
    - AddressAutocomplete.tsx contains `bg-surface` (dropdown background)
    - AddressAutocomplete.tsx contains `border-border` (dropdown border)
    - AddressAutocomplete.tsx contains `shadow-lg` (dropdown shadow)
    - AddressAutocomplete.tsx contains `hover:bg-surface-raised` (item hover)
    - AddressAutocomplete.tsx contains `bg-accent-light` (active item)
    - AddressAutocomplete.tsx does NOT contain `bg-white`
    - AddressAutocomplete.tsx does NOT contain `text-gray-`
    - AddressAutocomplete.tsx does NOT contain `border-gray-`
    - AddressAutocomplete.tsx does NOT contain `bg-primary-`
    - `pnpm --filter @offmarket/web build` exits 0
  </acceptance_criteria>
  <done>AddressAutocomplete uses .input class on text field, semantic dropdown with surface background, accent-light active items. All hardcoded color classes removed. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle PropertyImageUpload with DESIGN.md drop zone treatment</name>
  <files>apps/web/src/components/PropertyImageUpload.tsx</files>
  <read_first>
    - apps/web/src/components/PropertyImageUpload.tsx (329 lines — image upload with drag-and-drop, preview grid, remove buttons)
    - .planning/phases/02-shared-components/02-UI-SPEC.md (PropertyImageUpload contract section)
    - .planning/phases/02-shared-components/02-RESEARCH.md (Pattern 1 — token replacement)
  </read_first>
  <action>
Restyle PropertyImageUpload.tsx per UI-SPEC contract.

**Drop zone default state**:
`className="rounded-lg border-2 border-dashed border-border bg-surface-raised p-8 text-center"`

**Drop zone hover state** (mouse over):
`className="rounded-lg border-2 border-dashed border-accent bg-accent-light/20 p-8 text-center"`

**Drop zone active state** (file being dragged over):
`className="rounded-lg border-2 border-dashed border-accent bg-accent-light/30 p-8 text-center"`

Implement hover/active states by toggling classes based on drag event state (the component likely already has `isDragging` or similar state).

**Drop zone label**: `className="text-sm text-text-secondary"` with text `"Drop images here, or click to select"`

**Drop zone subtext**: `className="text-xs text-text-muted mt-1"` with text `"JPG or PNG, up to 10MB each"`

**Image preview thumbnails**: `className="rounded-md"` (8px border radius) in a grid layout.

**Remove image button**: Either `.btn-destructive btn-sm` for a text button, or an icon button with `className="absolute top-1 right-1 bg-error text-white rounded-md h-6 w-6 flex items-center justify-center"` with `aria-label="Remove image"`.

**Upload progress bar**: `className="bg-accent h-1 rounded-full"` for the progress indicator.

**Token replacements throughout:**
- `bg-white` -> `bg-surface`
- `bg-gray-*` -> appropriate semantic token
- `border-gray-*` -> `border-border`
- `text-gray-900`/`800` -> `text-text-base`
- `text-gray-600`/`700` -> `text-text-secondary`
- `text-gray-400`/`500` -> `text-text-muted`
- `bg-primary-*` / `text-primary-*` -> `bg-accent` / `text-accent`
- `bg-red-*` (delete button context) -> `bg-error`
- `text-red-*` -> `text-error`
- `rounded-xl` -> `rounded-lg` for containers, `rounded-md` for thumbnails

**Do NOT change:** File handling logic, upload progress tracking, image preview generation, size validation, or prop interfaces.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PropertyImageUpload.tsx contains `border-dashed` (drop zone border style)
    - PropertyImageUpload.tsx contains `bg-surface-raised` (drop zone default background)
    - PropertyImageUpload.tsx contains `border-accent` (drop zone hover/active border)
    - PropertyImageUpload.tsx contains `rounded-md` (thumbnail border radius)
    - PropertyImageUpload.tsx contains `bg-accent` (progress bar)
    - PropertyImageUpload.tsx contains `aria-label` with "Remove image" (accessibility)
    - PropertyImageUpload.tsx does NOT contain `bg-white`
    - PropertyImageUpload.tsx does NOT contain `text-gray-`
    - PropertyImageUpload.tsx does NOT contain `border-gray-`
    - PropertyImageUpload.tsx does NOT contain `bg-primary-`
    - `pnpm --filter @offmarket/web build` exits 0
  </acceptance_criteria>
  <done>PropertyImageUpload uses DESIGN.md drop zone styling with dashed accent border on hover/drag, surface-raised background, rounded-md thumbnails, accent progress bar, and accessible remove button. All hardcoded color classes removed. TypeScript compiles.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @offmarket/web build` passes
2. AddressAutocomplete uses .input class and semantic dropdown styling
3. PropertyImageUpload uses DESIGN.md drop zone treatment
4. No hardcoded color classes remain in either file
</verification>

<success_criteria>
- AddressAutocomplete input uses .input class, dropdown uses semantic surface/border/shadow
- PropertyImageUpload drop zone has dashed border, accent highlights on interaction states
- Image thumbnails use rounded-md
- Remove button has aria-label for accessibility
- Zero hardcoded color classes across both files
- TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-components/02-05-SUMMARY.md`
</output>
