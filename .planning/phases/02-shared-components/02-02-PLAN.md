---
phase: 02-shared-components
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/src/components/ContactBuyerModal.tsx
  - apps/web/src/components/PaymentModal.tsx
  - apps/web/src/components/UpgradeModal.tsx
  - apps/web/src/components/PostcardRequestModal.tsx
  - apps/web/src/components/browse/SaveSearchModal.tsx
autonomous: true
requirements:
  - COMP-04
  - COMP-05
must_haves:
  truths:
    - "All 5 modals use semantic surface background, correct border, and shadow"
    - "ContactBuyerModal and PaymentModal use modal-panel-lg (576px max-width)"
    - "UpgradeModal, PostcardRequestModal, and SaveSearchModal use modal-panel-sm (448px max-width)"
    - "All modal backdrops use bg-black/50 with backdrop-blur-sm"
    - "All form inputs within modals use .input class with teal focus ring"
    - "No icon-in-colored-circle patterns remain in any modal"
  artifacts:
    - path: "apps/web/src/components/ContactBuyerModal.tsx"
      provides: "Restyled contact buyer modal"
      contains: "modal-panel-lg"
    - path: "apps/web/src/components/PaymentModal.tsx"
      provides: "Restyled payment modal"
      contains: "modal-panel-lg"
    - path: "apps/web/src/components/UpgradeModal.tsx"
      provides: "Restyled upgrade modal"
      contains: "modal-panel-sm"
    - path: "apps/web/src/components/PostcardRequestModal.tsx"
      provides: "Restyled postcard request modal"
      contains: "modal-panel-sm"
    - path: "apps/web/src/components/browse/SaveSearchModal.tsx"
      provides: "Restyled save search modal"
      contains: "modal-panel-sm"
  key_links:
    - from: "all 5 modals"
      to: "apps/web/src/app/globals.css"
      via: "CSS classes modal-panel-sm, modal-panel-lg, modal-backdrop"
      pattern: "modal-panel"
---

<objective>
Restyle all 5 modal components to use DESIGN.md semantic tokens and the new modal shell CSS classes from globals.css.

Purpose: Modals appear throughout the app (contact buyer, payment, upgrade, postcard request, save search). Restyling them ensures consistent visual treatment across all interactive overlays.

Output: 5 restyled modal files using modal-panel-sm/lg classes, semantic tokens, and correct form input styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-components/02-CONTEXT.md
@.planning/phases/02-shared-components/02-UI-SPEC.md
@.planning/phases/02-shared-components/02-RESEARCH.md

@apps/web/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle ContactBuyerModal and PaymentModal (large panel modals)</name>
  <files>apps/web/src/components/ContactBuyerModal.tsx, apps/web/src/components/PaymentModal.tsx</files>
  <read_first>
    - apps/web/src/components/ContactBuyerModal.tsx (304 lines — has 3 render branches: loading, no-access, contact form. Each branch has its own backdrop+panel. Contains icon-in-colored-circle AI slop that must be removed)
    - apps/web/src/components/PaymentModal.tsx (313 lines — Stripe payment form modal)
    - .planning/phases/02-shared-components/02-UI-SPEC.md (Modal Shell section, Modal Content Structure section)
    - .planning/phases/02-shared-components/02-RESEARCH.md (Pattern 5 — Modal Content Structure, Pitfall 5 — Modal Branch Count)
    - apps/web/src/app/globals.css (verify modal-panel-lg class exists from Plan 01)
  </read_first>
  <action>
**ContactBuyerModal.tsx** — This modal has 3 separate return branches (Research Pitfall 5). ALL THREE must be updated:

1. **Loading state branch** (~line 116): Replace backdrop div `className="fixed inset-0 z-50 flex items-center justify-center bg-black/50"` with `className="modal-backdrop fixed inset-0 z-50 flex items-center justify-center"`. Replace panel div `className="bg-white rounded-lg shadow-xl w-full max-w-md mx-4"` (or similar) with `className="modal-panel-sm"`.

2. **No-access state branch** (~line 132): Same backdrop and panel replacements as loading state. Use `modal-panel-sm` for this state.

3. **Contact form branch** (~line 230): Backdrop same replacement. Panel uses `className="modal-panel-lg"` (576px for complex content per D-09).

**Token replacements across ALL branches:**
- `bg-white` -> `bg-surface`
- `bg-gray-50` / `bg-gray-100` -> `bg-surface-raised`
- `border-gray-200` / `border-gray-300` -> `border-border`
- `text-gray-900` / `text-gray-800` -> `text-text-base`
- `text-gray-600` / `text-gray-700` -> `text-text-secondary`
- `text-gray-400` / `text-gray-500` -> `text-text-muted`
- `bg-primary-600` / `bg-primary-500` -> `bg-accent`
- `text-primary-600` -> `text-accent`
- `bg-primary-100` -> `bg-accent-light`
- `hover:bg-primary-700` -> `hover:bg-accent-hover`
- `focus:ring-primary-500` -> `focus:ring-accent-light`
- `focus:border-primary-500` -> `focus:border-accent`
- `rounded-xl` (on any element) -> `rounded-lg` for panels, `rounded-md` for buttons

**AI slop removal** — Remove icon-in-colored-circle pattern (~line 157):
```tsx
// REMOVE this pattern:
<div className="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-primary-100 mb-4">
  <svg className="h-6 w-6 text-primary-600" ...>
</div>
// REPLACE with inline icon only (no background circle):
<svg className="h-6 w-6 text-text-secondary mx-auto mb-4" ...>
```

**Form inputs**: Apply `.input` class to all text inputs and textareas. Apply `.label` class to all form labels. Apply `.btn-primary` to submit button, `.btn-ghost` or `.btn-secondary` to cancel button.

**Modal header pattern**: `className="flex items-center justify-between p-6 border-b border-border"`
**Modal body**: `className="p-6"`
**Modal footer**: `className="flex items-center justify-end gap-3 p-6 border-t border-border"`
**Close button**: `className="btn-ghost h-8 w-8 p-0 rounded-md"` with X icon, `aria-label="Close"`

**PaymentModal.tsx** — Apply same token replacement map. Uses `modal-panel-lg` (complex Stripe content). Token swap all hardcoded colors. Keep all Stripe integration logic, Elements rendering, and payment processing unchanged.

**Do NOT change:** Any fetch/API calls, form submission handlers, Stripe elements, conditional rendering logic, or prop interfaces.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ContactBuyerModal.tsx contains `modal-panel-lg` (at least once for the form branch)
    - ContactBuyerModal.tsx contains `modal-panel-sm` (for loading and no-access branches)
    - ContactBuyerModal.tsx contains `modal-backdrop` (at least once)
    - ContactBuyerModal.tsx does NOT contain `bg-white`
    - ContactBuyerModal.tsx does NOT contain `text-gray-`
    - ContactBuyerModal.tsx does NOT contain `border-gray-`
    - ContactBuyerModal.tsx does NOT contain `bg-primary-100` (icon-in-circle removed)
    - ContactBuyerModal.tsx does NOT contain `rounded-full bg-primary` (icon-in-circle removed)
    - PaymentModal.tsx contains `modal-panel-lg`
    - PaymentModal.tsx contains `modal-backdrop`
    - PaymentModal.tsx does NOT contain `bg-white`
    - PaymentModal.tsx does NOT contain `text-gray-`
    - PaymentModal.tsx does NOT contain `border-gray-`
    - `pnpm --filter @offmarket/web build` exits 0
  </acceptance_criteria>
  <done>ContactBuyerModal and PaymentModal use modal shell classes, semantic tokens, correct form styling. All 3 ContactBuyerModal branches updated. AI slop icon-in-circle removed. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle UpgradeModal, PostcardRequestModal, and SaveSearchModal (small panel modals)</name>
  <files>apps/web/src/components/UpgradeModal.tsx, apps/web/src/components/PostcardRequestModal.tsx, apps/web/src/components/browse/SaveSearchModal.tsx</files>
  <read_first>
    - apps/web/src/components/UpgradeModal.tsx (140 lines — plan upgrade CTA modal)
    - apps/web/src/components/PostcardRequestModal.tsx (postcard request form)
    - apps/web/src/components/browse/SaveSearchModal.tsx (save search form)
    - .planning/phases/02-shared-components/02-UI-SPEC.md (Modal Shell section — all 3 use modal-panel-sm)
    - apps/web/src/app/globals.css (verify modal-panel-sm class exists)
  </read_first>
  <action>
All 3 modals use `modal-panel-sm` (max-w-md, 448px) per D-09 — they are simple forms/confirms.

**For each modal, apply the same pattern:**

1. **Backdrop**: Replace any `fixed inset-0 ... bg-black/50` or `bg-black bg-opacity-50` with `className="modal-backdrop fixed inset-0 z-50 flex items-center justify-center"`.

2. **Panel**: Replace `bg-white rounded-lg shadow-xl w-full max-w-md mx-4` (or similar) with `className="modal-panel-sm"`.

3. **Token replacements** (same map as Task 1):
   - `bg-white` -> `bg-surface`
   - `bg-gray-*` -> appropriate semantic token
   - `border-gray-*` -> `border-border`
   - `text-gray-900`/`text-gray-800` -> `text-text-base`
   - `text-gray-600`/`text-gray-700` -> `text-text-secondary`
   - `text-gray-400`/`text-gray-500` -> `text-text-muted`
   - `bg-primary-*` / `text-primary-*` -> `bg-accent` / `text-accent`
   - `hover:bg-primary-*` -> `hover:bg-accent-hover`
   - `focus:ring-primary-*` -> `focus:ring-accent-light`
   - `rounded-xl` -> `rounded-lg` for panels, `rounded-md` for buttons/inputs

4. **Form elements**: Apply `.input` class to inputs/textareas, `.label` to labels, `.btn-primary` to primary CTA, `.btn-ghost` or `.btn-secondary` to secondary/cancel.

5. **Modal structure**:
   - Header: `flex items-center justify-between p-6 border-b border-border`
   - Body: `p-6`
   - Footer: `flex items-center justify-end gap-3 p-6 border-t border-border`
   - Close button: `btn-ghost h-8 w-8 p-0 rounded-md` with `aria-label="Close"`

6. **CTA copy per UI-SPEC Copywriting Contract**:
   - UpgradeModal: `"Upgrade to Pro"`
   - SaveSearchModal: `"Save search"`
   - PostcardRequestModal: `"Send postcard request"`

**Do NOT change:** Any form submission logic, API calls, prop interfaces, or conditional rendering.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - UpgradeModal.tsx contains `modal-panel-sm`
    - UpgradeModal.tsx contains `modal-backdrop`
    - UpgradeModal.tsx does NOT contain `bg-white`
    - UpgradeModal.tsx does NOT contain `text-gray-`
    - PostcardRequestModal.tsx contains `modal-panel-sm`
    - PostcardRequestModal.tsx does NOT contain `bg-white`
    - PostcardRequestModal.tsx does NOT contain `text-gray-`
    - SaveSearchModal.tsx contains `modal-panel-sm`
    - SaveSearchModal.tsx does NOT contain `bg-white`
    - SaveSearchModal.tsx does NOT contain `text-gray-`
    - `pnpm --filter @offmarket/web build` exits 0
  </acceptance_criteria>
  <done>All 3 small-panel modals use modal-panel-sm, semantic tokens, correct form styling. CTA copy matches UI-SPEC copywriting contract. No hardcoded color classes remain. TypeScript compiles.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @offmarket/web build` passes
2. All 5 modals contain `modal-panel-sm` or `modal-panel-lg` (correct size per UI-SPEC)
3. All 5 modals contain `modal-backdrop`
4. No modal file contains `bg-white`, `text-gray-*`, `border-gray-*`, `bg-primary-*`, `text-primary-*`
5. ContactBuyerModal has no icon-in-colored-circle pattern
6. All form inputs use `.input` class
</verification>

<success_criteria>
- 5 modals restyled with modal shell CSS classes and semantic tokens
- ContactBuyerModal all 3 branches updated (loading, no-access, form)
- AI slop icon-in-circle removed from ContactBuyerModal
- All form elements use .input and .label classes
- CTA buttons use .btn-primary, cancel buttons use .btn-ghost or .btn-secondary
- Zero hardcoded color classes across all 5 modal files
- TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-components/02-02-SUMMARY.md`
</output>
