---
phase: 02-shared-components
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/src/components/browse/FilterPanel.tsx
  - apps/web/src/components/browse/RegionFilterPanel.tsx
  - apps/web/src/components/browse/EmptyState.tsx
autonomous: true
requirements:
  - COMP-09
  - COMP-10
must_haves:
  truths:
    - "FilterPanel uses semantic surface background, .label class for section headings, .badge-info for active filter indicators"
    - "RegionFilterPanel uses semantic tokens and .badge-info for active region indicator"
    - "EmptyState has zero AI slop patterns: no gradient cards, no icon-in-colored-circles, no emoji in headings"
    - "EmptyState stat cards use flat bg-surface with border, not gradient backgrounds"
  artifacts:
    - path: "apps/web/src/components/browse/FilterPanel.tsx"
      provides: "Restyled filter panel"
      contains: "bg-surface"
    - path: "apps/web/src/components/browse/RegionFilterPanel.tsx"
      provides: "Restyled region filter panel"
      contains: "bg-surface"
    - path: "apps/web/src/components/browse/EmptyState.tsx"
      provides: "Restyled empty state — AI slop removed"
      contains: "bg-surface"
  key_links:
    - from: "apps/web/src/components/browse/EmptyState.tsx"
      to: "apps/web/src/app/globals.css"
      via: ".card class, .badge-neutral class"
      pattern: "card"
---

<objective>
Restyle FilterPanel, RegionFilterPanel, and EmptyState with semantic tokens. EmptyState requires structural rework to remove AI slop patterns (gradient stat cards, icon-in-colored-circles, emoji).

Purpose: These browse components are the first interaction point for users exploring the marketplace. FilterPanel and RegionFilterPanel control the browsing experience; EmptyState is the discovery page shown when no region is selected.

Output: 3 restyled browse components with zero AI slop and full semantic token compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-components/02-CONTEXT.md
@.planning/phases/02-shared-components/02-UI-SPEC.md
@.planning/phases/02-shared-components/02-RESEARCH.md

@apps/web/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle FilterPanel and RegionFilterPanel with semantic tokens</name>
  <files>apps/web/src/components/browse/FilterPanel.tsx, apps/web/src/components/browse/RegionFilterPanel.tsx</files>
  <read_first>
    - apps/web/src/components/browse/FilterPanel.tsx (243 lines — browse filters with sections for property type, bedrooms, budget, etc.)
    - apps/web/src/components/browse/RegionFilterPanel.tsx (region-specific filter panel)
    - .planning/phases/02-shared-components/02-UI-SPEC.md (FilterPanel contract section)
    - .planning/phases/02-shared-components/02-RESEARCH.md (Pattern 1 — token replacement mapping)
    - apps/web/src/app/globals.css (verify .badge-info, .label, .input classes exist)
  </read_first>
  <action>
**FilterPanel.tsx** — Apply semantic tokens and use existing CSS classes:

1. **Panel container**: Replace `bg-white rounded-lg border border-gray-200 ...` (or similar) with `bg-surface rounded-lg border border-border`. Apply `p-4` internal padding.

2. **Section headings**: Apply `.label` class (`text-xs font-semibold uppercase tracking-wide text-text-secondary`). If headings currently use different styling, replace entirely with `.label` className.

3. **Section dividers**: `border-t border-border my-4`

4. **Filter option labels**: `text-sm text-text-base`

5. **Active filter indicators**: Replace any `bg-blue-100 text-blue-800` or `bg-primary-100 text-primary-600` badge patterns with `badge-info` class (which renders as `bg-accent-light text-accent rounded-sm`).

6. **Clear button**: `.btn-ghost text-sm`

7. **Apply button**: `.btn-primary btn-md w-full`

8. **Form inputs within filters** (selects, range inputs, checkboxes): Apply `.input` class where applicable. Checkboxes: `h-4 w-4 rounded-sm border-border text-accent focus:ring-accent-light`.

9. **Token replacements throughout:**
   - `bg-white` -> `bg-surface`
   - `bg-gray-50` / `bg-gray-100` -> `bg-surface-raised`
   - `border-gray-*` -> `border-border`
   - `text-gray-900` / `text-gray-800` -> `text-text-base`
   - `text-gray-600` / `text-gray-700` -> `text-text-secondary`
   - `text-gray-400` / `text-gray-500` -> `text-text-muted`
   - `bg-primary-*` / `text-primary-*` -> `bg-accent` / `text-accent`
   - `hover:bg-gray-*` -> `hover:bg-surface-raised`
   - `rounded-xl` -> `rounded-lg`

**RegionFilterPanel.tsx** — Same token replacement map. Apply `.badge-info` for active region indicator. Use `.label` for any section headings. Replace all hardcoded color classes with semantic equivalents.

**Do NOT change:** Filter logic, state management, callback props, or any data handling.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FilterPanel.tsx contains `bg-surface` (panel background)
    - FilterPanel.tsx contains `border-border` (panel border)
    - FilterPanel.tsx contains `btn-primary` (apply button)
    - FilterPanel.tsx does NOT contain `bg-white`
    - FilterPanel.tsx does NOT contain `text-gray-`
    - FilterPanel.tsx does NOT contain `border-gray-`
    - FilterPanel.tsx does NOT contain `bg-primary-`
    - FilterPanel.tsx does NOT contain `text-primary-`
    - RegionFilterPanel.tsx does NOT contain `bg-white`
    - RegionFilterPanel.tsx does NOT contain `text-gray-`
    - RegionFilterPanel.tsx does NOT contain `border-gray-`
    - `pnpm --filter @offmarket/web build` exits 0
  </acceptance_criteria>
  <done>FilterPanel and RegionFilterPanel use semantic tokens, .label for headings, .badge-info for active indicators, .btn-primary for apply button. Zero hardcoded color classes. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle EmptyState — remove AI slop, apply semantic tokens</name>
  <files>apps/web/src/components/browse/EmptyState.tsx</files>
  <read_first>
    - apps/web/src/components/browse/EmptyState.tsx (229 lines — full discovery page with map, stats, region list, quick actions. Contains multiple AI slop violations)
    - .planning/phases/02-shared-components/02-UI-SPEC.md (EmptyState contract section)
    - .planning/phases/02-shared-components/02-RESEARCH.md (Pattern 6 — EmptyState Structural Rework, Pitfall 7 — not a standard empty state)
  </read_first>
  <action>
**CRITICAL: Read the FULL EmptyState component before starting.** This is NOT a simple "no results" message. It's a 229-line discovery page with 5+ sub-sections (Research Pitfall 7). Each requires individual attention.

**AI Slop Removals (structural changes, not just token swaps):**

1. **Gradient stat cards** (~lines 131-138): Remove `bg-gradient-to-br from-primary-500 to-primary-600` and `bg-gradient-to-br from-accent-500 to-accent-600`. Replace with flat `.card` class (`bg-surface rounded-lg border border-border p-6`). Stat numbers: `font-display text-xl font-semibold text-text-base tabular-nums`. Stat labels: `.label` class.

2. **Icon-in-colored-circle** (~lines 199-202, 213-215): Remove `w-8 h-8 bg-primary-100 rounded-lg` and `w-8 h-8 bg-accent-100 rounded-lg` icon containers. Replace with inline icon only: `<svg className="h-5 w-5 text-text-muted" ...>` — no background circle or rounded container.

3. **Emoji in headings** (~line 143): Remove emoji character (e.g., fire emoji in "Hot Regions" heading). Use plain text heading only: `className="text-xl font-display font-semibold text-text-base mb-2"`.

4. **Color-coded rank numbers** (~lines 156-164): Remove `bg-yellow-100 text-yellow-700`, `bg-orange-100 text-orange-700` rank styling. Replace with `badge-neutral` class or plain `text-sm text-text-muted` rank numbers.

5. **`rounded-xl` containers** (~lines 122, 135, 142, 192): Replace all `rounded-xl` with `rounded-lg` (or use `.card` class which includes `rounded-lg`).

**EmptyState UI-SPEC contract values:**
- Container: `py-12 text-center` (centered is acceptable for empty states per UI-SPEC)
- Heading: `text-xl font-display font-semibold text-text-base mb-2`
- Body text: `text-sm text-text-secondary mb-6`
- Illustration: Simple outlined SVG icon (search or map pin) at `h-12 w-12 text-text-muted mx-auto mb-4`. NO colored background circle behind the icon.
- CTA button: `.btn-primary btn-md`

**Standard token replacements throughout:**
- `bg-white` -> `bg-surface`
- `bg-gray-*` -> appropriate semantic token
- `border-gray-*` -> `border-border`
- `text-gray-900`/`800` -> `text-text-base`
- `text-gray-600`/`700` -> `text-text-secondary`
- `text-gray-400`/`500` -> `text-text-muted`
- `bg-primary-*` / `text-primary-*` -> `bg-accent` / `text-accent`
- `bg-green-*` / `text-green-*` -> `bg-success/10` / `text-success`
- `bg-yellow-*` / `text-yellow-*` -> `bg-secondary-light` / `text-warning`
- `bg-red-*` / `text-red-*` (error context) -> `bg-error/10` / `text-error`

**Do NOT change:** Data fetching, region data, link hrefs, map rendering logic, or any functional behavior.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && pnpm --filter @offmarket/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - EmptyState.tsx does NOT contain `bg-gradient-to-` (no gradient backgrounds)
    - EmptyState.tsx does NOT contain `from-primary-` (no gradient from)
    - EmptyState.tsx does NOT contain `to-primary-` (no gradient to)
    - EmptyState.tsx does NOT contain `rounded-full bg-primary` (no icon-in-circle)
    - EmptyState.tsx does NOT contain `bg-primary-100` (no colored icon backgrounds)
    - EmptyState.tsx does NOT contain `bg-accent-100` (no colored icon backgrounds)
    - EmptyState.tsx does NOT contain emoji characters (fire, rocket, etc.)
    - EmptyState.tsx does NOT contain `bg-yellow-` (no color-coded ranks)
    - EmptyState.tsx does NOT contain `bg-orange-` (no color-coded ranks)
    - EmptyState.tsx does NOT contain `rounded-xl` (correct border radius)
    - EmptyState.tsx does NOT contain `bg-white`
    - EmptyState.tsx does NOT contain `text-gray-`
    - EmptyState.tsx contains `text-text-base` (semantic text token)
    - EmptyState.tsx contains `bg-surface` or `card` (semantic surface)
    - EmptyState.tsx contains `tabular-nums` (on stat numbers)
    - `pnpm --filter @offmarket/web build` exits 0
  </acceptance_criteria>
  <done>EmptyState has zero AI slop patterns. Gradient stat cards replaced with flat .card surfaces. Icon-in-circles replaced with inline icons. Emoji removed from headings. Rank colors replaced with neutral badges. All hardcoded colors replaced with semantic tokens. TypeScript compiles.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @offmarket/web build` passes
2. FilterPanel.tsx and RegionFilterPanel.tsx contain zero hardcoded color classes
3. EmptyState.tsx contains zero AI slop patterns (no gradients, no icon-in-circles, no emoji)
4. All 3 files use semantic tokens exclusively
</verification>

<success_criteria>
- FilterPanel uses .label headings, .badge-info active indicators, .btn-primary apply button
- RegionFilterPanel uses semantic tokens with .badge-info active indicator
- EmptyState structurally reworked: flat cards, inline icons, no emoji, neutral rank badges
- Zero hardcoded color classes across all 3 files
- Zero AI slop blacklist violations
- TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-components/02-03-SUMMARY.md`
</output>
