---
phase: 04-buyer-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/buyer/create/page.tsx
autonomous: true
requirements:
  - BUYR-01

must_haves:
  truths:
    - "Create wanted ad form shows DESIGN.md multi-card layout with semantic token colors in both light and dark mode"
    - "Target type selection cards use border-accent/bg-accent-light when selected, border-border/bg-surface when unselected"
    - "Property type and feature toggle badges use badge-info when active and badge-neutral when inactive (rounded-sm, not rounded-full)"
    - "Location tags display as badge-info with gap-1 and teal remove button"
    - "Post-creation success state uses success semantic tokens with flat bg-surface-raised CTA box (no gradient)"
    - "All numeric displays (budget, usage counts) use tabular-nums"
    - "Page container uses max-w-content (1120px)"
  artifacts:
    - path: "apps/web/src/app/buyer/create/page.tsx"
      provides: "Restyled create wanted ad page"
      contains: "max-w-content"
  key_links:
    - from: "apps/web/src/app/buyer/create/page.tsx"
      to: "apps/web/src/app/globals.css"
      via: "badge-info, badge-neutral, btn-primary, btn-secondary, card, input, label CSS classes"
      pattern: "badge-info|badge-neutral|btn-primary|btn-secondary|card|input"
---

<objective>
Restyle the create wanted ad page (766 lines) with DESIGN.md semantic tokens, replacing all hardcoded gray-*, primary-*, red-*, green-* Tailwind classes.

Purpose: BUYR-01 — the create ad form is the primary buyer conversion flow and must match the design system established in Phases 1-3.
Output: Fully restyled create/page.tsx with zero hardcoded color classes remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-buyer-pages/04-CONTEXT.md
@.planning/phases/04-buyer-pages/04-RESEARCH.md
@.planning/phases/04-buyer-pages/04-UI-SPEC.md
@apps/web/src/app/globals.css
@apps/web/tailwind.config.ts
@apps/web/src/app/buyer/create/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle create wanted ad page with DESIGN.md tokens</name>
  <files>apps/web/src/app/buyer/create/page.tsx</files>
  <read_first>
    - apps/web/src/app/buyer/create/page.tsx (current state — 766 lines, read fully before any edits)
    - apps/web/src/app/globals.css (badge-info, badge-neutral, btn-primary, btn-secondary, card, input, label class definitions)
    - apps/web/tailwind.config.ts (confirm max-w-content = 1120px at line 58)
    - .planning/phases/04-buyer-pages/04-RESEARCH.md (complete token swap table and code examples)
    - .planning/phases/04-buyer-pages/04-UI-SPEC.md (interaction contracts, anti-patterns list)
    - DESIGN.md (design authority)
  </read_first>
  <action>
Work through create/page.tsx top-to-bottom, applying every token swap from the RESEARCH.md swap table. The file has 3 render states (loading, success, form) — handle each:

**Loading skeleton (near top of render):**
- Replace `max-w-3xl` with `max-w-content` per D-12
- Replace all `bg-gray-200` skeleton bars with `bg-surface-raised` per D-16
- Replace `bg-gray-50` backgrounds with `bg-surface-raised`

**Success state (lines ~233-300, per D-09):**
- Success circle: `bg-green-100` -> `bg-success-light`, keep `rounded-full`
- Check icon: `text-green-600` -> `text-success`
- Postcard CTA box: replace `bg-gradient-to-r from-primary-50 to-primary-100 border border-primary-200 rounded-xl` with `bg-surface-raised border border-border rounded-lg` (flat, no gradient)
- CTA icon container: `p-3 bg-white rounded-lg shadow-sm shrink-0` -> `p-3 bg-surface rounded-md border border-border shrink-0`
- CTA icon: `text-primary-600` -> `text-accent`
- Success heading text: `text-gray-900` -> `text-primary`
- Success body text: `text-gray-600` -> `text-secondary`
- Replace `max-w-xl` with `max-w-content` per D-12
- Replace `btn-outline` at line ~280 with `btn-secondary` (btn-outline does not exist in globals.css, per Pitfall 6)

**Form container:**
- Replace `max-w-3xl` with `max-w-content` per D-12
- Page heading: `text-gray-900` -> `text-primary`, ensure `font-display` for General Sans
- Subheading/description: `text-gray-600` -> `text-secondary`

**Usage summary bar:**
- Container: `bg-gray-50 rounded-lg` -> `bg-surface-raised rounded-md`
- Label text: `text-gray-600` -> `text-secondary`
- Count text: `text-primary-600` -> `text-accent` for unlimited, `text-red-600` -> `text-error` for zero remaining
- Add `tabular-nums` to all numeric count spans per D-17
- Upgrade link: `text-primary-600 hover:text-primary-800` -> `text-accent hover:text-accent-hover`

**Target type selection cards (lines ~356-435, per D-02):**
- Selected state: `border-primary-500 bg-primary-50 ring-2 ring-primary-200` -> `border-accent bg-accent-light ring-2 ring-accent-light`
- Unselected state: `border-gray-200 hover:border-gray-300 hover:bg-gray-50` -> `border-border hover:border-border hover:bg-surface-raised`
- Disabled state: `border-gray-200 bg-gray-50 opacity-75` -> `border-border bg-surface-raised opacity-75`
- Icon bg active: `bg-primary-100` -> `bg-accent-light`; inactive: `bg-gray-100` -> `bg-surface-raised`
- Icon color active: `text-primary-600` -> `text-accent`; inactive: `text-gray-500` -> `text-muted`
- Card title active: `text-primary-700` -> `text-accent`; inactive: `text-gray-900` -> `text-primary`
- Card description: `text-gray-500` -> `text-muted`

**Location tags (lines ~509-526, per D-04):**
- Tag container: replace `inline-flex items-center gap-1 px-3 py-1 bg-primary-100 text-primary-800 rounded-full text-sm` with `badge-info gap-1`
- Remove button inside tag: `text-primary-600 hover:text-primary-800` -> `text-accent hover:text-accent-hover`

**Property type toggle badges (lines ~633-648, per D-03):**
- Active: replace `px-3 py-1.5 rounded-full text-sm font-medium bg-primary-600 text-white` with `badge-info cursor-pointer transition-colors`
- Inactive: replace `bg-gray-100 text-gray-700 hover:bg-gray-200` with `badge-neutral cursor-pointer hover:bg-surface-raised transition-colors`
- Remove ALL `px-*`, `py-*`, `rounded-*`, `text-sm`, `font-medium` — badge-info/badge-neutral provide these

**Feature selection badges (lines ~711-726, per D-03):**
- Same pattern as property type toggles above
- Active: `badge-info cursor-pointer transition-colors`
- Inactive: `badge-neutral cursor-pointer hover:bg-surface-raised transition-colors`
- Remove all inline sizing classes

**Error container:**
- `bg-red-50 border border-red-200` -> `bg-error-light border border-error`
- `text-red-700` / `text-red-600` -> `text-error` per D-14

**All form labels:** ensure they use the `.label` CSS class
**All form inputs/selects/textareas:** ensure they use the `.input` CSS class
**All card sections:** ensure they use the `.card` CSS class

**All remaining gray-* swaps per D-13:**
- `text-gray-900` -> `text-primary`
- `text-gray-700` -> `text-primary`
- `text-gray-600` -> `text-secondary`
- `text-gray-500` -> `text-muted`
- `text-gray-400` -> `text-muted`
- `bg-gray-50` -> `bg-surface-raised`
- `bg-gray-100` -> `bg-surface-raised`
- `border-gray-200` -> `border-border`
- `border-gray-300` -> `border-border`
- `hover:bg-gray-50` -> `hover:bg-surface-raised`

**All remaining primary-* swaps per D-15:**
- `text-primary-600` -> `text-accent`
- `text-primary-700` -> `text-accent`
- `text-primary-800` -> `text-accent`
- `bg-primary-50` -> `bg-accent-light`
- `bg-primary-100` -> `bg-accent-light`
- `bg-primary-600` -> `bg-accent`
- `border-primary-200` -> `border-accent`
- `border-primary-500` -> `border-accent`
- `ring-primary-200` -> `ring-accent-light`

**Budget display:** add `tabular-nums` to any element showing formatNZD output per D-17.

**Final verification pass:** grep the entire file for `gray-`, `primary-`, `red-`, `green-`, `amber-`, `yellow-`, `blue-`, `gradient`, `rounded-full` (on badges only), `max-w-3xl`, `max-w-xl`, `btn-outline` — none should remain.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -cE "gray-[0-9]|primary-[0-9]|red-[0-9]|green-[0-9]|amber-[0-9]|yellow-[0-9]|blue-[0-9]|bg-gradient|max-w-3xl|max-w-xl|btn-outline" apps/web/src/app/buyer/create/page.tsx | grep -q "^0$" && echo "PASS: No hardcoded colors remain" || echo "FAIL: Hardcoded colors found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "max-w-content" apps/web/src/app/buyer/create/page.tsx` returns 2 or more (loading + form containers)
    - `grep -c "badge-info" apps/web/src/app/buyer/create/page.tsx` returns 3 or more (property types, features, location tags)
    - `grep -c "badge-neutral" apps/web/src/app/buyer/create/page.tsx` returns 2 or more (inactive property types, inactive features)
    - `grep -c "bg-success-light" apps/web/src/app/buyer/create/page.tsx` returns 1 or more (success circle)
    - `grep -c "bg-surface-raised" apps/web/src/app/buyer/create/page.tsx` returns 3 or more (skeleton, CTA box, usage bar)
    - `grep -c "tabular-nums" apps/web/src/app/buyer/create/page.tsx` returns 2 or more (budget, usage counts)
    - `grep -c "btn-secondary" apps/web/src/app/buyer/create/page.tsx` returns 1 or more (replaces btn-outline)
    - `grep -cE "gray-[0-9]|primary-[0-9]|red-[0-9]|green-[0-9]|amber-[0-9]|yellow-[0-9]|blue-[0-9]|bg-gradient|max-w-3xl|max-w-xl|btn-outline" apps/web/src/app/buyer/create/page.tsx` returns 0
    - `pnpm --filter web build` completes without errors (TypeScript compilation passes)
  </acceptance_criteria>
  <done>Create wanted ad page uses only DESIGN.md semantic tokens. Zero hardcoded color classes remain. All badges use badge-info/badge-neutral (rounded-sm). Success state uses semantic success tokens with flat CTA box. Page container is max-w-content (1120px). All numbers use tabular-nums.</done>
</task>

</tasks>

<verification>
1. `grep -cE "gray-[0-9]|primary-[0-9]|red-[0-9]|green-[0-9]|amber-[0-9]|yellow-[0-9]|blue-[0-9]|bg-gradient|max-w-3xl|max-w-xl|btn-outline" apps/web/src/app/buyer/create/page.tsx` returns 0
2. `pnpm --filter web build` passes
3. Visual check: create page renders correctly in browser (light + dark mode)
</verification>

<success_criteria>
- Create wanted ad page contains zero hardcoded color classes (gray-*, primary-*, red-*, green-*, amber-*, yellow-*, blue-*)
- All selection badges use badge-info/badge-neutral pattern (rounded-sm, not rounded-full)
- Target type cards use border-accent/bg-accent-light selected state
- Success state uses success tokens, flat CTA box (no gradient)
- Page container is max-w-content (1120px)
- All numeric displays use tabular-nums
- TypeScript build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-buyer-pages/04-01-SUMMARY.md`
</output>
