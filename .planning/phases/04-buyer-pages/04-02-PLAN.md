---
phase: 04-buyer-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/buyer/my-ads/page.tsx
  - apps/web/src/app/buyer/postcards/page.tsx
autonomous: true
requirements:
  - BUYR-02
  - BUYR-03

must_haves:
  truths:
    - "My ads listing shows cards with badge-info location tags, text-accent tabular-nums match counts, and teal action buttons"
    - "My ads empty state uses EmptyState component pattern with semantic tokens"
    - "Postcards page shows badge-warning/info/success/error status badges via STATUS_LABELS.badgeClass"
    - "Postcards allowance display uses badge-info with tabular-nums on all numbers"
    - "How Postcards Work info section uses bg-surface-raised and text-primary/text-secondary"
    - "Both pages use max-w-content (1120px) containers"
    - "No hardcoded gray-*, primary-*, red-*, green-*, amber-*, yellow-*, blue-* classes remain in either file"
  artifacts:
    - path: "apps/web/src/app/buyer/my-ads/page.tsx"
      provides: "Restyled my ads listing page"
      contains: "max-w-content"
    - path: "apps/web/src/app/buyer/postcards/page.tsx"
      provides: "Restyled postcards page"
      contains: "badgeClass"
  key_links:
    - from: "apps/web/src/app/buyer/my-ads/page.tsx"
      to: "apps/web/src/app/globals.css"
      via: "badge-info, badge-neutral, card, btn-primary CSS classes"
      pattern: "badge-info|badge-neutral|card"
    - from: "apps/web/src/app/buyer/postcards/page.tsx"
      to: "apps/web/src/app/globals.css"
      via: "badge-warning, badge-info, badge-success, badge-error CSS classes"
      pattern: "badge-warning|badge-info|badge-success|badge-error"
---

<objective>
Restyle the my ads listing page (267 lines) and postcards page (298 lines) with DESIGN.md semantic tokens.

Purpose: BUYR-02 and BUYR-03 — the ad management and postcard pages complete the buyer workflow styling.
Output: Both pages fully restyled with zero hardcoded color classes remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-buyer-pages/04-CONTEXT.md
@.planning/phases/04-buyer-pages/04-RESEARCH.md
@.planning/phases/04-buyer-pages/04-UI-SPEC.md
@apps/web/src/app/globals.css
@apps/web/tailwind.config.ts
@apps/web/src/app/buyer/my-ads/page.tsx
@apps/web/src/app/buyer/postcards/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle my ads listing page with DESIGN.md tokens</name>
  <files>apps/web/src/app/buyer/my-ads/page.tsx</files>
  <read_first>
    - apps/web/src/app/buyer/my-ads/page.tsx (current state — 267 lines, read fully before any edits)
    - apps/web/src/app/globals.css (badge-info, badge-neutral, card, btn-primary class definitions)
    - apps/web/tailwind.config.ts (confirm max-w-content = 1120px at line 58)
    - .planning/phases/04-buyer-pages/04-RESEARCH.md (Pattern 7: Specific Address Indicator, Pattern 8: Delete Button, full token swap table)
    - .planning/phases/04-buyer-pages/04-UI-SPEC.md (Match Count Display contract, anti-patterns list)
    - DESIGN.md (design authority)
  </read_first>
  <action>
Work through my-ads/page.tsx top-to-bottom, applying every token swap from RESEARCH.md:

**Page container:**
- Replace `max-w-4xl` with `max-w-content` per D-12

**Loading skeleton:**
- Replace all `bg-gray-200` with `bg-surface-raised` per D-16
- Replace container `max-w-4xl` with `max-w-content`

**Error container:**
- `bg-red-50 border border-red-200` -> `bg-error-light border border-error` per D-14
- `text-red-700` / `text-red-600` -> `text-error`

**Page heading:**
- `text-gray-900` -> `text-primary`
- Ensure heading uses `font-display` for General Sans

**Ad cards (per D-06, D-07):**
- Card container: ensure `.card` class is used
- Match count number (lines ~172-191): `text-2xl font-bold text-primary-600` -> `text-2xl font-semibold text-accent tabular-nums` (font-bold -> font-semibold per UI-SPEC anti-pattern)
- "matches" label: `text-gray-500` -> `text-secondary`
- Specific address indicator circle: `w-10 h-10 mx-auto rounded-full bg-amber-100` -> `w-10 h-10 mx-auto rounded-full bg-accent-light` per D-07
- House icon: `text-amber-600` -> `text-accent` per D-07
- Location area tags: `inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-primary-100 text-primary-800` -> `badge-info`
- Overflow "+N more" tag: `inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-600` -> `badge-neutral`
- Specific address tag: `inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium bg-amber-100 text-amber-800` -> `badge-info gap-1`
- Budget display: add `tabular-nums` to element showing formatNZD output, swap `text-primary-600 font-medium` -> `text-accent font-medium tabular-nums`
- Card body text: `text-gray-600` -> `text-secondary`
- Card metadata: `text-gray-500` -> `text-muted`

**Action buttons:**
- View/Edit button: ensure `.btn-primary` or `text-accent hover:text-accent-hover` as appropriate
- Delete button: `text-red-600 hover:text-red-800` -> `text-sm text-error hover:text-error` (subtle text link per RESEARCH.md Option A recommendation)

**Empty state (per D-10):**
- Swap any hardcoded color to semantic tokens
- Ensure EmptyState component pattern is followed
- CTA button: ensure `.btn-primary`

**All remaining gray-* swaps per D-13:**
- `text-gray-900` -> `text-primary`
- `text-gray-700` -> `text-primary`
- `text-gray-600` -> `text-secondary`
- `text-gray-500` -> `text-muted`
- `text-gray-400` -> `text-muted`
- `bg-gray-50` -> `bg-surface-raised`
- `bg-gray-100` -> `bg-surface-raised`
- `border-gray-200` -> `border-border`
- `hover:bg-gray-50` -> `hover:bg-surface-raised`

**All remaining primary-* swaps per D-15:**
- `text-primary-600` -> `text-accent`
- `bg-primary-100` -> `bg-accent-light`
- `text-primary-800` -> `text-accent`

**Final verification pass:** grep for `gray-`, `primary-`, `red-`, `green-`, `amber-`, `yellow-`, `blue-`, `max-w-4xl`, `rounded-full` on badges, `font-bold` — none should remain.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -cE "gray-[0-9]|primary-[0-9]|red-[0-9]|green-[0-9]|amber-[0-9]|yellow-[0-9]|blue-[0-9]|max-w-4xl" apps/web/src/app/buyer/my-ads/page.tsx | grep -q "^0$" && echo "PASS: No hardcoded colors remain" || echo "FAIL: Hardcoded colors found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "max-w-content" apps/web/src/app/buyer/my-ads/page.tsx` returns 1 or more
    - `grep -c "badge-info" apps/web/src/app/buyer/my-ads/page.tsx` returns 2 or more (location tags, specific address tag)
    - `grep -c "badge-neutral" apps/web/src/app/buyer/my-ads/page.tsx` returns 1 or more (overflow tag)
    - `grep -c "tabular-nums" apps/web/src/app/buyer/my-ads/page.tsx` returns 2 or more (match count, budget)
    - `grep -c "text-accent" apps/web/src/app/buyer/my-ads/page.tsx` returns 2 or more (match count, budget, icons)
    - `grep -c "bg-accent-light" apps/web/src/app/buyer/my-ads/page.tsx` returns 1 or more (specific address indicator)
    - `grep -c "text-error" apps/web/src/app/buyer/my-ads/page.tsx` returns 1 or more (delete button)
    - `grep -cE "gray-[0-9]|primary-[0-9]|red-[0-9]|green-[0-9]|amber-[0-9]|yellow-[0-9]|blue-[0-9]|max-w-4xl" apps/web/src/app/buyer/my-ads/page.tsx` returns 0
  </acceptance_criteria>
  <done>My ads listing page uses only DESIGN.md semantic tokens. Location tags are badge-info. Match counts are text-accent tabular-nums. Specific address indicator uses bg-accent-light. Delete button uses text-error. Page container is max-w-content. Zero hardcoded color classes remain.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle postcards page with DESIGN.md tokens and STATUS_LABELS refactor</name>
  <files>apps/web/src/app/buyer/postcards/page.tsx</files>
  <read_first>
    - apps/web/src/app/buyer/postcards/page.tsx (current state — 298 lines, read fully before any edits)
    - apps/web/src/app/globals.css (badge-warning, badge-info, badge-success, badge-error class definitions at lines 113-117)
    - apps/web/tailwind.config.ts (confirm max-w-content = 1120px)
    - .planning/phases/04-buyer-pages/04-RESEARCH.md (Pattern 2: STATUS_LABELS Dictionary Replacement — exact before/after code)
    - .planning/phases/04-buyer-pages/04-UI-SPEC.md (Status Badges contract, Postcard Allowance Display, How Postcards Work info section)
    - DESIGN.md (design authority)
  </read_first>
  <action>
Work through postcards/page.tsx top-to-bottom:

**STATUS_LABELS dictionary (lines ~34-41, per D-05):**
Replace the entire dictionary. Rename the `color` field to `badgeClass`:
```typescript
const STATUS_LABELS: Record<string, { label: string; badgeClass: string }> = {
  PENDING:   { label: "Pending Review", badgeClass: "badge-warning" },
  APPROVED:  { label: "Approved",       badgeClass: "badge-info" },
  REJECTED:  { label: "Rejected",       badgeClass: "badge-error" },
  SENT:      { label: "Sent",           badgeClass: "badge-success" },
  DELIVERED: { label: "Delivered",      badgeClass: "badge-success" },
  FAILED:    { label: "Failed",         badgeClass: "badge-error" },
};
```

**STATUS_LABELS usage (line ~204 and fallback ~193-195):**
Update every reference from `statusInfo.color` to `statusInfo.badgeClass`.
Replace the `<span>` wrapper: `<span className={`inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium ${statusInfo.color}`}>` becomes `<span className={statusInfo.badgeClass}>` — the badge-* classes provide all the inline styles (padding, rounded-sm, text-xs, font-semibold).

**Page container:**
- Replace `max-w-4xl` with `max-w-content` per D-12

**Loading skeleton:**
- Replace all `bg-gray-200` with `bg-surface-raised` per D-16
- Replace container max-w with `max-w-content`

**Error container:**
- `bg-red-50 border border-red-200` -> `bg-error-light border border-error` per D-14
- `text-red-700` / `text-red-600` -> `text-error`

**Page heading:**
- `text-gray-900` -> `text-primary`
- Ensure heading uses `font-display` for General Sans

**Postcard allowance display (per D-08):**
- "N free left" badge: `bg-primary-100 text-primary-700 rounded-full` -> `badge-info` (remove inline padding/rounded classes)
- Usage line "N of M used": `text-xs text-gray-500` -> `text-xs text-secondary tabular-nums`
- All numbers in allowance display: add `tabular-nums`

**Postcard list cards:**
- Card container: ensure `.card` class
- Card text: `text-gray-900` -> `text-primary`, `text-gray-600` -> `text-secondary`, `text-gray-500` -> `text-muted`
- Postcard cost display: add `tabular-nums` per D-17
- Any `border-gray-200` -> `border-border`

**"How Postcards Work" info section (per D-11):**
- Container: `bg-gray-50` -> `bg-surface-raised`
- Heading: `text-gray-900` -> `text-primary`
- List text: `text-gray-600` -> `text-secondary`

**Empty states (per D-10):**
- Swap hardcoded colors to semantic tokens
- Ensure EmptyState component pattern is followed

**"Limit reached" badge:**
- `bg-amber-100 text-amber-700` -> `badge-warning`

**All remaining gray-* swaps per D-13:**
- `text-gray-900` -> `text-primary`
- `text-gray-600` -> `text-secondary`
- `text-gray-500` -> `text-muted`
- `bg-gray-50` -> `bg-surface-raised`
- `bg-gray-100` -> `bg-surface-raised`
- `border-gray-200` -> `border-border`
- `hover:bg-gray-50` -> `hover:bg-surface-raised`

**All remaining primary-* swaps per D-15:**
- `text-primary-600` -> `text-accent`
- `text-primary-700` -> `text-accent`
- `bg-primary-100` -> `bg-accent-light`
- `text-primary-800` -> `text-accent`

**Final verification pass:** grep for `gray-`, `primary-`, `red-`, `green-`, `amber-`, `yellow-`, `blue-`, `max-w-4xl`, `statusInfo.color`, `rounded-full` on badges — none should remain.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/offmarket && grep -cE "gray-[0-9]|primary-[0-9]|red-[0-9]|green-[0-9]|amber-[0-9]|yellow-[0-9]|blue-[0-9]|max-w-4xl|statusInfo\.color" apps/web/src/app/buyer/postcards/page.tsx | grep -q "^0$" && echo "PASS: No hardcoded colors remain" || echo "FAIL: Hardcoded colors found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "badgeClass" apps/web/src/app/buyer/postcards/page.tsx` returns 7 or more (6 in STATUS_LABELS + 1+ usage)
    - `grep -c "badge-warning" apps/web/src/app/buyer/postcards/page.tsx` returns 1 or more (PENDING status)
    - `grep -c "badge-info" apps/web/src/app/buyer/postcards/page.tsx` returns 1 or more (APPROVED status and/or allowance badge)
    - `grep -c "badge-success" apps/web/src/app/buyer/postcards/page.tsx` returns 2 or more (SENT + DELIVERED)
    - `grep -c "badge-error" apps/web/src/app/buyer/postcards/page.tsx` returns 2 or more (REJECTED + FAILED)
    - `grep -c "max-w-content" apps/web/src/app/buyer/postcards/page.tsx` returns 1 or more
    - `grep -c "tabular-nums" apps/web/src/app/buyer/postcards/page.tsx` returns 2 or more (allowance numbers, cost)
    - `grep -c "bg-surface-raised" apps/web/src/app/buyer/postcards/page.tsx` returns 2 or more (info section, skeletons)
    - `grep -c "statusInfo.color" apps/web/src/app/buyer/postcards/page.tsx` returns 0 (fully migrated to badgeClass)
    - `grep -cE "gray-[0-9]|primary-[0-9]|red-[0-9]|green-[0-9]|amber-[0-9]|yellow-[0-9]|blue-[0-9]|max-w-4xl" apps/web/src/app/buyer/postcards/page.tsx` returns 0
    - `pnpm --filter web build` completes without errors (TypeScript compilation passes — confirms statusInfo.badgeClass type is correct)
  </acceptance_criteria>
  <done>Postcards page uses only DESIGN.md semantic tokens. STATUS_LABELS dictionary uses badgeClass field with badge-warning/info/success/error classes. Allowance display uses badge-info with tabular-nums. Info section uses bg-surface-raised. Page container is max-w-content. Zero hardcoded color classes remain. TypeScript build passes.</done>
</task>

</tasks>

<verification>
1. `grep -cE "gray-[0-9]|primary-[0-9]|red-[0-9]|green-[0-9]|amber-[0-9]|yellow-[0-9]|blue-[0-9]|max-w-4xl" apps/web/src/app/buyer/my-ads/page.tsx apps/web/src/app/buyer/postcards/page.tsx` returns 0 for both files
2. `grep -c "statusInfo.color" apps/web/src/app/buyer/postcards/page.tsx` returns 0
3. `pnpm --filter web build` passes
4. Visual check: both pages render correctly in browser (light + dark mode)
</verification>

<success_criteria>
- My ads page contains zero hardcoded color classes; location tags are badge-info; match counts use text-accent tabular-nums; specific address indicator uses bg-accent-light
- Postcards page contains zero hardcoded color classes; STATUS_LABELS uses badgeClass field; all status badges use semantic badge-* classes; allowance uses badge-info with tabular-nums
- Both pages use max-w-content (1120px) containers
- TypeScript build passes (statusInfo.badgeClass type is correct)
</success_criteria>

<output>
After completion, create `.planning/phases/04-buyer-pages/04-02-SUMMARY.md`
</output>
